# 전사 백로그 (누적·우선순위)

> 기획팀이 관리. 완료 항목은 ~~취소선~~ 또는 하단 이동.

## P0 — 계약서 자동화 서비스 1차 (최우선)

> ★병목 갱신(2026-06-21 21:00): **M1 verbatim 실물 대조가 17:13 통과**(커밋 `6555a19` — 본문 31조 112문장 100%·별첨4 105/119=88%, 미일치 14건=빌더 치환자·케이스별 선택조항=엔진오류 아님). 어제부터의 ★최우선 병목 해소. → **다음 게이트 = 대표님의 1차 출시 선언**(자체 판정 추인 + 추가 정본 교차 여부) + 마케팅 M4(상표·도메인 미조회 지속). 개발팀 비-산출물은 계약·상담 양 축 + joint 패리티까지 소진.
- [ ] (사업팀) 1차 자동화 대상 계약서 종류·구조·필수조항·필드 정의서 작성
- [ ] (개발팀) 현행 엔진(담보신탁 7종 + 공동사업협약) 동작 검증 — 입력→미리보기→DOCX
- [ ] (개발팀) 1차 대상 계약서 모델/스키마/조문 엔진 반영
- [x] (사업팀) **산출 DOCX 조문 정확성 검수(verbatim 대조) — ★1차 출시 게이트 핵심 관문 통과** — 한투 표준 담보신탁계약서 정본 DOCX 대조 결과: 본문 제1~31조 112문장 verbatim **100% 일치**(2차 정본 교차검증 99.1%, 차이=빈칸표기뿐), 별첨4 신탁특약 **105/119문장(88%)** 일치·미일치 14건=빌더 치환자(`{{REPRESENTATIVE_TRUSTOR}}`·`{{AGENT_BANK}}` 등)·케이스별 조건부 선택조항(해당 정본 미선택)=**엔진 오류 아님**. 대조 도구 `compare-clauses.py`·산출물(계약서 전문·딜명)은 대외비·`.gitignore` 처리, body.ts/annex4.ts 주석에 결과 기록. 커밋 `6555a19`(17:13). 2026-06-21 완료. → ★남은 것 = 미일치 14건 "엔진오류 아님" 자체 판정의 사업팀/대표님 추인 + (권장) 추가 정본 교차 → 1차 출시 선언.
- [x] (개발팀) **우선수익자 순위(선·후순위) 순서 변경(▲▼) + 순위 라벨 — 배열 순서=법적 우선순위를 입력 지점에서 통제** — 담보신탁에서 우선수익자(`priorities`) 배열 순서는 곧 법적 우선순위인데(산출물이 배열 순서대로 "제N순위 우선수익자" 표·날인을 박고 `builders.js`, 본문 제3조 "최선순위 우선수익자의 의사가 의사결정 좌우"·제22조 환가·정산 순위도 이 순서를 따름), 입력 UI는 카드를 "우선수익자 1/2" **순번으로만** 보여줄 뿐 ① 순서가 법적 선·후순위임을 미고지 ② 순서 변경 수단 **전무**(삭제 후 재입력만)였던 갭 마감. 순수 `moveInArray`(model.ts, **인접 ±1 교환만**·범위 밖/dir≠±1 **동일 참조 no-op**·입력 무변형 불변) + 표시용 `priorityRankLabel(idx)=제N순위`(calc.ts, 빌더 출력 의미 일치·조문/빌더 무접촉) + `moveParty(role,idx,dir)`(contractStore, **priorities 만 재계산**=updateParty 정책·no-op 분기) + PartyCard `orderable`/`count`/`rankNote` props·▲▼ 버튼(**경계 disabled**·aria-label)·순위 배지 + StepPriority **우선수익자에만** 전달(idx0=최선순위)·"순서=법적 우선순위" 안내문. 회귀 가드 `verify-priority-reorder.mjs` **41/41 PASS**([A]교환·경계 no-op·불변 [B]제N순위 [C~E]store/PartyCard/StepPriority 배선·최선순위·orderable 1회 [F]무회귀) + verify-all **88/88·단언 2546/0** 회귀無, tsc/build green(전 10라우트), Playwright 가시 확인(제1순위·최선순위/제2순위 배지·경계 ▲▼ disabled·▼/▲ 양방향 순서 교환 시 순위 라벨·이름 정합 갱신·콘솔 0). 커밋 `4aa7664`. 조문·엔진 계산·산출물 빌더 무접촉(배열 순서 입력 통제·표시/접근성 경계만). 2026-06-22 완료. → **우선수익자 선·후순위 입력 통제 완비**(순서 변경 + 순위 가시화). ※동순위(공동순위) 라벨은 데이터 모델·조문(제3조 동순위 전원 동의) 정의 선행 필요로 별도.
- [x] (개발팀) **위탁자 순서 변경(▲▼) + "대표위탁자" 라벨 — 배열 순서=대표위탁자(별첨4) 결정을 입력 지점에서 통제** — 담보신탁에서 위탁자(`trustors`) 배열의 첫 번째(`trustors[0]`)는 곧 **대표위탁자**다(별첨4 신탁특약이 첫 위탁자를 "대표위탁자"로 선임해 위탁자 전원을 대리하여 신탁해지 포함 권한 행사: `builders.js` `representativeTrustor=trustors[0]`→`{{REPRESENTATIVE_TRUSTOR}}`·`annex.ts` 동일, 관계사 표·날인·파일명도 순서 추종). 즉 위탁자 순서=대표위탁자 결정이라는 큰 법적 효력인데, 입력 UI는 ① 첫 카드=대표위탁자 미고지 ② 순서 변경 수단 전무(삭제 후 재입력만)였던 갭 마감. ★직전 우선수익자 순서 변경(`4aa7664`)이 "위탁자 순위 무의미"로 분류했으나 위탁자는 대표위탁자 결정으로 순서가 큰 의미를 가짐을 빌더 근거로 확인 → 위탁자에 한해 확장(채무자/수익자는 실제 무의미=미적용). 우선수익자 인프라(`moveInArray`·store `moveParty(role,…)`·PartyCard `orderable/count/rankNote`) 재사용 + 표시용 `trustorRankLabel(idx)`(calc.ts, idx0 "대표위탁자"·그외 ""·빌더 trustors[0] 의미 일치) + PartyCard 역할별 순서 문구 props `orderNoun/moveUpHint/moveDownHint`(기본=순위/선순위로/후순위로=우선수익자 무변경, 위탁자=순서/대표위탁자 쪽으로/뒤로) + StepParties 위탁자 카드 배선(복수일 때만 rankNote·안내문 "맨 위=대표위탁자, 별첨4, ▲▼"). 회귀 가드 `verify-trustor-reorder.mjs` **32/32 PASS**(★[A]법적 근거=builders/annex trustors[0]·별첨4 조문 verbatim [C]★behavioral=순서 교체 시 대표위탁자 갑개발→을산업 변경) + `verify-priority-reorder` 41/41(orderable 2회 갱신) + verify-all **89/89·단언 2578/0** 회귀無, tsc/build green(전 10라우트), Playwright 가시 확인(1인=배지/▲▼/안내문 미노출 / 2인=위탁자1 "대표위탁자" 배지·경계 ▲▼ disabled·▼ 클릭 시 을산업↔갑개발 순서 교환+대표위탁자 동행·콘솔 0). 커밋 `be127a2`. 조문·엔진 계산·산출물 빌더 무접촉(배열 순서 입력 통제·표시/접근성 경계만). 2026-06-22 완료. → **위탁자 대표 순서 입력 통제 완비**(대표위탁자 가시화 + 순서 변경). 채무자/수익자는 순서 무의미로 미적용.
- [x] (개발팀) **내 계약 목록 — 계약 이름 변경(rename) 인라인 편집** — 목록은 삭제(+실행취소 큐)·복제·백업/복원·검색·정렬·일괄생성까지 갖췄으나 **저장된 계약 제목을 바꿀 수단이 전무**했다(제목은 저장 시 자동 생성·복제 시 "(사본)" 부여). 실무는 같은 위탁자·여러 사본을 **제목(딜명)으로 구분**(검색·정렬·카드 식별의 핵심)하므로 카드에서 바로 고칠 수 있어야 함 — `contractRepo` 데이터 관리 함수군 중 rename 만 누락이던 갭 마감. 순수 `normalizeTitle`(트림·빈→"제목 없음") + 순수 `renameRow`(★**제목만 변경**=`form_data`·`doc_type`·`status`·`created_at` 보존, `updated_at` 만 갱신, 입력 무변형·id 미존재 no-op) + async `renameContract` 신설, ContractsView 카드 제목 인라인 편집(`editId`/`editVal`, **Enter 저장/Esc 취소**, autoFocus, 이름변경 버튼=생성 중·편집 중 disabled, ★**편집 영역 stopPropagation 으로 카드 열기와 분리**). 회귀 가드 `verify-contracts-rename.mjs` **29/29 PASS**(normalizeTitle·renameRow 보존/불변/no-op·ContractsView 배선) + verify-all **87/87·단언 2505/0** 회귀無, tsc/build green(전 10라우트), Playwright 가시 확인(이름변경→autoFocus→Enter 저장·updated_at 갱신·목록 유지 / Esc 취소→복원·미저장 / 빈 제목→"제목 없음"·콘솔 0). 조문·엔진·검증 게이트·생성 로직·산출물·form_data 무접촉(표시 라벨만). 2026-06-22 완료. → **내 계약 데이터 관리 5축 완비**(삭제+실행취소·복제·백업/복원·정렬/검색·이름변경).
- [x] (개발팀) **상담(advisor·Pillar 2) 답변 본문 마크다운 헤딩 강등 렌더(h3 이하) — `#`/`##`/`###` 가 페이지 헤딩 아웃라인을 '문서 레벨'에서 오염시키던 a11y 갭(WCAG 1.3.1/2.4.6/2.4.10)** — 페르소나(`route.ts`)가 "마크다운 제목 적극 사용"을 지시해 LLM 이 `#`/`##`/`###` 를 자주 출력하고 react-markdown 이 그대로 h1/h2/h3 로 렌더하는데, `/advisor` 페이지엔 **h1 이 전혀 없고**(브랜드·breadcrumb 은 헤딩 아님) 빈 상태 h2("무엇을 도와드릴까요?")조차 대화 시작 시 사라진다 → 답변의 첫 헤딩이 **페이지 사실상 최상위 헤딩**이 되어 다중 h1·레벨 건너뜀·답변 귀속 불명으로 SR 헤딩 탐색이 망가지던 갭 마감. 순수 `demotedHeadingLevel`/`demotedHeadingTag`(markdown.ts, **offset +2·h6 클램프**=h1→h3·h2→h4·h3→h5·h4↓→h6, 상대 계층 보존·범위 밖 안전) + `makeHeading`+`MD_COMPONENTS: Components`(AdvisorChat, h1~h6→강등 태그+원본 레벨 클래스 `md-h md-h-N`)를 정상 답변 ReactMarkdown 에 `components` 전달 + globals.css 태그 기반 `.md h1/h2/h3` 규칙 **제거**→원본 레벨 클래스 `.md-h-N`(19/17/15px·brown)로 **시각 동일 보존**(`.md`·ReactMarkdown 은 advisor 전용=타 화면 무영향). 회귀 가드 `verify-advisor-heading-demote.mjs` **43/43 PASS**([A]순수 함수=강등 매핑·클램프·★모든 강등 ≥3(h1/h2 절대 미렌더)·단조성 [B]배선=6키·`components` 전달 [C]CSS 보존 [D]★오염 차단=`.md h1/h2/h3` 제거·리터럴 `<h1>` 0·빈 상태 h2 보존 [E]무회귀=remarkGfm·`.sr-only` 라벨·`.md`·parseAction) + verify-all **92/92·단언 2672/0** 회귀無, tsc/build green(전 10라우트), Playwright 실 API 가시 확인(`#`→H3·19px·`##`→H4·17px·`###`→H5·15px + 페이지 전역 **h1 0·h2 0**(오염 차단 실증)·표·출처·피드백 표시 무변경·콘솔 0). 커밋 `73eba5f`(내 4파일만). 조문·검색·캐시 판정·페르소나·로깅·산출물 무접촉(표시/접근성 경계만). 2026-06-22 완료. → **상담 답변 헤딩 아웃라인 SR 정합 완비**(발화자 턴 라벨[778ea4c]·readiness 마커 라벨[cabf4d9]에 이어 답변 본문 헤딩 계층까지 SR 의미화).
- [x] (개발팀) **위저드 readiness 마커(✓/⚠) 스크린리더 라벨 — 글리프 aria-hidden + `.sr-only` 의미 텍스트(WCAG 1.3.1)** — 위저드 서류 준비도 마커(✓ 생성 가능 / ⚠ 필수 입력 누락)는 sub-step pill·stepper 두 곳에서 색(.ok/.warn=시각)·글리프(✓/⚠=모호한 SR 낭독)로만 의미를 전달했고, 종전엔 글리프 `<span>` 에 `aria-label` 을 걸었으나 **generic role(`<span>`)의 aria-label 은 SR 이 신뢰성 있게 읽지 않는다**(ARIA in HTML)는 갭 마감. → 글리프 span 을 `aria-hidden="true"`(장식)로 두고 02:35 상담 턴 라벨이 신설한 `.sr-only` 유틸로 동일 문구를 분리해 버튼 접근명에 확실히 포함(이전 aria-label 과 동일 문구=의미 변화 0, 메커니즘만 견고화). 글리프·색 클래스(.ok/.warn)·title·goStep 배선 전부 보존(시각 UI 무변경·CSS 신규 0). 회귀 가드 `verify-stepflag-srlabel.mjs` **26/26 PASS**([A]`.sr-only` 시각 숨김 CSS [B]sub-step pill 글리프 aria-hidden+.sr-only 의미 텍스트·라벨 글리프 뒤 [C]stepper 동일 [D]색 클래스 보존=시각 무회귀 [E]★flag span aria-label 잔존 0·라벨 정확히 2곳 [F]docReady·title·goStep 무회귀) + verify-all **91/91·단언 2629/0** 회귀無(`verify-stepper-a11y` 15 무회귀), tsc/build green(전 10라우트), Playwright 가시 확인(stepper 7+pill 7 마커 글리프 `aria-hidden=true`·aria-label=null·`.sr-only` 1×1px 시각 숨김·접근명에 "필수 입력 누락" 포함·화면엔 글리프 그대로·콘솔 0). 커밋 `cabf4d9`. 조문·엔진·산출물·검증 판정 무접촉(표시/접근성 경계만). 2026-06-22 완료. → **위저드 상태 아이콘 SR 식별 완비**(상담 턴 라벨[778ea4c]의 `.sr-only` 패턴을 위저드 readiness 마커로 확장). ※헤더 doc-progress 요약·ContractsView 카드 칩은 글리프 옆 가시 텍스트가 이미 의미 전달(우선순위 낮음).
- [x] (개발팀) **상담(advisor·Pillar 2) 대화 턴 발화자 라벨(스크린리더) — user/assistant 턴을 시각(정렬·색)에만 의존하던 식별을 `.sr-only` 라벨로 의미 구조화(WCAG 1.3.1)** — `AdvisorChat` 의 대화 턴은 사용자/AI 를 정렬(user=우측·assistant=좌측)·배경색(user=blue-pastel)으로만 구분 — **순수 시각 신호**라 발화자 정보가 표현에만 있고 의미 구조엔 없어, 스크린리더로 이력을 탐색하면 각 버블이 "질문"인지 "답변"인지 알 수 없던 갭 마감(00:55 스트리밍 상태 aria-live·01:40/02:05 순서 변경 등이 다음스텝으로 반복 명시한 "대화 턴 시맨틱·턴 라벨"). 세 턴 렌더 첫머리에 화면에 안 보이는 `.sr-only` 발화자 라벨 삽입(user `"내 질문. "`·정상/오류 답변 `"상담 답변. "`) + `.sr-only` 시각 숨김 유틸 신설(`.advisor-live` 와 동일 clip·absolute·1px). ★**스트리밍 과다 낭독과 무관**=정적 텍스트·라이브 영역 아님(토큰마다 재낭독 0) → **컨테이너(`.advisor-msgs`)에 `role="log"`/`aria-live` 미부착**(둘 다 aria-live 함의로 본문 과다 낭독 — 직전 iteration 이 별도 `.advisor-live` 로 회피한 결함 유지)·기존 `aria-busy` 보존. 회귀 가드 `verify-advisor-turnlabels.mjs` **25/25 PASS**([A]CSS 숨김 [B]user 라벨·본문 앞 [C]정상 답변 라벨·`.md` 앞 [D]오류 턴 라벨+role=alert 보존 [E]`.sr-only` 스팬만·가시 누설 0 [F]★컨테이너 role=log/aria-live 미부착+aria-busy 보존 [G]무회귀) + verify-all **90/90·단언 2603/0** 회귀無, tsc/build green(전 10라우트), Playwright 실 API 키 가시 확인(`.sr-only` 스팬 2종 각 1×1px·position absolute·overflow hidden·스크린샷상 화면 미노출·컨테이너 role=null/aria-live=null/aria-busy=true·콘솔 0). 커밋 `778ea4c`(내 3파일만). 조문·검색·캐시 판정·페르소나·로깅·산출물·스트리밍 상태 고지 무접촉(표시/접근성 경계만). 2026-06-22 완료. → **상담 대화 턴 발화자 SR 식별 완비**(스트리밍 상태 고지[99e964f]에 이어 정적 턴 구조까지 SR 의미화).
- [x] (개발팀) **상담(advisor·Pillar 2) 스트리밍 답변 상태 스크린리더 고지(aria-live) — 생성 시작·도착·중지 폴라이트 라이브 영역(WCAG 4.1.3)** — `AdvisorChat` 답변은 토큰 단위로 스트리밍돼 `.advisor-msgs` 본문이 매 청크 갱신되나 본문에 `aria-live` 가 없어 스크린리더 사용자는 ① 전송이 등록돼 **생성 시작**됐는지 ② 답변이 **완료**됐는지 알 수 없던 a11y 갭 마감(오류만 기존 `role="alert"` 고지). 직전 iteration(00:30 "새 대화")의 다음스텝 후보 "스트리밍 영역 aria-live 검토(본문 미고지)" 마감. ★본문에 직접 `aria-live` 를 걸면 토큰마다 **과다 낭독**되므로 본문이 아니라 **화면에 보이지 않는 폴라이트 라이브 영역**(`.advisor-live`)에 간결 상태만 고지: `deliver` 가 setBusy(true) 직후 "생성하고 있습니다"·스트림 루프 종료 직후 "도착했습니다"·abort 분기에서만 "중지했습니다". ★**오류 분기는 `setLiveMsg("")`**(고지 문구 아님) — 실제 오류는 `role="alert"` 가 낭독해 **이중 낭독 방지**. ★**시작↔도착 교대**(문구가 달라 매 답변 재낭독) + **완료 고지는 루프 '뒤'**(토큰마다 호출 0). SR 영역(`role=status`·`aria-live=polite`·`aria-atomic`) + `.advisor-msgs` `aria-busy={busy}` + `newConversation` 도 liveMsg 비움. 회귀 가드 `verify-advisor-livestatus.mjs` **32/32 PASS**([A]상태+영역 [B]시작 [C]완료=루프뒤·catch전 [D]중지=abort전용 [E]★오류 빈 문자열·role=alert 보존 [F]★교대+루프 내 setLiveMsg 0 [G]CSS 숨김 [H]aria-busy [I]무회귀·setLiveMsg 5회) + verify-all **86/86·단언 2476/0** 회귀無, tsc/build green(전 10라우트), Playwright 실 API 키 가시 확인(라이브 영역 숨김·초기 빈 값→"생성 중→도착"·`aria-busy` true→false→2턴째 재낭독→중지 클릭 시 "중지했습니다"·콘솔 0 errors). 커밋 `99e964f`(내 3파일만 — 타 세션 미커밋분 비혼입). 조문·검색·캐시 판정·페르소나·로깅·산출물 무접촉(표시/접근성 경계만). 2026-06-22 완료. → **상담 스트리밍 상태 SR 접근성 완비**(생성 시작·완료·중지가 스크린리더로 고지).
- [x] (개발팀) **상담(advisor·Pillar 2) "새 대화" 시작(대화 이력 리셋) — 주제 전환 시 컨텍스트·캐시 적격 회복** — `AdvisorChat` 은 스트리밍·중지·재시도·출처·피드백·복사까지 갖췄으나 한 번 대화가 시작되면 빈 상태(제안 칩)로 되돌릴 수단이 **전무**, 무관한 새 주제도 이전 턴이 계속 누적되어 ① 시맨틱 Q&A 캐시가 **fresh single-turn 에만 적용**되므로(verify-advisor-cache [H]) 누적 이력이 캐시 적격을 잃고 ② 직전 턴이 새 질문의 맥락을 오염시키던 결함을 키우던 갭 마감. `newConversation()`(`setMsgs([])`+피드백/복사 리셋+`abortRef.abort()` 방어) 신설 → 캐시 적격 회복 + 주제 오염 제거. ★**입력란(초안) 무영향**(setInput 무호출 — retry 무손실 원칙 동일, 타이핑 중 새 질문 보존) ★**생성 중 무동작**(`if(busy)return`+버튼 `disabled={busy}`, 진행 중 스트림 deliver setMsgs 와 race 방지) ★**대화 시작 후에만 노출**(`{msgs.length>0&&…}`, 빈 상태 미노출). 회귀 가드 `verify-advisor-newchat.mjs` **22/22 PASS**([A]상태 리셋 [B]★초안 보존=내부 setInput 0·전체 2회 유지 [C]★busy 게이트+disabled [D]조건부 노출·빈 상태 보존 [E]abort 방어 [F]CSS [G]무회귀) + verify-all **85/85·단언 2444/0** 회귀無, tsc/build green(전 10라우트), Playwright 가시 확인(빈 상태 버튼無→생성 중 disabled→완료 후 클릭 시 빈 상태 리셋·제안 칩 복귀→초안 보존·콘솔 0). 커밋 `93be08b`. 페르소나·검색·캐시 판정·로깅·산출물·조문 무접촉(표시/전송 경계만). 2026-06-22 완료. → **상담 주제 전환 동선 완비**(새 대화로 컨텍스트 리셋 = 캐시 적격 회복).
- [x] (개발팀) **상담(advisor·Pillar 2) 시맨틱 Q&A 캐시 + ★정밀도 버그 수정 + 회귀 가드 신설** — 직전 iteration이 미커밋·worklog 미기록·가드 부재로 남긴 advisor 시맨틱 캐시 스트림(같은·유사 질문 재유입 시 사전생성/적립 답을 무 API 즉답: `cache.ts` 순수 매칭 + `cache-store.ts` I/O + `route.ts` 통합 + 정적 FAQ 시드 35종 + Supabase 적립 SQL)을 그린 게이트 표준으로 완결. fresh single-turn 에만 적용(멀티턴 미사용)·`ADVISOR_CACHE_OFF/THRESHOLD` env·Supabase 없으면 무동작(graceful degradation)·오류[오류]/단답 미적립. ★**Playwright 스모크가 잘못된 캐시답(false positive) 적발**: `sameStem`의 부분문자열 포함 규칙이 2자 일반어근("신탁")을 합성어("담보신탁이") 안에서 어근으로 오인 → **"담보신탁이 무엇인가요?" 가 "신탁 과세특례란 무엇인가요?" 의 엉뚱한 답에 적중**(설계가 경고한 "잘못된 캐시답=API보다 나쁨" 결함)하던 것을 발견·수정(포함은 "포함되는 토큰 3자 이상"일 때만 인정 — 근저당↔근저당권·토지신탁↔차입형토지신탁·접두 일치 구조↔구조인가요 보존). 신규 회귀 가드 `verify-advisor-cache.mjs` **41/41 PASS**([A]토큰화·바이그램 [B]정확일치 score=1·sources 보존 [C]조사/어미 강건성 [D]정밀도 게이트(minTerms) [E]threshold 게이팅 [F]best-of [G]★오적중 회귀 5단언 [H]route 무동작/멀티턴 안전 배선 [I]cache-store graceful degradation) + verify-all **84/84·단언 2422/0** 회귀無, tsc/build green(전 10라우트), **경험적 스윕(실 FAQ 35종) recall 35/35·교차주제 오적중 0**, 라이브 Playwright(실 API 키): 버그 질의→`miss`·담보신탁 정답 / 근접 리워딩("우선수익권이 무엇인가요?")→`hit` / 멀티턴 후속질문→`miss` / 콘솔 0 errors. 커밋 `7f32c92`. 조문·산출물·페르소나·RAG·로깅 무접촉. 2026-06-22 완료. → **상담 무 API 즉답 캐시 완비**(정밀도 우선·모호 질의는 안전하게 LLM 폴백). ※시드/DB 가 채워졌을 때만 활성(현재 FAQ 35종 번들·Supabase 보류).
- [x] (개발팀) **내 계약 목록 — 공동사업표준협약서(joint) 준비도 칩 + 목록-생성 패리티** — 목록(ContractsView)이 담보신탁에만 "서류 N/7 생성 가능" 칩 + "서류 N종 생성" 버튼을 주고(docReadiness=validateDoc 집계) **joint 계약은 준비 신호·목록-생성 수단 전무**(readyDocIds null)하던 갭 마감. joint 는 단일 산출물이라 boolean 한 개로 충분 → 게이트와 **같은 단일 출처** `validateJoint(form).ok` 재사용한 `jointReadiness(row)`(joint 아니면 null=칩 미표시·손상 저장본 try/catch 격리) + 카드 칩("✓ 협약서 생성 가능"/"⚠ 필수 입력 누락", collateral 칩과 동일 CSS=신규 0) + 통과 시 "⬇ 협약서 생성" 버튼(`generateJointDoc`, batch 상태 재사용). 회귀 가드 `verify-joint-readiness.mjs` 16/16 PASS(★[F] 게이트 정합=`jointReadiness===true ⟺ validateJoint.ok` 6케이스 모순 0) + verify-all 83/83·단언 2381/0 회귀無, tsc/build green(전 10라우트), Playwright 가시 확인(준비완료 joint→✓ 칩+생성 버튼→.docx 다운로드+성공 status / 작성중 joint→⚠ 칩·버튼 미노출·콘솔 0). 빌더·조문·검증 판정·생성 로직·산출물·데이터 모델 무접촉(표시 전용). 2026-06-21 완료. → **담보신탁·joint 양 폼 목록 준비도 칩 + 목록-생성 패리티 완비.**
- [x] (개발팀) **계약 목록(내 계약) UX 정비** — 담보신탁 계약마다 서류 준비도 칩(`✓ 7/7`/`⚠ N/7 생성 가능`, `validateDoc` 7종 집계 재사용·조문 무접촉) + 제목·서류명 검색 필터 + 카드 폴리시(`.contract-card`/상태 `.badge`). 구버전·손상 저장본 try/catch 격리. 회귀 가드 `verify-contracts-readiness.mjs` 12/12 PASS, tsc/build green, Playwright 가시 확인(✓7/7·⚠5/7·joint 칩無·검색 "1/3건"). 2026-06-21 완료. → (위저드·미리보기 UX는 직전 iteration들에서 마감, 목록까지 P0 UX 정비 3종 완료.)
- [x] (개발팀) **내 계약 목록 — 위탁자·물건 소재지 식별/검색 보강** — 카드/검색이 제목·서류종만 다뤄 "(사본)"·동명 계약 구분이 불가하던 갭 마감. 순수 함수 `contractIdentity(row)`(contractRepo.ts, 카드/검색 단일 출처·doc_type별 collateral/joint 분기·손상 저장본 안전 격리) 신설 → 카드 부제(`위탁자 ○○ · 소재지`, `.contract-card-identity`) + 검색 대상에 위탁자명·소재지 포함(placeholder도 갱신). 회귀 가드 `verify-contracts-search.mjs` 13/13 PASS + batch 16/16·readiness 12/12 회귀無, tsc/build green, Playwright 가시 확인(부제 3종·검색 위탁자/소재지/joint사업지/제목 정확 필터, 콘솔 0건). 조문·엔진 무접촉. 2026-06-21 완료.
- [x] (개발팀) **우선수익한도 비율(priorityRatio) 인라인 검증 피드백 — 범위 밖(100~150%) 비율을 입력 지점(StepLoanCalc)에서 즉시 안내** — 범위 밖 비율은 게이트(validateDoc)가 이미 생성을 차단(`verify-priorityratio-validity` 63단언)하나, StepLoanCalc 의 `type=number min/max`는 타이핑을 못 막고 `onChange`(`Number(v)||120`)는 0/빈 값만 가드해 음수·200·1200 이 그대로 들어오면 같은 화면의 **우선수익한도금액 표가 `대출금액×200%`라는 잘못된 큰 금액을 굵게 표시**하던 a11y/정확성 갭 마감(무엇이 왜 막혔는지는 Doc 게이트까지 가야 알 수 있었음, WCAG 3.3.1/4.1.2). 게이트와 **같은 단일 출처** `isValidRatio`(calc.ts, 빌더 `parseFloat||120` 정합)를 인라인에 재사용(판정 불일치 0)·범위 내/기본 120/빈 값은 미표시(나그 0). StepLoanCalc 비율 input 에 `aria-invalid`/`aria-describedby="loan-priorityRatio-err"` + 오류 div(role=alert), CSS 신규 0(`.field-hint`/`--c-danger` 재사용). 회귀 가드 `verify-loan-ratio-inline.mjs` 47/47 PASS(★게이트 정합=인라인 무효⟺게이트 차단·인라인 OFF⟺게이트 통과, 모순 0) + verify-all 76/76·단언 2102/0 회귀無, tsc/build green(전 10라우트), Playwright 가시 확인(기본 120 인라인 無→200 인라인 오류+aria→130 소멸·콘솔 0). 빌더·조문·게이트 판정·산출물 무접촉. 2026-06-21 완료. → **담보신탁 입력 인라인 검증 피드백 패리티 완비**(PartyCard 식별번호 ↔ JointForm 법인등록번호·협약일 ↔ StepLoanCalc 우선수익한도 비율).
- [x] (개발팀) **신탁보수(trustFee) 인라인 검증 피드백 — 채웠지만 0·음수·비숫자인 신탁보수를 입력 지점(StepBasic)에서 즉시 안내** — 신탁보수는 별첨3 보수액·신탁보수율 자동산정에 쓰이는 법적 금액. "채웠지만 0·음수·비숫자"는 게이트(validateDoc)가 이미 차단(`verify-trustfee-validity` 45단언, ₩-5,000.- 박힘 방지)하나, StepBasic `type=number`는 0·음수 타이핑을 못 막고(import·구버전·AI 머지로 비숫자 진입) 같은 화면 요약·신탁보수율이 그 값으로 계산되던 a11y/정확성 갭 마감(WCAG 3.3.1/4.1.2). 게이트와 **같은 단일 출처** `isPositiveAmount`(calc.ts) + 게이트와 같은 채움 조건 `hasText`(typeof string && trim>0)를 인라인에 재사용(판정 불일치 0)·빈 값/공백/유효 금액은 미표시(나그 0). 신탁보수 input 에 `aria-invalid`/`aria-describedby="basic-trustFee-err"` + 오류 div(role=alert), CSS 신규 0. 회귀 가드 `verify-trustfee-inline.mjs` 30/30 PASS(★게이트 정합=인라인 무효⟺게이트 차단·인라인 OFF⟺게이트 무차단, 모순 0) + verify-all 77/77·단언 2132/0 회귀無, tsc/build green(전 10라우트), Playwright 가시 확인(빈 값 인라인 無→-5000000/0 인라인 오류+aria→50000000 소멸·콘솔 0). 빌더·조문·게이트 판정·산출물 무접촉. 2026-06-21 완료. → **담보신탁 공통 입력 인라인 검증 피드백 패리티 완비**(식별번호 ↔ 비율 ↔ 법인등록번호·협약일 ↔ 신탁보수). ※신탁기간은 "빈 값=필수"만이라 인라인 나그 회피(의도적 제외).
- [x] (개발팀) **우선수익자 개별 대출금액(loanAmount) 인라인 검증 피드백 — 채웠지만 0·음수·비숫자인 개별 대출금액을 입력 지점(StepLoanCalc 표)에서 즉시 안내** — 개별 대출금액은 우선수익한도금액(=대출금액×비율)을 좌우하는 핵심 수치. "채웠지만 0·음수·비숫자"는 게이트(validateDoc)가 이미 차단(`verify-loan-amount-validity` 48단언)하나, ① 같은 컴포넌트의 우선수익한도 비율은 인라인 피드백을 받는데 **개별 대출금액 입력만 인라인 부재** ② 음수(-5000) 입력 시 같은 행 우선수익한도금액 셀이 `parseAmount` 가드라 truthy→**음수 한도(-6,000원 등)를 굵게(brown) 표시**하던 a11y/정확성 갭 마감(priorityRatio 인라인과 동형, WCAG 3.3.1/4.1.2). 게이트와 **같은 단일 출처** `isPositiveAmount`(calc.ts) + 같은 채움 조건(trim>0)을 인라인에 재사용(판정 불일치 0)·빈 값은 합계검사가 커버하므로 미표시(나그 0). 대출금액 input 에 `aria-invalid`/`aria-describedby="loan-amount-err-${i}"`(행별 유니크) + 오류 div(role=alert) + 한도 셀 가드 `parseAmount`→`isPositiveAmount`(음수→"—", 양수 100% 보존). CSS 신규 0. 회귀 가드 `verify-loan-amount-inline.mjs` 32/32 PASS(★게이트 정합=인라인 무효⟺게이트 차단·인라인 OFF⟺게이트 무차단, 모순 0) + verify-all 78/78·단언 2164/0 회귀無, tsc/build green(전 10라우트), Playwright 가시 확인(빈 값 인라인 無·한도 "—"→-5000 인라인 오류+aria+한도 "—" 유지→12,000,000,000 소멸·한도 "일백사십사억원정" 산정→빈 값 재입력 OFF·콘솔 0). 빌더·조문·게이트 판정·산출물 무접촉. 2026-06-21 완료. → **담보신탁 정량 입력 인라인 검증 피드백 패리티 완비**(식별번호 ↔ 비율 ↔ 개별 대출금액 ↔ 신탁보수 ↔ 법인등록번호·협약일).
- [x] (개발팀) **서류 금액(money) 인라인 검증 피드백 — 신탁부동산 가격·신탁재산 원본가액의 0·음수·비숫자 값을 입력 지점(DocStep)에서 즉시 안내** — 두 money 필드(`appform.valuationPrice`·`valReport.principalValue`)는 신청서/별첨 가격칸에 한글 금액으로 박히는 법적 금액. "채웠지만 0·음수·비숫자"는 게이트(`validateDoc→docMissing`)가 이미 차단하나, 입력 옆에서는 `amt>0` 아님→한글 에코만 조용히 사라질 뿐 무엇이 왜 막혔는지는 하단 검증 박스까지 가야 알 수 있었다(StepBasic 신탁보수·StepLoanCalc 개별 대출금액과 동일 인라인 부재 갭 — 게이트가 막는 금액 중 **마지막 인라인 미적용 2종**, WCAG 3.3.1/4.1.2). 게이트와 **같은 단일 출처** `isPositiveAmount`(calc.ts) + 같은 채움 조건(`hasText`=trim>0)을 인라인에 재사용(판정 불일치 0)·빈 값/공백/유효 금액 미표시(나그 0). money input 에 `aria-invalid`/`aria-describedby="${fid}-err"`(fid=`doc-${docId}-${f.key}`) + 오류 div(role=alert), 한글 에코(amt>0) 무변경, CSS 신규 0. 회귀 가드 `verify-docamount-inline.mjs` 52/52 PASS(★게이트 정합=인라인 무효⟺그 서류 게이트 차단, appform·valReport 각각·모순 0) + verify-all 79/79·단언 2216/0 회귀無, tsc/build green(전 10라우트), Playwright 가시 확인(원본가액 -5,000,000 인라인 오류+aria+에코 숨김→8,000,000,000 소멸·에코 "팔십억원정"·콘솔 0 errors). 커밋 `269cc79`(직전 미커밋분 신탁보수·개별대출금액 인라인 `0808295` 선마감). 빌더·조문·게이트 판정·산출물 무접촉. 2026-06-21 완료. → **담보신탁 전 금액 입력 인라인 검증 피드백 패리티 완비**(사업자·법인·생년월일 ↔ 등기번호 ↔ 비율·개별 대출금액 ↔ 신탁보수 ↔ 가격·원본가액 — 게이트가 막는 "채웠지만 무효" 정량값 전부 입력 지점 즉시 안내).
- [x] (개발팀) **신탁부동산 면적(area) 게이트 + StepProperty 인라인 검증 피드백 — 가격·원본가액·등기번호 패리티의 마지막 정량 입력 갭 마감** — 면적은 별첨1·신청서·계약서 별지·미리보기의 부동산표 면적칸에 `p.area + "㎡"`로 그대로 박히는 정량 출력값인데(builders.js), 금액 5종·체결일·비율·식별번호·등기 고유번호까지 게이트·인라인을 갖춘 뒤에도 **면적만 게이트도 인라인도 전무**해 "0·음수·비숫자·단위혼입(1234㎡)" 면적이 "-50㎡"·"NaN㎡"로 법적 표에 들어갈 수 있던 마지막 정량 갭 마감(WCAG 3.3.1/4.1.2). 게이트와 **같은 단일 출처** `isPositiveAmount`(쉼표·소수 1234.56 허용·0/음수/비숫자 무효)+`hasText`를 게이트(validate.ts 부동산 forEach)·인라인(StepProperty aria-invalid/describedby+role=alert, 채움&&무효일 때만 렌더)에 재사용(판정 불일치 0)·빈 값 미차단(선택 입력 무회귀). CSS 신규 0. 회귀 가드 `verify-area-inline.mjs` 72/72 PASS(★게이트 정합=인라인 무효⟺validateDoc 차단·OFF⟺무차단·다수 부동산 1-based·전 7종 차단·등기번호/가격 독립) + verify-all 82/82·단언 2365/0 회귀無, tsc/build green(전 10라우트), Playwright 가시 확인(-50 인라인 오류+aria+describedby↔id 일치→1234.56 소멸·콘솔 0). 빌더·조문·게이트 판정·산출물 무접촉(입력 완결성·표시/접근성만). 2026-06-21 완료. → **담보신탁 전 정량 출력 필드 게이트+인라인 패리티 완비**(가격·원본가액·개별 대출금액·신탁보수·비율·등기번호 ↔ 면적).
- [x] (개발팀) **우선수익한도 비율(priorityRatio) 무효 시 한도금액 표시 억제 — 인라인 오류 ↔ 같은 화면 표시 모순 제거** — 직전 priorityRatio 인라인(`94dfc7f`)은 범위 밖 비율 **오류 메시지**만 추가하고 한도표는 손대지 않아, 200% 입력 시 인라인은 "생성 불가"라고 경고하는데 바로 아래 개별·합계 우선수익한도금액 셀이 `대출금액 × 200%`(2배 과대, 예 5,000,000,000→10,000,000,000)를 굵은 brown 으로 자신 있게 표시하고 산식 footnote 도 "× 200%"라 단정하던 정확성/UX 모순 마감(개별 대출금액 무효 시 한도 셀 "—" 억제와 비대칭이던 갭, WCAG 3.3.1). 게이트는 무효 비율 생성을 이미 차단(산출물 무영향)이나 **입력 화면 표시**가 오도. `ratioInvalid` 단일 출처(`!isValidRatio`) 재사용 — 개별 셀 `isPositiveAmount(p.loanAmount) && !ratioInvalid`, 합계 셀 `ratioInvalid ? "—"`, footnote `× {ratioInvalid ? "비율" : "${%}"}` + 보류 안내로 4곳을 한 판정에 정합(새 판정·import·CSS 0). 회귀 가드 `verify-loan-ratio-inline.mjs` [F] 5단언 신규 **52/52 PASS** + `verify-loan-amount-inline` [D] 결합 가드 갱신 32/32 + verify-all 79/79·단언 2221/0 회귀無, tsc/build green(전 10라우트), Playwright 가시 확인(120%=6,000,000,000→200% 개별·합계 "—"·인라인 alert·aria-invalid·산식 "보류"→130% 6,500,000,000 재산정·콘솔 0). 빌더·조문·게이트 판정·산출물 무접촉(표시/접근성만). 2026-06-21 완료. → **인라인 무효 판정 ↔ 같은 화면 표시(한도 셀·합계·산식) 정합 완비.**
- [x] (개발팀) **신탁보수(trustFee) 무효 시 요약의 신탁보수 표시 억제 — 인라인 오류 ↔ 같은 화면 요약 음수 금액 모순 제거(StepLoanCalc 비율 표시 억제 패리티)** — 직전 priorityRatio 표시 억제와 동형의 마지막 갭 마감. 22:06 신탁보수 인라인(`0808295`)은 인라인 오류만 추가하고 StepBasic 하단 요약 footer(`신탁보수 {fmtKRW(c.trustFee)}`)는 미가드라, -5000000 입력 시 인라인은 "생성 불가"라는데 바로 아래 요약이 **"신탁보수 -5,000,000 원"을 자신 있게 표시**(fmtKRW 이 음수를 그대로 렌더, 인라인과 정면 모순)하던 정확성/UX 갭(WCAG 3.3.1). ※trustFeeRate 는 `fee>0` 가드라 이미 안전 — 요약 원시 에코만 미가드. 인라인과 **같은 단일 출처** `feeInvalid` 재사용해 요약을 `feeInvalid ? "—" : fmtKRW(c.trustFee)`로 분기(표시/인라인 모순 0·새 판정·import·CSS 0). 회귀 가드 `verify-trustfee-inline.mjs` [F] 3단언 신규 **33/33 PASS** + verify-all 79/79·단언 2224/0 회귀無, tsc/build green(전 10라우트), Playwright 가시 확인(-5,000,000 입력→요약 "신탁보수 —"·인라인 alert·aria-invalid→50,000,000 복원 시 "신탁보수 50,000,000 원" 재표시·콘솔 0). 커밋 `48d82ff`(직전 priorityRatio 표시 억제 미커밋분 `52d36c1` 선마감). 빌더·조문·게이트 판정·산출물 무접촉(표시/접근성만). 2026-06-21 완료. → **인라인 무효 판정 ↔ 같은 화면 표시 정합이 비율·신탁보수 양 정량값 완비**(무효 입력이 더 이상 굵은/음수 금액으로 사용자를 오도하지 않음).
- [x] (개발팀) **무효 우선수익한도 비율(priorityRatio)의 cross-screen 표시 억제 — StepBasic(STEP 05) 한도금액·신탁보수율·요약** — 직전 StepLoanCalc 무효 비율 표시 억제(`52d36c1`)의 마지막 cross-screen 갭 마감. 한도금액·보수율은 `recalcDerived`가 STEP 02-1 비율·대출금액으로 산정해 STEP 05(StepBasic)에 미러링하는데, recalcDerived(`totalPriorityLimit`)가 범위 밖 비율도 `parseAmount||120`로 곱해 산정하므로 StepLoanCalc 한도 셀은 "—"인데도 **StepBasic 의 한도금액 readonly·신탁보수율·요약은 무효 비율로 산정한 잘못된 큰 값(50억×200%=100억)을 자신 있게 표시**하던 모순 잔존(비율 인라인 오류는 StepLoanCalc 에만 존재→STEP 05엔 무효 신호 전무, WCAG 3.3.1). 게이트(validateDoc)와 **같은 단일 출처** `isValidRatio` 재사용해 `ratioInvalid`/`limitShowable=!ratioInvalid && isPositiveAmount(c.priorityLimit)` 파생으로 한도금액·보수율·요약 3곳 일괄 억제(데이터 모델·게이트·산출물 무변경, 표시만) + 한도금액 필드에 role=note 안내(aria-describedby 연결). 회귀 가드 `verify-stepbasic-ratio-display.mjs` **41/41 PASS**(★[F] 거동 정합=recalcDerived 가 무효 비율로 실제 양(+)의 잘못된 한도 산정함 증명[200%→100억]·그 무효 비율은 limitShowable=false 억제 AND 게이트 차단·유효 비율은 노출+게이트 무차단, 모순 0) + verify-all 80/80·단언 2265/0 회귀無, tsc/build green(전 10라우트), Playwright 가시 확인(유효 120%=한도 60억·보수율 0.8333%·note無→무효 200%=한도 필드 빈 값·placeholder "비율 확인 필요 — 산정 보류"·role=note·보수율 빈 값·요약 "한도 — · 신탁보수 50,000,000 원 · 보수율 (대기)"→유효 130% 복원 시 한도 65억·보수율 0.7692% 재표시·억제 고착 0·콘솔 0). 빌더·조문·게이트 판정·데이터 모델·산출물 무접촉(표시/접근성만). 2026-06-21 완료. → **무효 비율 판정 ↔ 모든 화면(StepLoanCalc 한도표 + StepBasic 한도금액·보수율·요약) 표시 정합 완비**.
- [ ] (디자인팀) 위저드·미리보기·계약목록 UX 정비 — (개발 산출 완료, 디자인팀 토큰·여백 최종 검수 대기)
- [x] (개발팀) **검증 게이트(필수 입력 누락 시 DOCX/PDF 차단+누락 안내) + 미리보기 2분할** — `DocStep.tsx`에 구현·연결 확인(M2-1/M2-2). 2026-06-21 확인.
- [x] (개발팀) **공동사업표준협약서(joint) 저장·불러오기·미저장(dirty) 추적 — joint 입력 통째 유실 결함 마감** — collateral 은 SaveBar 가 `form` 을 저장/복원/추적하나 joint 는 입력 모델이 `jointForm` 으로 분리됐는데 ① SaveBar.save 가 항상 빈 collateral `form` 을 저장(joint 입력 유실) ② loadContract 가 `form` 으로만 복원(재오픈 시 빈 폼) ③ isFormDirty/markSaved 가 `form` 기준이라 joint 편집이 미저장 경고·이탈 가드에 안 걸리던 데이터 유실 갭 마감. `contractIdentity` 의 joint 분기(gap.name/project.site)도 저장 데이터가 collateral 이라 死코드였음. "현재 열린 서류의 활성 폼"(joint=jointForm/그 외=form)을 저장·복원·dirty 단일 기준으로 라우팅(`StoredForm=ContractForm|JointForm` 타입 확장, `isFormDirty` 3-인자화·`_blankJointHash` 별도 빈 기준선, loadContract joint 분기·gap/project 병합 무크래시·교차오염 차단). 회귀 가드 `verify-joint-persistence.mjs` 31/31 PASS + verify-all 72/72·단언 1953/0 회귀無, tsc/build green, Playwright 가시 확인(joint 8필드 입력→dirty 표출→저장 시 form_data.gap/project 보존·collateral 키 미혼입→카드 부제 "위탁자 ○○개발 · 소재지" 부활→열기 시 8필드 전부 복원·콘솔 0). 빌더·조문·검증 게이트·산출물 무접촉. 2026-06-21 완료. → **담보신탁·joint 양 폼 저장·dirty 패리티 완비**(유실 방지 5축이 joint 동선까지 확장).
- [x] (개발팀) **공동사업표준협약서(joint) 생성 신선도(stale) 신호 — 담보신탁 DocStep 패리티 마감** — JointForm 은 Word/PDF 생성 후 갑 상호·사업명·협약일 등을 고쳐도 `✓ 생성 완료` 가 그대로 남아 구버전 협약서를 최신으로 오인·제출할 위험(법적 효력 문서, 정확성 최우선)이 잔존하던 마지막 정확성 패리티 갭 마감(DocStep 17:07/17:36 신선도의 joint 미적용). DocStep 과 동일 순수 함수 `genFreshness`(genStatus.ts) 재사용 + `genSnap` state·`formSnap=JSON.stringify(jointForm)`(값 기반) → `onDocx` 성공 시·`onPdf` `opened` 분기에서만 genSnap 기록(PDF 팝업 차단 시 거짓 fresh 방지), `freshness==="stale"` 시 danger 색 `● 입력이 변경되었습니다 — 다시 생성하세요` 우선 표시. 회귀 가드 `verify-generate-freshness.mjs` [G] 7종 추가 31/31 PASS + verify-all 단언 1965/0 회귀無(pdf-popup·joint-a11y 가드 2종 정합 갱신 포함), tsc/build green(전 10라우트), Playwright 가시 확인(9필드 입력→Word 생성 `✓ 완료`→사업명 수정 시 즉시 stale 전환·danger 색·콘솔 0). 빌더·조문·검증 게이트·산출물 무접촉(표시 전용). 2026-06-21 완료. → **담보신탁·joint 양 폼 생성 신선도 패리티 완비**(검증 게이트·저장/dirty·미리보기·PDF 팝업 가드·생성 신선도 5축 DocStep 패리티 도달).
- [x] (개발팀) **공동사업표준협약서(joint) 검증 게이트 — 담보신탁 DocStep 패리티 마감** — JointForm 은 검증 게이트가 전무해 갑(시행사) 상호·대표이사·주소, 사업명·사업부지·규모/용도, 협약일이 **모두 비어 있어도 협약서가 생성**(빈 칸이 박힌 법적 문서)되던 정확성 갭 마감(inbox C2 검증 게이트의 joint 미적용). 순수 함수 `validateJoint(jointForm)`(validate.ts, 누락 검사 + 협약일 달력 유효성 `isRealDate`·갑 법인등록번호 체크섬 `isValidCorpRegNo` 재사용·null/빈 객체 무크래시) 신설 → JointForm `!ok` 시 `.validate-box` 누락 안내(CSS 재사용=신규 0) + Word/PDF 버튼 `disabled={busy||!ok}` + `onDocx/onPdf` 방어 가드. 회귀 가드 `verify-joint-validate.mjs` 43/43 PASS + verify-all 71/71·단언 1922/0 회귀無, tsc/build green(전 10라우트), Playwright 가시 확인(빈 폼→7항목 차단·두 버튼 disabled / 채움→활성·WYSIWYG·콘솔 0). 커밋 `8529c79`. 빌더·조문·산출물 무접촉. 2026-06-21 완료. → **담보신탁·joint 양 폼 검증 게이트 패리티 완비.**
- [x] (개발팀) **공동사업표준협약서(joint) 검증 게이트 누락 항목 → 입력 필드 점프 — 담보신탁 DocStep validate-jump 의 단일 폼 버전 패리티 마감** — DocStep 은 누락 항목이 `.validate-jump` 버튼이라 클릭 시 해당 입력 단계로 점프하나, JointForm 은 누락 안내가 정적 `<strong>` 텍스트라 긴 단일 폼에서 해당 입력을 눈으로 찾아야 했다. joint 는 스텝이 없는 단일 스크롤 폼이므로 "누락 라벨 → 해당 입력 필드 DOM id 로 스크롤·포커스"가 동형. `validateJoint` 반환 형(`missing:string[]`) 무변경(43단언 가드 무회귀) + **별도 순수 함수** `jointFieldIdForMissing`(validate.ts, 라벨 접두사→`joint-*` id 8쌍·동적 접미사 무시·DOM 무접근) 신설, JointForm `focusMissing`(getElementById→scrollIntoView+focus) + 누락 항목을 `.validate-jump` 버튼화 + 법인등록번호 앞칸에 `id="joint-gapCorpRegFront"`(aria-labelledby div id 와 분리=중복 id 없음). CSS·헬퍼 신규 0. 회귀 가드 `verify-joint-validate-jump.mjs` 44/44 PASS(★핵심 불변식=emit 가능한 모든 누락 라벨이 실재 입력 id 로 매핑, 死점프 0) + verify-all 73/73·단언 2009/0 회귀無(verify-joint-validate import 정규식 완화 포함), tsc/build green(전 10라우트), Playwright 가시 확인(빈 폼 7항목 전부 클릭 버튼→"사업부지" 클릭 시 activeElement=`#joint-projectSite`·콘솔 0). 조문·엔진·산출물 무접촉. 2026-06-21 완료. → **담보신탁·joint 양 폼 검증 게이트 누락 항목 점프 패리티 완비**(DocStep=스텝 점프 / joint=필드 포커스).
- [x] (개발팀) **공동사업표준협약서(joint) 인라인 검증 피드백 — 갑 법인등록번호 체크섬 + 협약일 달력 유효성 인라인 안내(담보신탁 PartyCard/StepBasic 패리티)** — PartyCard 는 무효 식별번호를 그 필드 옆 인라인 오류(aria-invalid+aria-describedby+role=alert, `verify-inline-error-a11y`)로 즉시 알리나, JointForm 은 갑 법인등록번호 무효·협약일 실재하지 않는 날짜(2/31 등)가 하단 게이트(`validateJoint`)에만 모여 그 입력 옆 인라인 피드백이 전무하던 a11y/UX 갭 마감. 특히 협약일은 담보신탁(드롭다운)과 달리 **자유 텍스트**라 잘못된 날짜를 직접 칠 수 있어 가치 큼. 게이트와 **같은 단일 출처**(`isValidCorpRegNo`·`isRealDate`)를 인라인에 재사용(판정 불일치 0)·부분 입력엔 미표시(나그 방지)·CSS 신규 0(`.field-hint`/`--c-danger` 재사용). 회귀 가드 `verify-joint-inline-validity.mjs` 20/20 PASS(★게이트 정합=인라인 무효⟹`validateJoint`도 차단, 모순 0) + verify-all 74/74·단언 2029/0 회귀無, tsc/build green(전 10라우트), Playwright 가시 확인(무효 법인번호/협약일 2-31 입력→인라인 오류+aria 연결, 유효/윤년으로 고치면 소멸, 콘솔 0). 빌더·조문·게이트 판정·산출물 무접촉. 2026-06-21 완료. → **담보신탁·joint 양 폼 인라인 검증 피드백 패리티 완비.**
- [x] (개발팀) **검증 게이트 누락 항목 → 입력 단계 점프** — 누락 안내가 "STEP 01 …" 텍스트로만 위치를 알려주고 이동 수단이 없던 갭 마감. `Missing`에 `stepIdx` 추가(`where`는 STEPS 파생=단일 출처), DocStep 검증 박스 항목을 `.validate-jump` 버튼화→클릭 시 `goToStep`(setStep+setTab)로 탭·스텝 동시 점프. 회귀 가드 `verify-validate-jump.mjs` 15/15 PASS + readiness 14/14 회귀無, tsc/build green, Playwright 가시 확인(Doc 02→"신탁 부동산" 클릭→STEP 03 전환, 콘솔 0건). 조문·엔진 무접촉. 2026-06-21 완료.
- [x] (개발팀) **위저드 서류별 생성 가능 여부(✓/⚠) 마커** — 검증 게이트를 tab3 서류 pill에서 한눈에. 7종 중 막힌 서류 즉시 식별(`validateDoc` 재사용, 조문 무손상). 회귀 가드 `verify-doc-readiness.mjs` 13/13 PASS, tsc/build green, Playwright 가시 확인. 2026-06-21 완료.
- [x] (개발팀) **내 계약 목록에서 바로 "서류 N종 생성"(.docx)** — 준비도 칩이 보여주기만 하던 목록에서 계약을 열지 않고도 준비된 서류 일괄 다운로드(목록↔위저드 일괄생성 패리티). `readyDocIds`로 칩 수=일괄 대상 단일 출처 통일, 위저드 `generateAllReady`와 동일 패턴(`generateCollateralDoc` 순차 350ms)·**누락 서류 제외**(정확성 보존). 카드당 진행/완료 메시지(`role=status`), joint/0종 미노출. CSS 신규 없음(`btn-primary`·`field-hint` 재사용). 회귀 가드 `verify-contracts-batch.mjs` 16/16 PASS(핵심 불변식=일괄 대상이 검증 통과 서류와 정확히 일치) + readiness 12/12 회귀無, tsc/build green, Playwright 가시 확인(5/7 카드→정확히 5개 .docx, 막힌 2종 제외, joint 버튼無). 조문·엔진·생성 로직 무접촉. 2026-06-21 완료.
- [x] (개발팀) **준비된 서류 일괄 생성(.docx)** — 7종을 각 step에서 7번 생성하던 수고를 헤더 버튼 한 번으로(랜딩 카피 "입력 한 번으로 Word·PDF 일괄 생성"과 실제 앱 간극 해소). 진행 요약과 동일 집계(`validateDoc.ok`)로 ready 서류만 `generateCollateralDoc` 순차 호출(350ms 간격)·**누락(⚠) 서류는 제외해 미완 서류 미생성**(정확성 보존). `readyCount>0`일 때만 노출, 생성 진행/완료 메시지(`role=status`). 회귀 가드 `verify-batch-generate.mjs` 11/11 PASS(핵심 불변식=일괄 대상이 검증 통과 서류와 정확히 일치) + doc-progress-summary 12/12 회귀無, tsc/build green, Playwright 가시 확인(5/7 시 버튼 출현→클릭 시 정확히 5개 .docx, 막힌 2종 제외). 조문·엔진·생성 로직 무접촉. 2026-06-21 완료.
- [x] (개발팀) **위저드 헤더 "서류 생성 준비 현황" 요약(✓ N/7) + 첫 막힌 서류 한 클릭 이동** — 서류별 마커가 pill·stepper에만 있고 stepper는 <980px 숨김이라 모바일에 진행도 요약 부재 + 데스크톱도 7개 pill 스캔 필요하던 갭. 헤더에 항상 노출되는 진행 미터+`서류 N/7 생성 가능` 카운트+`firstBlocked` 점프 버튼(`goStep`) 추가(`docReady` 재집계, 조문·엔진·검증 판정 무손상). 회귀 가드 `verify-doc-progress-summary.mjs` 12/12 PASS + readiness 14/14·validate-jump 15/15 회귀無, tsc/build green, Playwright 가시 확인(0/7→5/7→6/7→✓7/7 그린·완료). 2026-06-21 완료.
- [x] (개발팀) **우선수익자 복수(2인) 본문 표/날인 전원 표기 검증** — 26/26 PASS (`scripts/verify-multiparty.mjs`, 위탁자2+우선수익자2, HTML+실제 DOCX 바이트). 2026-06-20 완료. → 사업팀 verbatim 실물 대조 대기.
- [x] (개발팀) **서류별 실시간 미리보기 WYSIWYG** — DocStep 미리보기가 docId 무관하게 항상 계약서 본문만 보이던 버그(7종 중 6종 미리보기≠생성물) 수정. `previewDocHTML(form,docId)`가 PDF 빌더 완성 HTML(contract/appform/generic)을 자동인쇄 `<script>`만 제거해 `<iframe srcDoc>`로 격리 렌더 → 실제 생성물과 동일. **조문 verbatim 무접촉(빌더 출력 재사용)**. 회귀 가드 `verify-doc-preview.mjs` 33/33 PASS, `verify-doc-readiness` 14/14 회귀無, tsc/build green, Playwright 가시 확인(위임장↔계약서 즉시 전환). 2026-06-21 완료.

### 검증 발견 (2026-06-21 02 verify)
- [x] (개발팀) **verify-doc-readiness 회귀가드 적색(12/13) 수정** — STEP 05 신설로 stale된 `nonDoc.length===5` 하드코딩을 STEPS **파생**(파티션 완전성 단언)으로 교체 → **14/14 PASS**. 향후 STEP 추가에도 stale 안 됨. tsc EXIT 0, 앱 소스 무변경(테스트 전용). 2026-06-21 06:58 완료.
- [ ] (개발팀) **StepConditions iteration worklog/백로그 사후 기록** — Wizard+98/model.ts+39/schema±/StepConditions.tsx 신규 변경이 06-21 worklog(마지막 00:24) 누락. 09 morning 보고 전 보강. (※ tsc/next build green, 정확성 가드레일 통과 — 정정 아닌 로깅 보완.)
- [ ] (사업팀) **conditions 신규 8키 조문 연동 시 verbatim 게이트** — `collateralType·licenseType·onbid·feePayer·collateralOrder` 등은 현재 "프로파일 기록(조문 미접촉)". 향후 조문 연동 착수 전 **표준양식(.docx) verbatim 수급 필수**(추정 조문 금지). verbatim 대조 체크리스트에 편입.

- [x] (개발팀) **TrustForm 브랜드 아이콘(favicon) + OG/Twitter 메타** — 대표님 확정 브랜딩("타이틀·메타·OG·아이콘") 중 미완이던 아이콘·OG 마감. `src/app/icon.svg` 신규(브라운 그라데이션+서류+모노그램 T) + `layout.tsx` icons/openGraph/twitter 선언 → 매 검증마다 반복되던 **콘솔 favicon 404 소멸**(Playwright 콘솔 0건, favicon.ico 요청 자체 제거). 회귀 가드 `verify-brand-icon.mjs` 9/9 PASS, tsc/build green(`○ /icon.svg` 라우트 생성). 조문·엔진 무접촉. 2026-06-21 완료. → (디자인) 본문 로고 텍스트·토큰 일관성·og:image는 호스팅 확정 후.

### ★대표님 결정 대기 (2026-06-21 21:00 신규/갱신)
- [ ] (★대표님) **1차 출시 선언 게이트** — M1 verbatim 통과(본문 100%/별첨4 88%, 미일치=치환자·선택조항)를 근거로 "담보신탁 단일 출시 가능" 선언 여부. 자체 판정 "엔진오류 아님"의 추인 + (권장) 추가 정본 1건 이상 교차.
- [ ] (★대표님/사업팀) **처분신탁(disposal) 범위 결정** — 2026-06-21 19:58 `disposalBody.ts`(160줄 조문 데이터, 커밋 `59b8670`) 신규 추가됨. **현재 src 어디에도 import 안 됨 = 산출물 경로 미연결 standalone 데이터**(1차 범위=담보신탁 단일과 출력상 무충돌). 단 ① 2차 대상 채택 여부 미결정 ② 채택 시 **연결 전 verbatim 대조 선행 필수**(담보신탁과 동일·추정 조문 금지). 연결 전까지 출력 노출 금지.
- [ ] (★대표님) **법무 초안 3종 검토 경로** — 2026-06-21 17:39 `docs/legal/{terms,privacy,disclaimer}.md` 초안 추가(커밋 `4cf81f1`). 변호사법 경계·면책 핵심 자산이나 **초안=외부 법률 검토 미필**. 출시 전 검토 필요 여부·시점 결정.

## P1 — 운영 기반
- [ ] (개발팀) `.env.local` 셋업(ANTHROPIC/SUPABASE) + 로컬 구동 확인
- [ ] (개발팀) Supabase 프로젝트 + 마이그레이션 적용(멀티테넌트)
- [ ] (개발팀) 배포 환경 확정 — ★대표님 결정 필요(Vercel 등)
- [ ] (기획팀) 브랜드명 확정 — ★대표님 결정 필요

## P2 — 론칭 준비
- [ ] (디자인팀) 랜딩/앱 디자인 시스템 정비
- [ ] (마케팅팀) 포지셔닝·세일즈 원페이저·리드 리서치
- [ ] (마케팅팀) 가격/패키지 방향 초안

## 기술부채 / 향후 확장
- [x] (개발팀) **계약 삭제 실행취소 — 연속 삭제 시 먼저 지운 계약의 영구 유실 차단(단일 슬롯→큐)** — 로컬 우선(localStorage)이라 삭제는 영구적이고 7초 "실행취소"가 안전망인데, `ContractsView` 가 단일 `undoRow`/`undoTimer` 라 7초 창 안에 둘째 계약을 삭제하면 `clearTimeout` 후 직전 실행취소를 **덮어써** 먼저 지운 계약이 되돌릴 수단 없이 영구 유실(게다가 무표시)되던, 안전망이 제 약속을 어기던 데이터 유실 갭 마감. 순수 함수 `enqueueUndo`/`dequeueUndo`(contractRepo.ts, 최근 맨 앞·id 중복 차단·해당 항목만 제거·입력 무변형) 신설 → `undoRows`(큐) + `undoTimers: Map<id,timer>`(항목별 독립 만료) 로 전환, 실행취소 바를 건별 스택 렌더(CSS 신규 0=`.contracts-undo` margin 재사용). 회귀 가드 `verify-contract-undo-delete.mjs` [F]/[G] 10단언 추가 **25/25 PASS** + verify-all 75/75·단언 2055/0 회귀無, tsc/build green(전 10라우트), Playwright 가시 확인(판교·역삼 연속 삭제→바 2개 동시 표출→판교 실행취소 시 복원·역삼 바 독립 유지·콘솔 0). 커밋 `c00e5e5`. 조문·엔진·검증 게이트·산출물 무접촉(표시/상태 관리만). 2026-06-21 완료. → **유실 방지 계열 강화**(삭제 실행취소가 연속 삭제에도 건별 독립 보장).
- [x] (개발팀) **계약 대화(chat·Pillar 1) 공동사업표준협약서(joint) 교차오염 차단 — ChatPanel 이 docTypeId 인지** — AI 자동 채움(update_form)은 담보신탁 ContractForm 전용(스키마·`summarizeForm`·`mergeFormPatch` 모두 collateral `form` 1:1)인데 채팅 FAB 는 TrustApp 전역이라 joint 작성 중에도 열려 ① 빈 collateral 컨텍스트 전송 ② AI 패치를 **숨은 collateral form 에 적용**(교차오염 — 같은 세션 후속 담보신탁 계약에 잔존) ③ 인사·placeholder "담보신탁" 고정 오해, 의 정확성/교차오염 갭 마감. `isJoint=docTypeId==="joint"` 파생 → joint 이면 인사·placeholder=양식 직접 입력 안내·formSummary=joint 맥락 문구·**AI 패치 미적용(교차오염 차단)** + 미지원 안내 note. collateral 경로 100% 무변경. 회귀 가드 `verify-chat-joint-guard.mjs` 16/16 PASS(★mergeFormPatch=else if 1곳·joint 분기 미적용) + verify-all 75/75·단언 2045/0 회귀無, tsc/build green(전 10라우트), Playwright 가시 확인(joint→안내 인사·placeholder / 담보신탁→기존 인사·placeholder 보존·콘솔 0). 도구 스키마·/api/chat·조문·산출물 무접촉(호출부 분기만). 커밋 `2d1c3f0`. ※joint **전체 NL 자동 채움**(joint 도구 스키마+라우트+store 라우팅, API 키 필요)은 규모 큼 → 별도 사이클. 2026-06-21 완료.
- [x] (개발팀) **계약 대화(chat·Pillar 1) AI 폼 반영 항목 알림** — AI 가 `update_form` 으로 채운 patch 가 `mergeFormPatch` 로 **조용히 법적 폼에 반영**되던 것을 채팅 중앙 초록 note `✓ … 반영됨` 으로 가시화(검수·신뢰, 정확성 가드레일). 순수 함수 `summarizePatch`(formSchema.ts)=항목명+인원/건수만(★값 미노출), note 는 API 전송 제외(`kind`), `.chat-msg.note` success 토큰 재사용(신규 색 0). 회귀 가드 `verify-chat-patch-summary.mjs` 31/31 PASS(★PII 누출 0 종합) + verify-all 69/69·단언 1852/0 회귀無, tsc/build green, Playwright 실 AI 대화 가시 확인(콘솔 0건). 조문·엔진·`mergeFormPatch`·산출물 무접촉. 커밋 `5be47d3`. 2026-06-21 완료.
- [x] (개발팀) **상담 답변(advisor·Pillar 2) 생성 실패 시 무손실 재시도("다시 시도") — ChatPanel verify-chat-resend 의 상담측 패리티 마감** — AdvisorChat.ask 는 전송 직전 setInput("")로 입력을 비운 뒤 실패 시 마지막 assistant 자리표시자를 "오류: …"로만 바꿔, 사용자가 질문을 **다시 타이핑**해야 했다(계약 작성 ChatPanel 은 무손실 재전송을 이미 갖췄으나 상담측 부재 갭). 입력란 원문 복원은 "실패 중 새로 타이핑한 내용 덮어쓰기" 새 유실을 만들므로, 입력란 무접촉 + 실패 질문이 이력 버블로 보존된 점을 이용해 동일 이력 재전송. fetch+스트림을 딜리버리 코어 `deliver(base)`로 분리(최초·재시도 공용, 송신 단일 출처 `sanitizeHistory(base)`), `ask`=`deliver([...msgs,질문])`·`retry`=`deliver(msgs.slice(0,-1))`(오류 자리표시자 1개만 제거), 실패 자리표시자 `error:true`→`role=alert` 오류+"다시 시도"만(피드백/복사/출처 미노출, canRetry=비-busy && 마지막이 error). 중지(abort)·AbortController 배선 무변경(verify-advisor-stop 무회귀). 회귀 가드 `verify-advisor-resend.mjs` 28/28 PASS(★재전송 동일성=최초↔재시도 송신 페이로드 완전 동일·질문 무중복·오류 자리표시자 미전송 대조 증명) + verify-all 81/81·단언 2293/0 회귀無, tsc/build green(전 10라우트), Playwright 실동작(실 API 키, fetch 502 패치→"오류:"+다시 시도·오류엔 피드백/복사 無→복원 후 클릭→질문 재타이핑 0·새 답변·이전 답변 보존·콘솔 0). 페르소나·/api/advisor·RAG·로깅·산출물·조문 무접촉(표시/전송 경계만). 2026-06-21 완료. → **계약(ChatPanel)·상담(AdvisorChat) 양 챗 무손실 재전송/재시도 패리티 완비.**
- [x] (개발팀) **계약 대화(chat·Pillar 1) 전송 실패 시 무손실 재전송("다시 시도")** — `ChatPanel.send` 가 전송 직전 `setInput("")` 후 오류 시 친화 메시지만 표시 → 재타이핑 마찰. **입력란 복원(`setInput(raw)`) 대신** 입력란 무접촉 `retry()`(진행 중 타이핑 무손실): fetch+응답 처리를 `deliver(history)` 로 분리(최초/재전송 공용), 실패 시 보존된 사용자 버블 이력을 그대로 재전송. 순수 함수 `buildChatApiMessages`(formSchema.ts, 최초=재전송 동일 페이로드 단일 출처) + `다시 시도` 버튼(`canRetry`=오류+미응답 사용자 메시지, btn 재사용·CSS 0). 회귀 가드 `verify-chat-resend.mjs` 27/27 PASS + verify-all 70/70·단언 1879/0 회귀無, tsc/build green, Playwright 가시 확인(502 실패→버블 보존+`다시 시도`→재전송 성공·중복 0·콘솔 0). 조문·엔진·전송 규약·PII 토큰화·산출물 무접촉. 2026-06-21 완료.
- [x] (개발팀) **렌더 크래시 에러 경계(error.tsx·global-error.tsx) + 복구 UI — 유실 방지 계열의 마지막 갭(렌더 신뢰성) 마감** — 로컬 우선(localStorage) 구조라 사용자는 "내 계약"에 시간을 들여 법적 서류 데이터를 입력하는데, 앱에 에러 경계가 전무(`error.tsx`/`global-error.tsx`/ErrorBoundary 0개)해 한 컴포넌트라도 렌더 중 throw 하면 Next 기본 에러 화면으로 떨어져 복구 동선·안내 없이 작업이 끊기던 결함 마감. `ErrorRecovery`(복구 UI 단일 출처: 안심 문구 "내 계약 데이터 브라우저 안전 보관"·다시시도(reset)·처음으로(href="/")·role=alert·digest) + `src/app/error.tsx`(세그먼트 경계 /·/app·/advisor·/login) + `src/app/global-error.tsx`(루트 레이아웃 최후 폴백·자체 html/body·globals.css) + globals.css `.error-recovery`(브랜드 토큰 재사용·신규 색 0). 표시 전용=조문·엔진·검증·산출물 무접촉. 회귀 가드 `verify-error-boundary.mjs` 23/23 PASS + `npm run verify` 60/60·단언 1497/0(회귀無), tsc/build green(전 10라우트), Playwright 실동작(임시 throw 라우트→복구 카드 렌더·"다시 시도"/"처음으로"·정상 /app 콘솔 0건). 커밋 `ef0c41c`·`2aaeb47`. 2026-06-21 완료. → **유실 방지 5축 완비**(dirty=세션 내/백업=세션 간/삭제 undo=실수 삭제/저장 실패=저장소 쓰기/에러 경계=렌더 신뢰성).
- [x] (개발팀) **PDF 인쇄창 팝업 차단 시 거짓 성공·거짓 신선도 차단** — DocStep "🖨 PDF 생성"이 `window.open` null(팝업 차단) 반환 시 빌더는 alert만 띄우고 void 반환했는데 `onPdf`가 무조건 "PDF 인쇄창을 열었습니다" + `setGenSnap`(생성 신선도 스냅샷)을 기록 → **만든 적 없는 PDF가 fresh로 오인**(17:07/17:21 신선도 작업이 막은 "미생성 오인"이 팝업 차단 경로에 잔존)되던 정확성 갭 마감. `generateDocPDF`/`generateJointPDF`가 열림=true/차단·문서정의부재=false 반환(index.ts 파사드 boolean 전파), `onPdf`는 `opened===true`일 때만 성공 표시+genSnap 기록·차단 시 친화 안내+**genSnap 미기록**(거짓 신선도 방지). 조문·엔진·빌더 본문·산출물 내용 무접촉(반환 신호+호출부 분기만). 회귀 가드 `verify-pdf-popup-guard.mjs` 33/33 PASS + verify-all 56/56·단언 1386/0 회귀無, tsc/build green, Playwright 가시 확인(차단→실패안내·stale 미발생 / 열림→성공+HTML write→입력 수정 시 stale 정상). 2026-06-21 완료. → 생성 신선도 정합성(단건·헤더 일괄·PDF 팝업 차단) 3경계 마감.
- [x] (개발팀) **공동사업표준협약서(joint) PDF 팝업 차단 시 거짓 성공 차단 — JointForm.onPdf 반환값 분기** — 17:21 작업이 빌더(`generateJointPDF`)·파사드(`generateJointPDFDoc`)를 "차단=false/열림=true"로 고쳤으나(가드 [E] 검증), **호출부 `JointForm.onPdf` 가 반환값을 무시**하고 무조건 "PDF 인쇄창을 열었습니다" 표시 → 팝업 차단으로 안 열렸는데도 거짓 성공이 잔존(DocStep 은 [F] 배선 검증됐으나 **joint 컴포넌트 배선은 가드 부재**였던 갭). `onPdf` 가 `const opened=generateJointPDFDoc(...)` 수신→삼항 분기(열림=성공/차단=친화 실패 안내). 회귀 가드 `verify-pdf-popup-guard.mjs` [G] 4종 추가 37/37 PASS + verify-all 72/72·단언 1957/0 회귀無, tsc/build green(전 10라우트), Playwright 가시 확인(차단→실패안내+alert 1회·거짓 성공無 / 열림→성공+협약서 7,893자 기록·콘솔 0). 커밋 `2a7f657`. 빌더·조문·산출물 무접촉(호출부 분기만). 2026-06-21 완료. → **담보신탁·joint 양 폼 PDF 팝업 차단 거짓 성공 차단 패리티 완비.**
- [x] (개발팀) **미저장 변경(dirty) 추적 + 유실 방지** — localStorage 무자동저장 구조에서 저장 후 추가 편집/이탈 시 조용한 데이터 유실 가드. SaveBar dirty 표시(`● 저장되지 않은 변경`↔`✓ 저장됨`)+`beforeunload`+goHome confirm. 회귀 가드 `verify-dirty-tracking.mjs` 7/7 PASS, tsc/build green, Playwright 가시 확인. 2026-06-21 완료.
- [x] (개발팀) **dirty 가드 확장 — 위저드↔다른 계약 열기(loadContract) 시 미저장 경고** — `openContract`에 `isFormDirty` confirm 가드 추가(goHome과 동일 패턴). 회귀 가드 `verify-dirty-tracking.mjs` [G] 추가 9/9 PASS, tsc/build green, Playwright 가시 확인(취소 시 덮어쓰기 차단). 2026-06-21 완료. → goHome/beforeunload/loadContract 유실 경로 3종 마감.
- [x] (개발팀) **미리보기 입력 디바운스** — DocStep 우측 미리보기가 매 키 입력마다 완성 문서 HTML(37KB+) 재생성·iframe 재파싱하던 비용을 250ms 디바운스(`useDebounced`, 미리보기 한정)로 제거. 입력 필드·검증 게이트는 raw form 직결(즉시성·정확성 보존), 조문·빌더 무손상. 회귀 가드 `verify-preview-debounce.mjs` 8/8 PASS + `verify-doc-preview` 33/33 회귀無, tsc/build green, Playwright 가시 확인(입력 즉시·미리보기 250ms 후 반영). 2026-06-21 완료.
- [x] (개발팀) **미리보기 "갱신 중…" 인디케이터** — 250ms 디바운스 대기 구간에 피드백이 없어 미리보기가 "멈춘 것"처럼 보이던 인지 공백 해소(직전 5개 iteration 1순위 후보 마감). `previewPending = form !== debouncedForm`(store 새 참조 스프레드 = 갱신 대기 신호)일 때만 펄스 점+"갱신 중…" 조건부 렌더(`role=status`/`aria-live` a11y, `prefers-reduced-motion` off). 디바운스 계약 무변경(미리보기 debouncedForm·검증 raw form), 조문·엔진 무접촉. 회귀 가드 `verify-preview-updating.mjs` 12/12 PASS + debounce 8/8·doc-preview 33/33 회귀無, tsc/build green, Playwright 가시 확인(입력 전 부재→60ms 표출 "갱신 중…"→250ms 후 소멸). 2026-06-21 완료.
- [x] (개발팀) **서류 미리보기 "🔍 크게 보기"(새 창 전체 크기) — 좁은 2분할 패널 정독 한계 해소(완성도 UX)** — DocStep 미리보기가 sticky·뷰포트 높이 제한·~12px 라 다중 페이지 법적 서류 정독이 어렵던 갭 마감. 순수 헬퍼 `openDocPreviewWindow(html, openFn)`(src/lib/ui/preview-window.ts, window.open 주입·`opened/empty/blocked` 신호·html 무변형 write·PDF 팝업 가드와 동형 차단 처리) + DocStep preview-head `🔍 크게 보기` 버튼(`previewDocHTML` 출력을 자동인쇄 스크립트 없이 그대로 새 창에 — 읽기 전용). 회귀 가드 `verify-preview-window.mjs` 33/33 PASS + verify-all 67/67·단언 1781/0 회귀無, tsc/build green, Playwright 가시 확인(클릭→새 탭 전체 문서 5,362자·script 부재·콘솔 0건). 조문·엔진·산출물 무접촉. 2026-06-21 완료. → (후보) joint(공동사업협약) 폼에도 동일 적용(현재 DocStep 한정). ✓ 19:30 마감.
- [x] (개발팀) **공동사업표준협약서(joint) 미리보기 "🔍 크게 보기"(새 창 전체 크기) — 담보신탁 DocStep 과 동형 정독 검수 동선 마감** — JointForm 은 미리보기가 전무해 Word/PDF 생성 전 본문(표준협약 13조+특약 7조, 2페이지) 육안 확인 동선이 없던 갭 마감(담보신탁 DocStep 의 "크게 보기" 패리티). `previewJointHTML(jointForm)`(builders.js, `buildJointFullHTML`+`stripAutoPrint`=자동인쇄 스크립트 제거된 읽기 전용 완성 HTML·담보신탁 `previewDocHTML` 과 동형) + index 파사드 노출 + JointForm `🔍 크게 보기` 버튼(`openDocPreviewWindow` 재사용·차단 시 친화 안내, CSS·헬퍼 신규 0). 회귀 가드 `verify-joint-preview-window.mjs` 30/30 PASS + verify-all 68/68·단언 1811/0 회귀無, tsc/build green(전 10라우트), Playwright 가시 확인(클릭→새 탭 4,127자 전체 협약서·script 0개=인쇄창 안 뜸·사업명 WYSIWYG 반영·콘솔 0건). 조문·엔진·산출물 무접촉. 커밋 `4705472`. 2026-06-21 완료. → joint 우측 실시간 2분할 미리보기는 레이아웃 재구성 필요·별도 후보. ✓ 19:39 마감.
- [x] (개발팀) **공동사업표준협약서(joint) 우측 실시간 2분할 미리보기 — 담보신탁 DocStep 과 동형 WYSIWYG 정독 동선 마감(완성도 UX)** — JointForm 에 "크게 보기"(새 창)만 있고 입력↔결과를 나란히 보는 실시간 2분할 미리보기가 부재하던 마지막 DocStep 패리티 갭 마감. `previewJointHTML(debouncedForm)` 출력을 `iframe srcDoc` 으로 격리 렌더(실 산출물과 동일·자동인쇄 스크립트 0)하는 우측 패널 + 250ms 디바운스·`previewPending` "갱신 중…" 인디케이터·팝업 차단 preview-note(전부 DocStep 동형), "크게 보기"는 preview-head 로 이동. 기존 CSS(`.doc-split`/`.preview-*`)·헬퍼(`useDebounced`·`openDocPreviewWindow`) 재사용 = **CSS·헬퍼 신규 0**. `onExpandPreview` 는 라이브 `jointForm` 유지(기존 가드 무회귀). 회귀 가드 `verify-joint-preview-window.mjs` [G] 10종 추가 40/40 PASS + verify-all 68/68·단언 1821/0 회귀無, tsc/build green(전 10라우트), Playwright 가시 확인(2분할 렌더·iframe 4,157자 전체 협약서·script 0·사업명/갑 상호 WYSIWYG·콘솔 0 errors). 빌더·엔진·산출물 무접촉(표시 전용). 커밋 `8e20062`. 2026-06-21 완료. → **담보신탁·joint 양 폼 미리보기 패리티(우측 2분할 + 크게 보기) 완비.**
- [x] (개발팀) **localStorage 저장 실패(용량 초과·비활성) 가드 — 유실 방지 계열의 마지막 갭(저장소 쓰기 신뢰성)** — 로컬 우선 구조라 저장 실패=즉시 데이터 유실인데, 모든 쓰기 경로가 거치는 `writeAll`이 `setItem`을 무방비 호출해 `QuotaExceededError`(용량 초과·5~10MB 한정)·`SecurityError`(사생활 모드)가 영문 DOMException 으로 그대로 노출되고 복구 동선(백업·정리)이 안내되지 않던 결함 마감. `StorageWriteError`(Error 상속·`quota` 플래그) + 순수 헬퍼 `isQuotaExceeded`(브라우저 5변종 식별·null 무크래시)·`storageWriteErrorMessage`(단일 출처·quota 분기) 신설 → `writeAll` try/catch 단일 지점 보강(setItem 실패 시 기존 저장본 무손상=부분기록 없음, 친화적 한글 안내 rethrow). SaveBar 기존 catch 경로 재사용=UI 마크업 무변경. 회귀 가드 `verify-storage-write-guard.mjs` 27/27 PASS([D]★용량 초과→StorageWriteError(quota=true)+기존 데이터 무손상 behavioral) + verify-all 46/46·단언 1075/0 회귀無, tsc/build green. 조문·엔진·산출물 무접촉. 2026-06-21 완료. → **유실 방지 4계열 완비**(dirty=세션 내/백업=세션 간/삭제 undo=실수 삭제/저장 실패 가드=저장소 쓰기).
- [x] (개발팀) **계약 백업(내보내기)·복원(가져오기)** — 로컬 우선(localStorage) 데이터의 영구 유실 갭 마감(캐시 삭제·기기 변경·동료 공유). `contractRepo.ts`에 순수 헬퍼 4종(`makeBackup`/`parseBackup`/`isValidRow`/`mergeImported`=**비파괴 병합**: 새 id만 추가·기존 보존·중복/손상 격리) + `export/importContracts`. ContractsView `.contracts-backup` 액션 바(내보내기=JSON 다운로드·0건 disabled / 가져오기=file input·**0건일 때도 노출**해 새 기기 복원 가능). 회귀 가드 `verify-backup.mjs` 25/25 PASS + duplicate 18/18 회귀無, tsc/build green, Playwright 가시 확인(내보내기→캐시삭제 시뮬→가져오기 복원→재가져오기 멱등 0추가→비정상 파일 형식 가드). 조문·엔진·생성 로직 무접촉. 2026-06-21 완료. → dirty 가드(세션 내) + 백업(세션 간) = 유실 방지 완비.
- [x] (개발팀) **내 계약 "복제"(duplicate)** — 동일 위탁자·유사 구조 계약 반복 작성 가속. `contractRepo.duplicateContract`(form_data 깊은 복사로 참조 분리·새 uuid·시각·상태 draft 초기화) + 순수 헬퍼 `nextCopyTitle`(중첩 방지·번호 유니크)/`makeDuplicateRow`(src 무변형) 분리 export, ContractsView 카드에 `복제` 버튼(CSS 신규 0, btn-ghost 재사용). 회귀 가드 `verify-contract-duplicate.mjs` 18/18 PASS(제목 규칙·메타 보존·깊은 복사 원본 미오염·원본 불변), tsc/build green, Playwright 가시 확인(완료 계약 복제→`(사본)` 작성중·원본 보존·재복제 `(사본 2)`·콘솔 0건). 조문·엔진·검증 무접촉. 2026-06-21 완료.
- [x] (개발팀) **내 계약 삭제 실행취소(undo)** — 로컬 우선 구조의 마지막 비가역 유실 경로(삭제) 마감. 기존 `confirm` 후 영구삭제를 **즉시 삭제 + 7초 실행취소**(Gmail식)로 교체. 순수 헬퍼 `restoreRow`(멱등·무변형) + `restoreContract`(contractRepo.ts), `.contracts-undo` 바(role=status). 회귀 가드 `verify-contract-undo-delete.mjs` 15/15 PASS + duplicate 24/24·backup 25/25 회귀無, tsc/build green(전 10라우트), Playwright 가시 확인(삭제→undo 바→실행취소 시 위탁자·소재지까지 완전 복원, 콘솔 0건). 조문·엔진 무접촉. 2026-06-21 완료. → dirty 가드(세션 내)+백업(세션 간)+삭제 undo = **유실 방지 3계열 완비.**
- [x] (개발팀) **복제 직후 사본 바로 열기** — 복제의 목적은 거의 항상 "사본을 고쳐 새 계약 만들기"인데 기존 `onDuplicate`는 목록만 새로고침해 사본을 다시 찾아 `열기`해야 했다(복제→탐색→열기 3스텝). `duplicateContract` 반환 id 로 직후 `listContracts()`에서 사본을 찾아 `onOpen(copy)`로 바로 위저드 로드(기존 dirty 가드 재사용 — 미저장 변경 시 confirm·취소해도 사본은 목록에 추가됨). 회귀 가드 `verify-contract-duplicate.mjs` [E] 추가 24/24 PASS, tsc/build green(전 10라우트), Playwright 가시 확인(복제→위저드 `(사본)` 즉시 전환·콘솔 0건 / dirty 시 confirm·취소해도 `(사본 2)` 목록 추가). 조문·엔진 무접촉. 2026-06-21 완료. → 11:14 복제 도입 이후 7개 iteration 1순위 후보 마감.
- [x] (개발팀) **저장된 계약 "이어서 작성" 시 미완 서류 단계로 진입** — `loadContract`가 항상 STEP 01로 떨어지던 재개 마찰 마감(draft를 며칠에 걸쳐 이어 쓸 때 어디까지 했는지 직접 재탐색해야 하던 갭). 순수 함수 `firstIncompleteDocStep(form)`(validate.ts, 기존 `validateDoc`로 첫 생성불가 서류 step 반환·없으면 null) 신설 → `loadContract`가 미완 서류 step(tab/idx)으로 진입, 전부 충족 시 STEP 01(처음부터 검토). 회귀 가드 `verify-resume-step.mjs` 12/12 PASS + validate-jump 15/15·doc-readiness 14/14 회귀無, tsc/build green(전 10라우트), Playwright 가시 확인(작성중→Doc 04 valReport 바로 진입·검증박스 안내 / 완료→STEP 01부터, 콘솔 0건). 조문·엔진·검증 판정 무접촉. 2026-06-21 완료.
- [x] (개발팀) **계약 체결일 달력 유효성 — 실재하지 않는 날짜(2월 31일 등) 차단** — 일 드롭다운이 월 무관 1~31 노출 + 검증 게이트가 존재만 검사해 **2026-02-31 같은 무효 날짜로 계약서 생성 가능**하던 정확성 결함 마감(체결일=7종 서류 자동반영 핵심 데이터, 법적 효력 문서). 순수 헬퍼 `daysInMonth`/`isRealDate`(calc.ts, 윤년 4·100·400 단일 출처) + `validateDoc` 무효 날짜 차단(import·구버전·AI머지 방어) + `StepBasic` 일 드롭다운 월별 유효일만 노출·연/월 변경 시 말일 보정(드롭다운 무효 생성 0). 회귀 가드 `verify-date-validity.mjs` 37/37 PASS + resume-step 12/12·validate-jump 15/15·doc-readiness 14/14 회귀無, tsc/build green, Playwright 가시 확인(3월 max31→2월 31→28 보정·2028 윤년 2월 max29, 콘솔 0건). 조문·엔진·DOCX 무접촉. 2026-06-21 완료.
- [x] (개발팀) **금액 입력 유효성 — 가격·원본가액 "유효한 양(+)의 금액" 강제** — 검증 게이트가 가격(`appform.valuationPrice`)·원본가액(`valReport.principalValue`)을 `hasText`로만 검사해 "미정"·"0"·"-5000"도 통과시켜 **빌더가 빈칸/잘못된 금액으로 렌더한 법적 서류**가 생성되던 정확성 결함(체결일 2월31일과 동일 유형) 마감. 순수 헬퍼 `isPositiveAmount`(calc.ts, `parseAmount>0` 단일 출처) + `validateDoc` 2단계 검사(누락 / 유효하지 않은 금액, import·구버전·AI머지 방어). 회귀 가드 `verify-amount-validity.mjs` 39/39 PASS + validate 의존 가드 9종(date-validity 37·doc-readiness 14·validate-jump 15·resume-step 12·progress-summary 12·batch-generate 11·contracts-readiness 12·contracts-batch 16·sort-filter 21) 전종 회귀無, tsc/build green(전 10라우트), Playwright 가시 확인(가격 -5000→"유효하지 않은 금액"·Word 버튼 비활성, 양수→안내 소멸, 콘솔 0건). 조문·엔진·생성 로직 무접촉. 2026-06-21 완료.
- [x] (개발팀) **우선수익자 개별 대출금액 유효성 — 음수/무효(0·비숫자) 개별 금액 차단** — 게이트가 대출금액을 합계 `some(>0)`로만 검사해 우선수익자 다수 시 한 명이라도 양수면 통과 → 개별 음수/무효가 그 사람의 **우선수익한도금액(=loanAmount×비율)을 음수/잘못된 금액**으로 법적 서류 표에 박던 결함(실측: 한도금액 자동산정이 `12,000,000,000`→`11,999,994,000`으로 -5000 차감 확인). 순수 헬퍼 `isPositiveAmount`(calc.ts, 단일 출처) 재사용 + `validate.ts` `commonMissing`에 합계 양수일 때 개별 루프 추가(`hasText && !isPositiveAmount`→`우선수익자 N 대출금액 (유효하지 않은 금액)`, 1-based 식별·STEP 02-1 점프, **빈 값은 합계검사로 충분히 커버해 무효 오탐 없음**, import·구버전·AI머지 방어). 회귀 가드 `verify-loan-amount-validity.mjs` 48/48 PASS + validate 의존 가드 10종(amount 39·date 37·doc-readiness 14·validate-jump 15·resume 12·progress 12·batch 11·contracts-readiness 12·contracts-batch 16·sort-filter 21) 전종 회귀無, tsc/build green, Playwright 가시 확인(우선수익자 2인 중 -5000 → 진행 0/7·Word·PDF 둘 다 비활성·점프 버튼 STEP 02-1 이동, 콘솔 0건). 조문·엔진·생성 로직 무접촉. 2026-06-21 완료. → **데이터 정합성 계열(체결일·가격/원본가액·개별 대출금액) 핵심 3종 마감**, 남은 금액=trustFee(영향 점검 후).
- [x] (개발팀) **신탁보수(trustFee) 유효성 게이트 편입 — "0·음수·비숫자" 무효 신탁보수 차단(빈 값은 허용=무회귀)** — UI에 `*` 필수지만 게이트 미포함이던 trustFee가 무효(-5000 등)일 때 빌더가 별첨3 보수액을 `₩-5,000.-`·valReport를 `-5,000 원`으로 법적 서류에 박던 정확성 결함(가격·원본가액·개별 대출금액과 동일 유형) 마감. ★영향 점검: **빈 값은 차단하지 않음**(빌더가 "신탁보수 미입력 (STEP 04)"로 명시 렌더 → 빈 trustFee 기존 계약 무회귀), **채웠지만 0·음수·비숫자만 차단**. `validate.ts` `commonMissing`에 `hasText && !isPositiveAmount`→`신탁보수 (유효하지 않은 금액)`(STEP 04 점프, `isPositiveAmount` 단일 출처 재사용, import·구버전·AI머지 방어, 전 7종 공통 차단). 회귀 가드 `verify-trustfee-validity.mjs` 45/45 PASS + validate 의존 가드 11종(loan 48·amount 39·date 37·doc-readiness 14·validate-jump 15·resume 12·progress 12·batch 11·contracts-readiness 12·contracts-batch 16·sort-filter 21) 전종 회귀無, tsc/build green, Playwright 가시 확인(7/7 계약 trustFee -5000 → 0/7·Word·PDF 비활성·Doc 02 점프 STEP 04 / 빈 값 클리어 시 7/7 복원, 콘솔 0건). 조문·엔진·생성 로직 무접촉. 2026-06-21 완료. → **데이터 정합성 금액 계열 완전 마감**(체결일·가격·원본가액·개별 대출금액·신탁보수 5종 = 게이트가 검사하는 모든 금액 필드).
- [x] (개발팀) **신탁기간(trustPeriod) 필수 입력 게이트 편입 — 게이트가 검사하지 않던 마지막 필수 공통 항목 마감** — UI에 `*` 필수이나 게이트 미포함이던 trustPeriod가 빈 값일 때 valReport(원본가액신고서) kvRow `c.trustPeriod || ""`로 신탁기간을 **placeholder 없이 빈칸**으로, appform은 사용자가 입력하지 않은 **하드코딩 기간 문구로 대체** 렌더하던 정합성 결함(금액 5종과 동일 "존재 검사 누락" 유형) 마감. ★영향 점검: `blankContractForm` 기본값이 비어있지 않아 정상 계약 무회귀(빈 신탁기간은 애초에 올바른 적 없음), trustFee와 달리 명시 placeholder 없어 빈 값 **차단**이 정합. `validate.ts` `commonMissing`에 `!hasText(trustPeriod)`→`신탁기간`(STEP 04 점프) 추가, **자유 텍스트라 형식 아닌 "존재"만 검사**(추정 형식 강제 금지), import·구버전·AI머지 방어. 회귀 가드 `verify-trustperiod-validity.mjs` 34/34 PASS + validate 의존 가드 12종 전종 회귀無, tsc/build green, Playwright 가시 확인(7/7→신탁기간 클리어 시 0/7·전 서류 ⚠·Word/PDF 비활성·Doc 02 점프 STEP 04 / 재입력 시 7/7 복원, 콘솔 0건). 조문·엔진·생성 로직 무접촉. 2026-06-21 완료. → **필수 공통 입력 게이트 완전 마감**(당사자·우선수익자·대출금액·물건·체결일·신탁보수·신탁기간 = UI `*` 필수 공통 항목 전부 + 금액 5종 유효성).
- [x] (개발팀) **우선수익한도 비율(priorityRatio) 유효성 게이트 편입 — 음수·범위 밖 비율 차단(0·빈 값은 빌더 기본 120% 처리로 허용=무회귀)** — 게이트가 검사하지 않던 마지막 "법적 수치 입력". priorityRatio는 우선수익한도금액(=대출금액×비율)을 좌우하는 핵심 수치(별첨2/3·appform 한도표·valReport priorityLimit)인데, UI `type=number min/max`는 타이핑을 막지 못해 음수(-50)·범위 밖(200·1200)이 저장 가능 + import·구버전·AI머지 유입 → 한도금액을 음수/과대로 산출하던 결함(금액·날짜 계열과 동일 유형) 마감. 신규 단일 출처 `isValidRatio`(calc.ts, 빌더 `||120`와 정합·`parseAmount(v)||120`을 [100,150]로 검사 → 0/빈/비숫자는 120 처리로 통과·음수·범위 밖만 무효) + `validate.ts` `commonMissing`에 `우선수익한도 비율 (100~150% 범위)`(STEP 02-1 점프) 차단(전 7종 공통, StepLoanCalc onChange 무변경=타이핑 UX 회귀 방지). 회귀 가드 `verify-priorityratio-validity.mjs` 63/63 PASS + validate 의존 가드 13종 전종 회귀無, tsc/build green(전 10라우트), Playwright 가시 확인(비율 120 7/7→200 변경 시 0/7·전 서류 ⚠·Word/PDF 비활성·Doc 02 점프 STEP 02-1 / 130 복원 시 7/7, 콘솔 0건). 조문·엔진·빌더 무접촉. 2026-06-21 완료. → **게이트가 검사하는 모든 정량 입력(필수 공통 7종 + 금액 5종 + 비율 범위) 정합성 완전 마감.**
- [x] (개발팀) **사업자등록번호(bizNo) 유효성 게이트 편입 — 국세청 표준 체크섬 차단** — 신청서 관계사 표(`partyTable`)·별첨에 각 당사자의 사업자등록번호가 그대로 박히는데(`bizP1-bizP2-bizP3`) 게이트가 검사하지 않아 오타로 체크섬 깨진/부분 번호가 법적 서류에 들어가던 결함(정량 입력 마감 후 남은 "정형 식별번호") 마감. 신규 단일 출처 `isValidBizNo`(calc.ts, 10자리+국세청 가중치 `[1,3,7,1,3,7,1,3,5]` 체크섬) + `validate.ts` `commonMissing` `checkBiz` 루프(`length>0 && !isValidBizNo`→`{label} N 사업자등록번호 (유효하지 않은 번호)`, 위탁자 STEP 01/우선수익자 STEP 02, 채무자·수익자는 sameAsTrustor면 미검사·별도 입력 시만 검사, **빈 값은 선택 입력이라 허용=무회귀**) + `PartyCard.tsx` 10자리 완전입력+체크섬오류 시 인라인 `role=alert` 안내(부분 입력엔 미표시). 회귀 가드 `verify-bizno-validity.mjs` 49/49 PASS + validate 의존 가드 14종 전종 회귀無, tsc/build green(전 10라우트), Playwright 가시 확인(124-81-00999→인라인 안내·Doc 02 검증박스·Word/PDF 비활성, 00998 정정 시 안내 소멸, 콘솔 0건). 조문·엔진·빌더 무접촉. 2026-06-21 완료. → 정량 입력 + 정형 식별번호(사업자등록번호) 정합성 게이트 마감.
- [x] (개발팀) **부동산 등기 고유번호(regNo) 형식 유효성 게이트 편입 — 14자리 형식 차단(★추정 체크섬 금지)** — 신탁부동산 표(신청서 partyTable·계약서 별지, `builders.js: tc(p.regNo)`)에 박히는 마지막 정형 식별번호. 게이트가 자릿수를 검사하지 않아 오타·부분 입력(12자리 등)이 법적 서류에 그대로 들어가던 결함 마감. ★사업자번호·법인번호와 달리 **공개 표준 체크섬이 존재하지 않으므로** 체크섬을 임의 생성하지 않고(추정 금지 — 유효 실제 번호 오탐 차단 방지) "정확히 14자리" 형식 완결성만 검사(앱 OCR/추출 정규식 `\d{4}-\d{4}-\d{6}` 동일 출처). 신규 단일 출처 `isValidRegNo`(calc.ts, 숫자만 추출→length===14) + `validate.ts` `commonMissing` 소재지 다음 `hasText(regNo) && !isValidRegNo`→`부동산 N 등기 고유번호 (형식 오류 — 14자리)`(STEP 03 점프, 빈 값 허용=선택 입력 무회귀, import·구버전·AI머지 방어) + `StepProperty.tsx` 라이브 자릿수 인디케이터(`role=alert` "현재 N자리", 14자리 도달 시 소멸). 회귀 가드 `verify-regno-validity.mjs` 51/51 PASS([G]★어떤 14자리든 통과=임의 체크섬 오탐 없음 포함) + validate 의존 가드 15종 전종 회귀無, tsc/build green(전 10라우트), Playwright 가시 확인(12자리→인라인 안내·Doc 02 점프 버튼·Word/PDF 비활성 / 14자리→소멸, 콘솔 0건). 조문·엔진·빌더 무접촉. 2026-06-21 완료. → **정형 식별번호 정합성 게이트 마감(사업자번호 10·법인번호 13·등기 고유번호 14자리).** 실제 등기번호 체크섬 공식 확인 시 isValidRegNo만 보강.
- [x] (개발팀) **법인등록번호(corpRegNo) 유효성 게이트 편입 — 표준 13자리 체크섬 차단(법인 type 한정)** — 계약서 본문·관계사 표·별첨에 박히는 정형 식별번호 중 사업자번호에 이어 게이트가 검사하지 않던 마지막 항목. 신규 단일 출처 `isValidCorpRegNo`(calc.ts, 13자리+가중치 `[1,2,1,2,…]` 체크섬, 앱 내 실재 상수 `110111-7125720`로 검증) + `validate.ts` `checkCorpReg` 루프(`type==="법인"`+`length>0 && !isValidCorpRegNo`→`{label} N 법인등록번호 (유효하지 않은 번호)`, 위탁자 STEP 01/우선수익자 STEP 02·채무자/수익자 sameAsTrustor면 미검사) + `PartyCard.tsx` 법인·13자리 완전입력+체크섬오류 시 인라인 `role=alert`(개인·부분입력 미표시). ★개인=생년월일 칸이라 체크섬 비대상(사업자번호와의 핵심 차이), 빈 값 무회귀(선택 입력), PII는 restorePIIDeep로 토큰 복원 후 검증(전송 없음). 회귀 가드 `verify-corpreg-validity.mjs` 55/55 PASS(+[H]개인 type 비대상) + bizNo 49/49 등 validate 의존 가드 회귀無, tsc/build green(전 10라우트), Playwright 가시 확인(110111-7125721→인라인 안내·7125720 정정 시 소멸·개인 전환 시 소멸, 콘솔 0건). 조문·엔진·빌더 무접촉. 2026-06-21 완료. → **정형 식별번호(사업자번호 10자리+법인번호 13자리) 정합성 게이트 마감.** 남은 정형 식별번호=물건 등기 고유번호(Property.regNo).
- [x] (개발팀) **법인 전용 입력 affordance(대표이사·사내이사·법인등기부 PDF) 개인 비노출** — `PartyCard.tsx`가 이사회 직위(대표이사·사내이사)·법인등기부 OCR 버튼을 type 무관 항상 노출해 자연인(개인) 당사자에게 무의미한 칸을 보여주던 입력 혼동 마감(12:14 식별번호 라벨 정합 thread 입력측 잔여 갭). `isCorp=type!=="개인"` 단일 출처로 세 affordance를 `{isCorp && …}` 게이트(★**비파괴**: 모델 값 미클리어→법인↔개인 전환 시 보존, ★**사업자등록번호는 유지**=개인사업자 보유). 산출물·서명란·빌더·조문·검증·데이터 모델 **무접촉**(개인 서명란 형식=추정 금지→사업팀 M1 verbatim 대조 후 별도). 회귀 가드 `verify-party-corp-fields.mjs` 12/12 PASS + 빌더/validate 의존 가드 6종(party-id-label 8·corpreg 55·birthdate 65·bizno 49·appform-party-id-label 10·doc-preview 33) 회귀無, tsc/build green(전 10라우트), Playwright 가시 확인(법인→대표이사 `홍대표` 입력→개인 전환 시 3종 숨김·사업자등록번호 유지·생년월일 라벨→법인 재전환 시 필드 복귀+`홍대표` 보존, 콘솔 0건). 2026-06-21 완료. → 입력측 법인 전용 affordance 개인 비노출 마감.
- [x] (개발팀) **신청서(appform) 관계사 표 식별번호 라벨 ↔ 산출물 정합성** — 계약서 본문·입력 UI는 `개인 ? 생년월일 : 법인등록번호` 분기인데 **신청서 appform 관계사 표만** 라벨을 항상 "법인등록번호"로 하드코딩(DOCX `partyTable` 1174·HTML `partyRowsHTML` 1753)해 **개인 당사자의 생년월일이 "법인등록번호" 라벨 아래 박히던** 산출물 내부 불일치 마감. 계약서 본문 분기를 미러링(추정 조문 0·법인 산출물 byte 무변경, 개인일 때만 "생년월일")·`partyGroup` 4종(위탁자·채무자·수익자·우선수익자) 전부 적용. 회귀 가드 `verify-appform-party-id-label.mjs` 10/10 PASS([D]★동적: 실제 `previewDocHTML(form,"appform")` 렌더로 개인⇒생년월일·법인⇒법인등록번호 증명·미리보기 iframe 동일 경로) + doc-preview 33·party-id-label 8·corpreg 55·birthdate 65·bizno 49·regno 51 회귀無, tsc/build green(전 10라우트). 조문·엔진·검증 게이트·데이터 모델 무접촉. 2026-06-21 완료. → **식별번호 라벨 입력↔전 산출물 정합성 완전 마감**(입력 UI·계약서 본문·신청서 단일 분기).
- [x] (개발팀) **당사자 식별번호 입력 라벨 ↔ 산출물 라벨 정합성** — 빌더(builders.js)는 식별번호 칸을 `type==="개인" ? "생년월일" : "법인등록번호"`로 렌더하는데 입력 UI(PartyCard)는 type 무관 항상 "법인등록번호"여서 **개인 당사자가 "법인등록번호" 칸에 생년월일을 입력**하던 입력↔출력 불일치 마감. 순수 헬퍼 `partyIdLabel(type)`(calc.ts, 빌더 분기와 동일 단일 출처) 신설 → PartyCard 라벨·placeholder를 type 분기(데이터 모델·빌더·검증·조문 무변경=산출물 byte 무변경). 회귀 가드 `verify-party-id-label.mjs` 8/8 PASS([B]빌더 정합성 단언 포함) + corpreg 55·bizno 49 무회귀, tsc/build green(전 10라우트), Playwright 가시 확인(법인→"법인등록번호"·개인→"생년월일" 즉시 전환, 콘솔 0건). 2026-06-21 완료. → (후보) 대표이사·사내이사 등 법인 전용 필드 type 분기 + appform partyTable 라벨 일관화(산출물 변경=verbatim 영향 점검 후).
- [x] (개발팀) **개인 당사자 생년월일(YYMMDD) 실재 날짜 유효성 게이트 편입 — 법인=법인등록번호 체크섬과 대칭되는 마지막 식별번호 정합성 갭 마감** — 개인 식별번호 칸은 산출물에 "생년월일"(앞 6자리 YYMMDD)로 렌더되는데(builders.js) 게이트가 `type==="법인"`만 검사하고 개인은 전혀 검사하지 않아 `991332`(월13)·`990230`(2월30일) 등 실재하지 않는 날짜가 법적 서류에 박히던 결함. ★추정 체크섬 금지(주민번호 체크섬 2020.10 폐지=공개식 부재 → 등기번호와 동일하게 미적용), 앞 6자리 **달력 유효성만** 검사(체결일과 동일 `isRealDate`/`daysInMonth` 단일 출처, 세기는 뒤 7자리 첫 자리 국가 표준 코드로 해석·미입력 시 윤년 가능). 신규 `isValidBirthDate`(calc.ts) + `validate.ts` `checkBirth` 루프(개인 한정·sameAsTrustor 미검사·빈 값 허용=무회귀) + `PartyCard.tsx` 인라인 alert. 회귀 가드 `verify-birthdate-validity.mjs` 65/65 PASS([J]★세기 코드 2000s 윤년 2/29 통과·1900s/1800s 평년 차단 포함) + validate 의존 가드 13종 전종 회귀無, tsc/build green(전 10라우트), Playwright 가시 확인(개인 전환→생년월일 라벨·991332 인라인 alert·900101 정정 시 소멸·Doc 02 점프 버튼·Word/PDF 비활성, 콘솔 0건). 조문·엔진·빌더 무접촉. 2026-06-21 완료. → **정형 식별번호 정합성 게이트 완전 마감**(사업자번호 10·법인번호 13·등기번호 14·개인 생년월일 YYMMDD).
- [x] (개발팀) **대형 금액 입력 라이브 에코(천단위 콤마 + 한글 금액)** — 신탁 서류에 박히는 두 핵심 대형 금액(신탁부동산 가격 `appform.valuationPrice`·신탁재산 원본가액 `valReport.principalValue`, 수십~수백억)이 raw 입력만으로 0 개수를 눈으로 검증할 인접 수단이 없던 갭 마감(대출금액은 StepLoanCalc 인접 산정값으로 검증되나 DocStep 두 금액은 부재). `DocField`에 선택 `money?` 플래그 추가→두 필드 표기, `DocStep`이 `f.money && parseAmount(val)>0`일 때 입력 아래 `.amount-echo`(`fmtKRW`+`amountToHangul`, 기존 순수 헬퍼·빌더와 동일 한글 출처=입력↔출력 정합) 렌더. principalValue는 기존 저장본 콤마 호환 위해 type="text" 유지(money로만 에코). 표시 전용=조문·엔진·산출물·검증·입력 type·저장 구조 무접촉. 회귀 가드 `verify-amount-echo.mjs` 20/20 PASS + 의존 가드(amount-validity 39·doc-readiness 14·contracts-readiness 12·doc-preview 33) 회귀無, tsc/build green(전 10라우트), Playwright 가시 확인(가격 120억→`12,000,000,000 원`·`일백이십억원정`/음수→에코 소멸/원본가액 콤마 입력 `8,000,000,000 원`·`팔십억원정`, 콘솔 0건). 2026-06-21 완료.
- [x] (개발팀) **대출금액·우선수익한도금액 표 한글 금액 라이브 에코** — 12:43 DocStep 두 대형 금액 에코에 이어, 세 번째이자 가장 많은 서류에 박히는 대형 금액(대출금액→우선수익한도금액=별첨2/3·appform 한도표·valReport priorityLimit 4종+)의 0 개수 육안 검증 보조 마감. StepLoanCalc 표가 `toLocaleString` 숫자만 보이던 갭에 행별 대출금액·행별 한도·합계 2칸 모두 `amountToHangul` 한글 에코(`.loan-hangul`) 추가(`parseAmount>0`/`total>0` 조건). 표시 전용=조문·엔진·산출물·검증·입력 onChange·산정 로직(priorityLimitFor·totalLoan·totalPriorityLimit) 무접촉, 한글은 빌더(builders.js)와 동일 `amountToHangul` 출처=입력↔출력 정합. 회귀 가드 `verify-loan-hangul-echo.mjs` 21/21 PASS + amount-echo 20/20 회귀無, tsc/build green(전 10라우트), Playwright 가시 확인(국민은행 120억→`일백사십사억원정`한도·신한 50억→`육십억원정`·합계 한도 204억→`이백사억원정`/대출금액 0→에코 소멸·한도 `—`·합계 동적 재계산, 콘솔 0건). 2026-06-21 완료. → **대형 법적 금액 한글 에코 3계열 완전 마감**(가격·원본가액=DocStep / 대출금액·우선수익한도금액=StepLoanCalc).
- [x] (개발팀) **위저드 헤더 "남은 필수 입력 통합 체크리스트"(중복 제거 + 항목별 단계 점프)** — 헤더가 "서류 N/7" 카운트+firstBlocked(첫 막힌 서류 1종 이동)만, 각 DocStep 검증박스는 그 서류 한정만 보여줘 "계약 전체에서 앞으로 뭘 더 입력해야 하나"를 한눈에 볼 수단이 없던 비-산출물 탐색성 갭 마감. 기존 `docReady` useMemo를 `{docReady,missingList}` 단일 패스로 확장(서류별 `validateDoc.missing`을 label 기준 중복 제거=공통 누락 7×→1회, 37건→7건) → 헤더 `.doc-progress`에 접이식 `<details>` 체크리스트(목록 비면 미노출), 각 항목 기존 `.validate-jump`·`goStep(stepIdx)` 재사용으로 탭·스텝 동시 점프. 회귀 가드 `verify-missing-checklist.mjs` 23/23 PASS([F]통합 목록=각 서류 누락 합집합과 정확 일치 포함) + verify-all 36/36 PASS·단언 897, tsc/build green, Playwright 가시 확인(빈 양식 6건→중복없는 점프→소재지 클릭 STEP 03 전환→입력 시 6→5 증분, 콘솔 0건). 조문·엔진·빌더·검증 판정·데이터 모델 무접촉(validateDoc 결과 읽어 표시만). 2026-06-21 완료.
- [x] (개발팀) **진행 단계(stepper) 사이드바 키보드/스크린리더 접근성** — 좌측 `진행 단계` 항목 13개가 `<div onClick>`이라 키보드(Tab/Enter/Space) 포커스·활성화 불가·스크린리더 미인식·포커스 표시 부재(탭·서브스텝 pill 은 `<button>`인데 stepper만 div=일관성 결함, 웹접근성 KWCAG). `<button type="button">`로 교체(네이티브 키보드 처리·`aria-current="step"`·클릭 `goStep` 보존) + globals.css `<button>` 리셋(전폭·좌측정렬·무배경·폰트 상속=외관 픽셀 동일)+`:focus-visible` 아웃라인. 조문·엔진·빌더·검증·산출물 무접촉(네비 시맨틱만). 회귀 가드 `verify-stepper-a11y.mjs` 15/15 PASS + `npm run verify` 38/38·단언 927 PASS/0 FAIL(회귀無), tsc/build green(전 10라우트), Playwright 가시 확인(stepper 13개 전부 BUTTON·포커스 가능·**비활성 항목 포커스 후 실제 Enter 키로 STEP 전환 실증**·외관 무변·콘솔 0건). 2026-06-21 완료. → 위저드 주 네비게이션 3종(탭·pill·stepper) 키보드 접근 통일.
- [x] (개발팀) **DocStep 입력 라벨↔컨트롤 접근성(htmlFor/id)** — 7종 서류 주 데이터 입력면(`DocStep.tsx` generic 필드)이 `<div className="field-label">`+이름 없는 컨트롤이라 라벨 클릭 포커스·스크린리더 접근명 부재(특히 placeholder 없는 textarea=접근명 0)이던 폼 a11y 갭 마감. text/amount·select·textarea를 `<label htmlFor={fid}>`+컨트롤 `id={fid}`(`doc-${docId}-${key}` 단일 패턴)로 연결, radio는 `role=radiogroup`+`aria-labelledby`, toggle은 기존 감싼 label 유지. 산출물·조문·엔진·검증·데이터 모델·CSS 무접촉(라벨 시맨틱만). 회귀 가드 `verify-docstep-label-a11y.mjs` 10/10 PASS + `npm run verify` 39/39·단언 937 PASS/0 FAIL(회귀無), tsc/build green(전 10라우트), Playwright 가시 확인(Doc 01 5필드 라벨 `for`→실 컨트롤·라벨 클릭 시 포커스·orphan div.field-label 0·money 에코 무회귀, 콘솔 0건). 2026-06-21 완료. → (후속·비산출물) STEP 입력 컴포넌트(StepBasic·StepProperty·StepLoanCalc·PartyCard) 동일 패턴 a11y 정비 + alert aria-live 일관성 감사.
- [x] (개발팀) **STEP 입력 컴포넌트(StepBasic·StepProperty·PartyCard) 라벨↔컨트롤 접근성** — DocStep a11y 직후 후속. 담보신탁 1차 범위 핵심 입력 STEP 3종의 `<div className="field-label">`+이름 없는 컨트롤을 `<label htmlFor>`+id 로 연결(단일 필드), 복합(체결일자 년·월·일 / 식별번호 앞·뒤 / 사업자번호 3칸)은 그룹 라벨 id+`role=group aria-labelledby`+하위 input 개별 `aria-label`. PartyCard 는 `fid(key)=party-${role}-${idx}-${key}` 단일 출처로 다중 렌더 id 충돌 방지. 산출물·조문·엔진·검증·데이터 모델·isCorp 게이트·partyIdLabel 분기·CSS 무접촉(라벨 시맨틱만). 신규 가드 `verify-step-input-a11y.mjs` 29/29 PASS + 기존 가드 2종(party-corp-fields·party-id-label) 옛 div 하드매칭을 새 마크업으로 갱신(불변식 보존), `npm run verify` 40/40·단언 966 PASS/0 FAIL, tsc/build green(전 10라우트), Playwright 가시 확인(STEP 01 PartyCard·STEP 04 StepProperty·STEP 05 StepBasic 전부 label[for]→실 컨트롤·라벨 클릭 시 포커스·복합 그룹 named·무연결 div 0·콘솔 0건). 2026-06-21 완료. → (후속) StepConditions·StepLoanCalc·JointForm a11y + alert aria-live 감사.
- [x] (개발팀) **a11y 후속(1차 범위분) — StepConditions·StepLoanCalc 라벨↔컨트롤** — StepConditions(재사용 `Section` 컴포넌트에 `id` 필수 prop 신설→헤딩 div id, collateralType·majorityCriteria select `aria-labelledby`=헤딩, 건축주 명의·인허가 유형 select `<div className="field-label">`→`<label htmlFor>`+id, 대리금융기관 input aria-label, 담보보수·담보차수 라디오 묶음 `role=radiogroup aria-labelledby`, 무연결 div.field-label 0)·StepLoanCalc(비율 `<label htmlFor>`+input id, 표 행별 대출금액 input `aria-label`='회사명 또는 우선수익자 N' 폴백). 조문 자동반영 분기 onChange·산출물·검증 게이트 무접촉. 회귀 가드 `verify-conditions-loan-a11y.mjs` 21/21 PASS + `npm run verify` 41/41·단언 987 PASS/0 FAIL(회귀無), tsc/build green(전 10라우트), Playwright 가시 확인(select 4종 접근명·라디오 2종 labelledby·비율 라벨 클릭 포커스·표 input aria-label·콘솔 0건). 2026-06-21 완료. → 위저드 담보신탁 1차 범위 입력 전면 a11y 마감(네비 3종+DocStep+STEP 입력 5종).
- [x] (개발팀) **인라인 검증 오류 ↔ 입력 컨트롤 접근성 연결(aria-invalid + aria-describedby)** — 라벨↔컨트롤(15:08·15:24)에 이은 a11y thread 마지막 갭(오류 상태의 프로그래밍적 연결) 마감. 인라인 검증 오류(PartyCard 사업자번호·법인번호·생년월일 / StepProperty 등기번호)가 `role="alert"` div 로 표시만 되고 입력 컨트롤엔 `aria-invalid`·`aria-describedby` 부재라 스크린리더가 필드 포커스 시 invalid 상태·오류 내용을 못 얻던 결함(WCAG 3.3.1·4.1.2). 오류 input 에 `aria-invalid={flag||undefined}`+`aria-describedby={flag?errId:undefined}`(무오탐), 오류 div 에 매칭 id(corp/birth 는 type 상호배타라 활성 1개 참조). 기존 `role="alert"` 보존(이중 안내). 회귀 가드 `verify-inline-error-a11y.mjs` 12/12 PASS([D]★고아 참조 0 포함) + `npm run verify` 42/42·단언 999 PASS/0 FAIL(회귀無), tsc/build green(전 10라우트), Playwright 가시 확인(사업자번호 체크섬 오류 → 3칸 input aria-invalid+describedby 가 role=alert 오류 div 로 정확 해소 → 유효 정정 시 양쪽 해제·div 소멸, 콘솔 0건). 조문·엔진·검증 게이트·데이터 모델 무접촉. 2026-06-21 완료.
- [x] (개발팀) **a11y 후속(잔여) — JointForm 라벨↔컨트롤 + 메시지성 live region `role="status"` 표기 일관성** — JointForm(공동사업, 1차 범위 외) 11필드 `<div className="field-label">`→`<label htmlFor>`/그룹(법인등록번호) 전환·생성 메시지 span `role=status` + `amount-echo`·`loan-hangul`(기존 `aria-live`만)에 `role="status"` 가산해 메시지성 live region 표기 통일(고아 aria-live 0). 회귀 가드 `verify-joint-a11y.mjs` 19/19 PASS([D4]★aria-live 만 갖던 회귀 차단) + `npm run verify` 43/43·단언 1018 PASS/0 FAIL(회귀無), tsc/build green(전 10라우트), Playwright 가시 확인(JointForm 라벨 10 전부 컨트롤 해석·상호 라벨 클릭 포커스·법인등록번호 role=group+앞/뒷자리 aria-label·무연결 div 0, 콘솔 0건). 조문·엔진·빌더·검증·데이터 모델 무접촉. 2026-06-21 완료. → **위저드 a11y thread 완전 마감**(네비 3종+DocStep+STEP 입력 5종+인라인 오류 상태+JointForm+live region 일관성). 비-산출물 a11y 후보 소진.
- [x] (개발팀) **위저드 pagenav 접근명 + breadcrumb 장식 구분자 SR 숨김** — "a11y thread 완전 마감" 선언이 놓쳤던 잔여 네비 a11y 2종. ①pagenav 이전/다음 버튼(아이콘 글리프 ‹ › 만 담은 유일한 아이콘 전용 무명 `<button>`, 매 step 전환 사용)에 `aria-label`("이전/다음 단계")+글리프 `aria-hidden` span(WCAG 4.1.2) ②breadcrumb 장식 구분자 span.sep 6곳(TrustApp 5·AdvisorApp 1) `aria-hidden="true"`(WCAG 1.3.1, SR 가 "›"/"·" 미announce). 순수 가산·외관 무변(pagenav 는 이미 button)·onClick/goStep·disabled 경계 가드 보존. 회귀 가드 `verify-nav-a11y.mjs` 10/10 PASS + `npm run verify` 44/44·단언 1028 PASS/0 FAIL(회귀無), tsc/build green(전 10라우트), Playwright 가시 확인(pagenav 2종 접근명·글리프 aria-hidden·구분자 5곳 전부 aria-hidden·콘솔 0건). 조문·엔진·산출물 무접촉. 2026-06-21 완료.
- [x] (개발팀) **breadcrumb crumb `<span onClick>`→`<button>` 키보드 접근 전환** — TrustApp 글로벌 breadcrumb 의 상호작용 crumb 4종(신탁사 선택·회사·서류종·내 계약)이 stepper 와 동일하게 `<span className="crumb" onClick>`(키보드 포커스·Enter 불가·SR 미인식)이던 마지막 `<span onClick>` 네비 잔재 마감. `<button type="button">`화(현재 뷰면 `aria-current="page"`)+현재 위치 라벨 span 에 aria-current 가산, globals.css **`button.crumb` 한정** 리셋(`font/color/line-height: inherit`·무배경/무테두리/무패딩·appearance none=span 외관 픽셀 유지)+`:focus-visible` 아웃라인(★일반 `.crumb` span/Link 무접촉). onClick(goHome/setView) 경로·AdvisorApp(이미 Link/라벨) 무접촉. 회귀 가드 `verify-breadcrumb-a11y.mjs` 20/20 PASS + `npm run verify` 45/45·단언 1048 PASS/0 FAIL(회귀無), tsc/build green(전 10라우트), Playwright 가시 확인(crumb 4종 BUTTON·12px 상속·활성 weight700/ink 보존·**home crumb 포커스 후 Enter 키로 view 전환 실증**·Link crumb=A 무접촉, 콘솔 0건). 조문·엔진·산출물 무접촉. 2026-06-21 완료. → **앱 전수 상호작용 네비(위저드 탭·pill·stepper + 글로벌 breadcrumb) `<span/div onClick>` 잔재 0**.
- [x] (개발팀) **입력창 Enter 전송 IME(한글 조합) 안전 가드** — 채팅(AdvisorChat·ChatPanel)·로그인 Enter 핸들러가 `e.key==="Enter"` 만 보고 IME 조합(`isComposing`)을 확인하지 않아, 한국어 사용자가 마지막 글자를 조합하다 Enter 로 확정하는 순간 **미완성 문장이 조기 전송**되던 입력 결함(100% 한국어 대상 제품 핵심) 마감. 순수 헬퍼 `isComposing`/`isSubmitEnter`(`src/lib/ui/keys.ts`, `nativeEvent.isComposing` 우선) **단일 출처** 신설 → 세 Enter 핸들러 배선(Shift+Enter 줄바꿈·로그인 allowShift 즉시 제출 동작 보존, raw `e.key==="Enter"` 직접 비교 0). 회귀 가드 `verify-submit-key.mjs` 31/31 PASS([B]★조합 Enter 양쪽 경로 전송 아님 [F]배선 회귀 정적 차단) + `npm run verify` 52/52·단언 1262/0(회귀無), tsc/build green(전 10라우트), Playwright 실동작(fetch 스파이: 조합 Enter→요청 0건·입력 보존 / 일반 Enter→`/api/advisor` 요청 1건, 콘솔 0건). 조문·엔진·산출물 무접촉. 2026-06-21 완료(커밋 `5b7f25d`). → 앱 전수 Enter 전송 입력경계 IME 안전성 통일.
- [x] (개발팀) **계약서 생성(Word/PDF) 후 입력 변경 시 "✓ 생성 완료" → "● 입력이 변경되었습니다 — 다시 생성하세요" 전환 (Pillar 1 정확성 trust 갭)** — DocStep "✓ 생성 완료" 확인이 생성 시점 입력에 대한 것인데, 생성 후 사용자가 입력을 고쳐도(금액·당사자 정정) 그대로 남아 **이미 받은 구버전을 최신으로 오인해 잘못된 법적 서류를 제출**할 위험(dirty 가드가 저장 경계를 막듯, 생성-확인 신선도는 미가드)이던 마지막 표시 경계 갭 마감. 순수 함수 `genFreshness(current, snapshot)`(`src/lib/engine/genStatus.ts`, none/fresh/stale 단일 출처) + DocStep 생성 성공 시 `JSON.stringify(form)` 스냅샷 기록(Word·PDF)·`formSnap` vs `genSnap` 으로 stale 시 danger 안내 전환·입력 변경/서류 전환 시 초기화. 값 기반 비교(store dirty 추적 패턴)=참조 의존 없음. 회귀 가드 `verify-generate-freshness.mjs` 15/15 PASS + `npm run verify` 55/55·단언 1344/0(회귀無), tsc/build green(전 10라우트), Playwright 실동작(완료 계약 시드→Doc 01 Word 생성→"✓ 완료"→산정방법 1필드 수정 시 "● 입력이 변경되었습니다 — 다시 생성하세요"(danger) 전환·Doc 02 전환 시 초기화, 콘솔 0). 조문·엔진·빌더·검증·산출물·생성 로직 무접촉(표시 상태만). 2026-06-21 완료. → (후속·비산출물) 동일 신선도 신호를 위저드/목록 일괄생성(generateAllReady·ContractsView)에도 확장 검토.
- [x] (개발팀) **위저드 헤더 "일괄 생성(.docx)" 신선도 확장 — 일괄 생성 후 입력 변경 시 "✓ N종 완료" → "● 입력이 변경되었습니다 — 다시 일괄 생성하세요"** — 17:07 DocStep 단건 신선도의 후속(다음스텝 명시)으로, 헤더 `generateAllReady`(준비 5~7종 일괄 .docx)도 동일 갭(생성 후 편집해도 완료 메시지 잔존→구버전 묶음 오인 제출 위험, 단건보다 영향 큼)이던 것을 마감. 단일 출처 `genFreshness`(genStatus.ts) **재사용**(새 판정 0) — Wizard 에 `batchSnap`(생성 시점 스냅샷)·`formSnap=JSON.stringify(form)`·`batchFreshness` 파생, 일괄 생성 성공 시 스냅샷 기록·입력 변경 시 `batchMsg` 비움 effect·stale 시 danger 안내 우선 렌더. ★ContractsView 목록 일괄생성은 **저장 스냅샷 기반·라이브 편집 부재**라 신선도 비적용(억지 적용 시 노이즈)=의도적 제외. 회귀 가드 `verify-generate-freshness.mjs` **24/24 PASS**([F] 배선 9 추가=DocStep·Wizard 양쪽 genFreshness import·스냅샷 기록·stale 전환 문구·formSnap effect·danger 색 실소스 단언) + `npm run verify` **55/55·단언 1353/0**(회귀無), tsc/build green(전 10라우트 `/app`), Playwright 실동작(담보신탁 신규→공통 필드 입력 readyCount 5/7→일괄 생성 5종 .docx 다운로드+"✓ 준비된 5종…완료"(blue)→소재지 1필드 수정 시 "● …다시 일괄 생성하세요"(danger·role=status) 전환, 콘솔 0). 조문·엔진·빌더·검증·산출물·생성 로직 무접촉(표시 상태만). 2026-06-21 완료. → 생성-확인 신선도 단건(DocStep)+헤더 일괄(Wizard) 양 경계 마감.
- [ ] (개발팀) `priorityRankLabel` 공동순위(동순위) 라벨 지원 — 현재 단순 N순위. 동순위 입력 UX 정의 후 착수.
- [x] (개발팀) **내 계약 목록 정렬·상태 필터(완료↔작성중) 토글** — 검색·준비도 칩 위에 상태 세그먼트(`전체`/`작성중`/`완료`, 실시간 건수)+정렬 셀렉트(`최근수정`/`제목 가나다`/`위탁자순`/`생성 준비도순`) 추가. `filtered`→`visible` 통합 파이프라인(`useMemo`), 산출정의 없는 종류는 준비도 정렬 시 맨 뒤. `.seg`/`.seg-btn`/`.seg-num` 신규(기존 브랜드 토큰). 회귀 가드 `verify-contracts-sort-filter.mjs` 21/21 PASS(+위탁자순 [H]) + readiness 12/12 회귀無, tsc/build green, Playwright 가시 확인(작성중 토글 "2/4건"·준비도순 5/7→0/7·위탁자순 갑>병>을>빈값). 조문·엔진 무접촉. 2026-06-21 완료. → **위탁자순 정렬 추가**(12:08): `contractIdentity` 단일 출처로 가나다 정렬·빈 위탁자 맨 뒤, [H] 3종+[G] 직교성 trustor 편입.
- [x] (개발팀) **좌측 `진행 단계`(stepper) 사이드바 무스타일 수정** — 죽은 `.step`/`.step-num`(미사용 CSS)을 실제 사용명 `.stepper-item`/`.stepper-num`으로 개명해 의도된 디자인(번호 원형 배지·활성 하이라이트) 적용 + sub-step과 동일 ✓/⚠ readiness 배지(`.stepper-flag`) 통일(`docReady` 재사용, 조문 무손상). 회귀 가드 `verify-stepper-styling.mjs` 15/15 PASS(클래스 drift 정적 차단), tsc/build green, Playwright 가시 확인. 2026-06-21 완료.
- [x] (개발팀) **구버전 저장본 로드 크래시 가드** — `loadContract` 얕은머지 시 `docContents` 키 누락 → `validate.ts` TypeError(DocStep 렌더 크래시). validate 옵셔널체이닝 + loadContract docContents 한 단계 병합으로 수정. 회귀 가드 `verify-loadcontract-merge.mjs` 15/15 PASS. 2026-06-20 완료.
- [x] (개발팀) **verify-all 러너 Windows spawn 내성 보강 — 간헐 오탐 FAIL(실행 미완료를 실제 회귀로 오보고) 마감** — `npm run verify`(출시 그린 게이트 단일 명령)가 36종 순차 spawn 중 드물게 자식이 단언까지 실행되지 못하면(spawn 타이밍/loader 전이) `status===0`만 보던 판정이 이를 **실제 회귀 FAIL 로 오보고**(deterministic `verify-dirty-tracking` 단독 9/9인데 통합 간헐 FAIL)하던 신뢰성 결함 마감. 순수 헬퍼 `_verify-all-helpers.mjs`(`parseMarker`/`shouldRetry`/`isGuardPass`, `_`접두사로 가드 글롭 회피) 단일 출처 신설 → **"단언 마커 부재 = 실행 미완료"만 재시도(최대 3회), 마커 있으면(통과·fail>0 무관) 결과 확정·무재시도**(★실제 회귀 미마스킹). 신규 behavioral 가드 `verify-all-resilience.mjs` 15/15 PASS + `npm run verify` 37/37·단언 912 PASS/0 FAIL(회귀 0), 배선 end-to-end 실증(transient→재시도 후 실행미완료 FAIL / fail마커→즉시 FAIL 무재시도), tsc/build green. 앱 소스·조문·엔진 무접촉. 2026-06-21 완료.
- [x] (개발팀) **회귀 가드 통합 러너(verify-all) + 오늘 누적 미커밋 작업 3커밋 정리** — "매 iteration 회귀가드 전종 PASS" 그린 게이트가 단일 명령 부재로 매번 손으로 가드명을 나열·일부만 돌리던 재현성 갭 + 하루치(~20 iteration) 작업 전체가 미커밋(앱 15파일 1182줄+가드 26종)으로 유실 위험이던 M3 갭 동시 마감. `scripts/verify-all.mjs`(verify-*.mjs 전종 발견→동일 플래그 spawnSync 순차 실행·종료코드 권위 판정·요약 문구 편차 대응·실패만 상세 출력·exit 1) + `package.json "verify"` + `.gitignore` 중복 `.vercel` 제거. `npm run verify` → **가드 35/35 PASS·단언 874 PASS/0 FAIL**, tsc/build green 확인 후 3커밋(`747fa9b` 테스트·`15aa6b7` 엔진/유효성·`e58624c` UX). 앱 소스·조문·산출물 무접촉. 2026-06-21 완료. → 이후 매 iteration `npm run verify` 한 줄로 전종 회귀 확인. trust-qna 바이너리·trust-corp 보고문서는 범위 외 제외.

- [x] (개발팀) **계약 대화(chat·Pillar 1) 요청 본문 검증(서버 입력 경계) — `/api/chat` 이 마지막 무검증 AI POST 로 어긋난 본문을 502(Claude 장애)로 오분류·영문 메시지 노출하던 견고성·일관성·정보누출 갭 마감** — 무계정·무인증 공개 POST 인 `/api/chat`(계약 폼 채우기 대화)이 `req.json()` 만 try/catch 후 `body.messages.map()` 으로 검증 없이 소비 → 형태 어긋난 본문(`{}`·`{messages:"x"}`·content 비문자열)이 Claude try 블록에서 TypeError→**502 "Claude 호출 실패: \<영문 TypeError\>"** 오분류·영문 누출(원칙 3). 형제 `/api/advisor`(parseAdvisorBody)·`/api/advisor/feedback` 은 검증해 400 주는데 chat 만 무방비(마지막 무검증 AI POST). 순수 함수 `parseChatBody`(`src/lib/chat/request.ts`, parseAdvisorBody 동일 messages 규약 + 계약 전용 formSummary 문자열 정규화=프롬프트 "undefined" 주입 방지) 신설 + route.ts 단일 지점 검증·`!ok`→400(formSummary 누락은 거절 아닌 `""` 정규화). 회귀 가드 `verify-chat-request.mjs` 41/41 PASS([F]★formSummary 정규화 항상 string) + `npm run verify` 62/62·단언 1566/0(회귀無), tsc/build green(전 10라우트 `ƒ /api/chat`). 조문·엔진·페르소나·도구 정의 무접촉. 2026-06-21 완료. → 전 AI POST 입력 경계 검증 통일. (잔여) ChatPanel 클라이언트 오류 친화 한국어(네트워크 실패 영문 누출 — advisorErrorMessage 의 계약 측 대칭).

- [x] (개발팀) **계약 대화(chat·Pillar 1) 오류 메시지 친화적 한국어 치환 + 오류 분류기 두 Pillar 공용 단일 출처(lib/ui) 승격 — 영문 SDK/네트워크 오류 누출 마감(직전 항목 "잔여" 처리)** — 계약 측 두 오류 경로가 원문을 그대로 노출하던 상담 측 동형 갭 마감: ①`/api/chat` catch 가 `"Claude 호출 실패: "+e.message`(502)로 영문 SDK 메시지를 공개·무인증 엔드포인트에 노출(원칙 3), ②`ChatPanel` catch 가 `e.message`로 네트워크 영문(`Failed to fetch`)·원시 JSON 을 화면에 렌더. 상담 분류기를 두 Pillar 공용 단일 출처 `src/lib/ui/error-message.ts`(`friendlyErrorMessage`·`FRIENDLY_ERROR`, keys.ts 동일 계층)로 승격 + `lib/advisor/error-message.ts` 재노출(상대경로 — 가드 ESM 로더 `@/` 미해석)로 기존 상담 import·가드 무변경. `/api/chat`·`ChatPanel` 배선, `ChatPanel` `!res.ok`→`res.text()` raw throw 로 passthrough 성립(이중 일반화 방지). 회귀 가드 `verify-chat-error-message.mjs` 54/54 PASS([D]★누출 0 [F]배선 [G]★상담 하위호환=동일 함수/상수 재노출) + `verify-advisor-error-message` 44/44 무회귀 + `npm run verify` 가드 63종·단언 1620/0(회귀無·dirty-tracking 기존 libuv 플레이크 외), tsc/build green(전 10라우트 `ƒ /api/chat`), Playwright 실동작(네트워크 실패→친화 한국어·영문 누출 0 / 실서버 정상 응답 무회귀 / 502 raw JSON→친화 한국어 verbatim·JSON 누출 0, 콘솔 0). 커밋 `0fe946a`. 조문·엔진·산출물 무접촉. 2026-06-21 완료. → **전 AI 엔드포인트(상담·계약 양 Pillar) 오류 표시 경계 친화 한국어 치환 통일**(단일 출처 lib/ui/error-message).

- [x] (개발팀) **계약 대화(chat·Pillar 1) 패치 적용 파이프라인 회귀 가드 — `toolInputToPatch`/`normalizePatchIds` 불변식 고정(AI→법적 폼 변환 경계, 가드 0종이던 핵심 다리)** — `ChatPanel.tsx` L76-78 `restorePIIDeep→toolInputToPatch→normalizePatchIds→mergeFormPatch` 파이프라인 중 두 순수 함수(`formSchema.ts`)는 **AI 제안값이 법적 서류 폼에 도달하는 유일한 변환 지점**인데 가드 0종이던 갭 마감. 깨지면 ①별첨4 특약 4요소(`contractOptions`)가 `docContents.contract` 라우팅 실패→조용히 유실, ②법인/사업자번호가 저장 필드(`corpRegFront/Back`·`bizP1/2/3`)로 분리 실패→빌더·검증 게이트 읽는 형태와 어긋남. 현 코드 정상 — 불변식 고정만(`formSchema.ts` 무변경·앱 소스·조문·엔진·산출물 무접촉, 테스트 전용). 회귀 가드 `verify-chat-patch.mjs` 42/42 PASS([A]contractOptions→docContents.contract 라우팅·값 참조 보존 [D/E]corpRegNo/bizNo 대시 유무·텍스트 추출 분리·키 제거 [F]미해당 역할(properties)·비배열/null role 무크래시·다중 당사자 독립 [G]파싱 불가·비문자열 현 동작 보존 [H]★통합 파이프라인 end-to-end·최종 patch store 미인식 잔여 키 0) + `npm run verify` 가드 64종·단언 1662/0(회귀無·신규 +42·dirty-tracking 기존 libuv 플레이크 외), tsc/build green(전 10라우트 `ƒ /api/chat`). 커밋 `1cafb4a`. 2026-06-21 완료. → 계약 대화(Pillar 1) 변환 경계 불변식 고정. (잔여 후보) `summarizeForm`(PII 마스킹) 회귀 가드. → **완료(아래 19:00)**.

- [x] (개발팀) **계약 대화(chat·Pillar 1) 폼 요약 PII 마스킹 회귀 가드 — `summarizeForm` 불변식 고정(폼 컨텍스트→Claude 전송의 유일한 PII 마스킹 경계, 가드 0종이던 직전 thread 명시 잔여 후보)** — `ChatPanel` L58 이 매 요청 보내는 `formSummary=summarizeForm(form)` 의 `form` 은 실데이터(법인등록번호·사업자번호·소재지·신탁보수 원본)인데, 폼 컨텍스트는 `tokenizePII` 경로를 타지 않아 `summarizeForm` 이 곧 폼측 PII 경계. 식별번호 노출이 "개선"으로 잘못 들어오는 회귀 차단(원칙 3). 현 마스킹 정책 고정 — 숨김(법인/사업자번호·소재지·등기번호·신탁보수)·의도적 표시(name·대표이사·대출금액·우선수익한도·계약일·비율). 회귀 가드 `verify-chat-summary.mjs` 36/36 PASS([B]법인등록번호 원본 미등장 [C]소재지·regNo 비노출 [D]신탁보수 마스킹 [E]사업자번호 전면 비노출 [H]★실 PII 가득 폼 요약 누출 0 종합 스캔=식별번호·소재지·보수 15토막 전부 미등장) + `npm run verify` 65/65·단언 1698/0(회귀無·신규 +36), tsc/build green(전 10라우트 `ƒ /api/chat`). 커밋 `7ba41a9`. `formSchema.ts` 무변경·앱 소스·조문·엔진·산출물 무접촉(테스트 전용). 2026-06-21 완료. → 계약 대화(Pillar 1) AI 경계 순수 함수 가드 4종 완비(입력 검증·오류 메시지·패치 적용·폼 요약 PII). (잔여 후보) `restorePIIDeep`/`tokenizePII` 라운드트립 회귀 가드. → **완료(아래 19:07)**.

- [x] (개발팀) **계약 대화(chat·Pillar 1) PII 토큰화·복원 라운드트립 회귀 가드 — `tokenizePII`/`restorePII`/`restorePIIDeep`/`extractIdentifiers` 불변식 고정(사용자 raw 입력→Claude 전송의 유일한 PII 토큰화 경계, 가드 0종이던 직전 thread 명시 잔여 후보)** — `ChatPanel.send` L43 이 매 전송 직전 raw 텍스트를 `tokenizePII(raw, piiMap.current)` 로 토큰화해 무인증 공개 `/api/chat` 으로 보내고(매핑은 클라이언트 useRef 보관), 응답(L69 `restorePII`)·패치(L76 `restorePIIDeep`)에서 역치환한다. `src/lib/privacy/tokenize.ts` 의 이 4개 순수 함수가 곧 "사용자 입력측 PII 경계"이자 "화면·폼 복원 다리"인데 가드 0종이던 갭 마감(요약·패치·요청·오류 경계는 가드화, 이 라운드트립만 미커버). 깨지면 ①토큰화 누락 시 평문 식별번호 Claude 전송(원칙 3 위반), ②복원 누락 시 화면·폼에 `[법인등록번호_1]` 토큰 잔존→법적 서류 데이터 오염. ★실런타임 probe 로 현 동작 고정: **패턴 순서 의존**(법인등록번호 패턴 첫째라 주민번호 형태도 법인 라벨이나 마스킹은 유지=보안 불변식)·동일 값=동일 토큰·counter 누적·in-place. 회귀 가드 `verify-chat-tokenize.mjs` 50/50 PASS([A]4종 토큰화·원본 미등장 [B]★라운드트립 항등 `restorePII(tokenizePII(t))===t` [C]동일 토큰 재사용·counter 연속·in-place [D]★전송문 누출 0 [E]restorePIIDeep 중첩·비문자열/구조 보존 [F]extractIdentifiers 결정론 추출·부분 미추출 [G]엣지(순서 의존·무변형·미매핑 토큰) [H]★ChatPanel 전송→응답→패치 복원 end-to-end·잔여 토큰 0) + `npm run verify` 66/66·단언 1748/0(회귀無·신규 +50), tsc/build green(전 10라우트 `ƒ /api/chat`). 커밋 `ce595a1`. `tokenize.ts` 무변경·앱 소스·조문·엔진·산출물 무접촉(테스트 전용). 2026-06-21 완료. → **계약 대화(Pillar 1) AI 경계 순수 함수 가드 5종 완비**(입력 검증·오류 메시지·패치 적용·폼 요약 PII·PII 토큰화/복원). 양 Pillar AI 입력/전송/복원 경계 회귀 커버리지 확보.

## P1.5 — 상담(advisor·Pillar 2) 가드레일·회귀 커버리지 (신규, 2026-06-21 15:18 발굴)
> 계약(Pillar 1)은 회귀 가드 46종이나 상담(Pillar 2) RAG 는 0종이었음 — 첫 가드 도입(로그 PII)으로 시작. 계약 측 "비-산출물 소진" 선언이 가렸던 두 번째 축의 갭.
- [x] (개발팀) **상담 로그 PII 평문 적재 가드** — 자가고도화 로그(`log.ts: logQuery/logFeedback`)가 사용자 질문·피드백 메모를 `advisor-logs/*.jsonl` 에 평문 적재해 사업자/주민/법인/등기번호가 디스크에 잔류하던 가드레일 결함(CLAUDE.md 원칙 3) 마감. `redactForLog=tokenizePII(s).text`(계약 측과 동일 단일 출처) 신설 → 저장 직전 식별번호만 1방향 토큰화(분석용 주변 텍스트 보존=RAG 공백 판별 100% 유지), 상담 답변·외부 전송·검색 경로 무변경(로그 저장만). 상담 Pillar **최초** 회귀 가드 `verify-advisor-log-redact.mjs` 18/18 PASS + `npm run verify` 47/47·단언 1093/0(회귀無), tsc/build green(`ƒ /api/advisor` 포함). 2026-06-21 완료.
- [ ] (개발팀·★별도 iteration·제품 결정 안건) **상담 입력의 Claude 전송 경로 PII 토큰화 여부** — 계약 폼은 토큰화 후 전송하나 상담은 대화 입력을 raw 전송. 일반 신탁 자문엔 식별번호 불요이나 대화 맥락상 답변 품질 영향 가능 → 전송 경계 토큰화 도입은 **답변 품질 영향 점검 + ★대표님/팀 판단** 선행. (이번엔 저위험 로그 경로만 적용.)
- [x] (개발팀) **상담 RAG 핵심 로직(`retrieve.ts`) 회귀 가드** — 가드 0종이던 RAG 검색 채점/임계 로직 불변식 고정. behavioral 가드 `verify-advisor-retrieve.mjs` 신설(실제 `retrieve()`/`formatContext()` 호출, KNOWLEDGE 미등장 합성 토큰 zorpaa·zbeta + extra 청크로 격리 관측): 태그 가중 3·본문 등장 cap 3·기본 임계 3·bd- 임계 6(노이즈 더 높은 바)·qna- 부스트 ×1.25·topK(기본3)·내림차순·토큰 2자/dedup·빈질의 []·extra 합집합·formatContext 포맷. **28/28 PASS** + `npm run verify` 48/48·단언 1121/0(회귀無), tsc/build green(`ƒ /api/advisor` 포함). 앱 소스·`retrieve.ts` 무변경(테스트 전용). ★retriever 는 의도적 임베딩 교체 대상이라 절대점수 단언[A~C]은 vocab 전용·의미 단언[D~J]은 교체 후 보존하도록 헤더 명시. 2026-06-21 완료. → 상담(Pillar 2) 가드 2종(로그 PII·리트리버) 확보. 잔여 후보=`api/advisor/route.ts` 컨텍스트 주입 경계·`backdata.ts` 인덱싱 가드.
- [x] (개발팀) **상담 근거 출처 식별성 차단 — "참고한 자료" 칩이 페르소나 가드레일을 우회해 back-data 원본 문서명을 노출하던 누출 갭** — 페르소나(route.ts L28-30)는 LLM 답변에서 출처명·회사명·사업장명 비노출을 지시하나, `X-Advisor-Sources` 헤더→칩(`AdvisorChat` `{s.topic}`) 경로는 LLM을 우회해 bd- 청크 `topic`(=원본 문서 제목, 실측 "2020 건설공사 실무가이드북(최종)" 등 내부규정·딜 문서명 자리)을 평문 노출하던 결함(원칙 3) 마감. 신규 순수 모듈 `src/lib/advisor/sources.ts`(`isBackdata`·`publicSourceLabel`=bd→"내부 참고자료"/core→topic 보존·`buildSources`=안전 라벨 dedup으로 내부문서 다수→단일 칩=개수·식별성까지 비노출) → route.ts 인라인 dedup을 `buildSources` 호출로 교체(전송 경계 단일 지점). core 개념어·`formatContext`(가드된 LLM 경계)·답변·검색 무변경=무회귀. 회귀 가드 `verify-advisor-sources.mjs` 23/23 PASS([E]★누출 0=출력에 원본 문서명 토막조차 미등장) + `npm run verify` 49/49·단언 1144/0(회귀無), tsc/build green(`ƒ /api/advisor` 포함), ★실인덱스 end-to-end behavioral(39,221청크 retrieve→bd 4건 적중→헤더 출처 `[{"topic":"내부 참고자료","kind":"backdata"}]`·LEAKED NONE) 실증. 조문·엔진·산출물 무접촉. 2026-06-21 완료. → 상담(Pillar 2) 가드 3종(로그 PII·리트리버·출처 식별성). 잔여 후보=컨텍스트 주입 경계·`backdata.ts` 인덱싱 가드.
- [x] (개발팀) **상담 답변 서류작성 액션 마커(`<<doc:ID>>`) 스트리밍 중 부분 마커 본문 노출 차단** — 페르소나가 서류작성 의사 시 답변 끝줄에 출력하는 내부 마커(`<<doc:collateral|joint|fund>>`)를 클라이언트가 본문 제거+버튼 변환(마커 비노출이 계약)하는데, 마커가 답변 끝에 오고 응답이 토큰 스트리밍이라 닫는 `>>` 도착 전 **부분 마커**(`<<doc:`·`<<doc:collateral`)가 `<ReactMarkdown>` 본문에 잠시 렌더돼 **내부 프로토콜 문자열이 사용자에게 깜빡이던** 결함(15:33 출처 칩 누출·15:18 로그 PII 와 동형=가드된 LLM 경계 옆의 잔여 표시 경로) 마감. 인라인 `parseAction`→순수 모듈 `src/lib/advisor/action-marker.ts` 추출(`sources.ts` 동일 패턴), ★완성 마커 docId 추출·제거 무변경 + 본문 끝 **부분 마커 제거만 가산**(`stripTrailingPartialMarker`: `<<d` 이상만 마커로 간주·단독 `<<`/리터럴 `<<`/단일 `<` 보존=오탐 0), `DOC_IDS`/`DOC_LABEL` 단일 출처 이관. 회귀 가드 `verify-advisor-doc-marker.mjs` 56/56 PASS([B]★스트리밍 부분 마커 전 단계→본문 잔존 0 [D]★오탐 0) + `npm run verify` 50/50·단언 1200/0(회귀無), tsc/build green(`/advisor`·`ƒ /api/advisor` 포함), ★Playwright **실컴포넌트 end-to-end**(fetch 모킹 부분→완성 마커 스트림→MutationObserver 전 프레임 캡처: `everLeakedMarkerDuringStream=false`·최종 본문 마커 없음·서류작성 버튼 정상 렌더·콘솔 0) 실증. 조문·엔진·답변·전송·검색·산출물 무접촉. 2026-06-21 완료. → 상담(Pillar 2) 가드 4종(로그 PII·리트리버·출처 식별성·액션 마커). 잔여 후보=컨텍스트 주입 경계·`backdata.ts` 인덱싱 가드.
- [x] (개발팀) **상담 요청 본문 검증(서버 입력 경계) — 무인증 공개 POST 가 어긋난 messages 에 잡히지 않은 500 으로 죽던 견고성·일관성 갭** — `/api/advisor` 는 `req.json()` 만 try/catch 후 `body.messages` 를 검증 없이 `[...].reverse()`·`.map()` 으로 소비해 형태가 어긋난 본문(`{}`·`{messages:"x"}`·`{messages:[]}`·content 비문자열)이 try/catch *밖*에서 TypeError→**잡히지 않은 500**. ★형제 라우트 `feedback/route.ts` 는 body 검증(400)하는데 메인만 무방비(비일관). 순수 함수 `parseAdvisorBody`(`src/lib/advisor/request.ts`, messages 비빈 배열+원소 `{role,content:string}`+user 1개 이상 검증, 유효 시 messages **무변형** 반환) 신설 → route.ts 단일 지점 검증·`!ok` 시 400(feedback 패턴 미러), 정상 UX·전송·페르소나 무회귀. 회귀 가드 `verify-advisor-request.mjs` 31/31 PASS([B]★크래시 유발 12종→400 [C]원소 오류 9종→거절 [E]멀티턴 무변형) + `npm run verify` 51/51·단언 1231/0(회귀無), tsc/build green(`ƒ /api/advisor` 포함). 조문·엔진·검색·로깅 무접촉. 2026-06-21 완료. → 상담(Pillar 2) 가드 5종(로그 PII·리트리버·출처 식별성·액션 마커·요청 검증). 잔여 후보=컨텍스트 주입 경계·`backdata.ts` 인덱싱 가드.
- [x] (개발팀) **상담 back-data 인덱스 로더(`backdata.ts`) 견고성·캐싱 회귀 가드 + `parseBackdataIndex` 순수 함수 추출 — P1.5 "잔여 후보=backdata.ts 인덱싱 가드" 마감** — 상담 RAG 인덱스(`_backdata-index.json`, 실측 66MB·39,221청크)를 매 요청 경로(retrieve)에서 로드하는 `loadBackdataChunks`가 가드 0종이던 마지막 Pillar 2 핵심 모듈. 깨진 JSON·비배열(부분 쓰기·디스크 오류)이 throw 로 새면 무인증 공개 엔드포인트 통째 500, 캐싱(`if (cache) return cache`)이 깨지면 매 요청 66MB 재파싱. 파싱 로직을 순수 함수 `parseBackdataIndex(raw|null)`(단일 출처, 어떤 입력에도 throw 0·비배열/깨진 JSON→[])로 추출 + fs `readBackdataRaw` 분리, 원본 동작 100% 보존(유효 배열 형태 미검증 통과). 회귀 가드 `verify-advisor-backdata.mjs` 28/28 PASS([G]★손상 18종 fuzz throw 0 [H]★캐싱 2회 동일 참조+실인덱스 39,221청크 형태 실증 [I]캐싱 게이트·견고성 배선 정적 고정) + `npm run verify` 61/61·단언 1525/0(회귀無), tsc/build green(전 10라우트). 조문·엔진·산출물·페르소나 무접촉. 커밋 `1c539f7`. 2026-06-21 완료. → **상담(Pillar 2) 가드 11종 — Pillar 2 핵심 모듈 회귀 커버리지 확보.**
- [x] (개발팀) **상담 답변 생성 "중지(Stop)" 버튼 — 토큰 스트리밍 답변을 사용자가 끊을 표준 수단 부재 마감(B2B 챗 UI 기본 동선)** — 상담 답변은 서버 토큰 스트리밍이라 길거나 빗나간 답변을 끝까지 기다려야 했고, 생성 중 "전송" 버튼은 `disabled={busy}` 로만 비활성화돼 진행 스트림을 끊을 표준 수단(전 챗 UI 기본 기능)이 전무하던 갭 마감. `AdvisorChat` 에 `abortRef`+`stopGenerating()`(AbortController) 도입 — `ask()` 가 요청마다 컨트롤러 생성·`fetch({signal})` 전달, 입력행 버튼을 `busy ? (■ 중지·btn-stop) : (전송)` 삼항으로 전환. ★중지=오류 아님: `signal.aborted` 분기에서 부분 답변 보존·빈 자리표시자(▍ 커서)만 제거·`advisorErrorMessage`("오류:")는 비-abort(else) 분기에서만 호출(누출 경계 무회귀). `.btn-stop`(danger 토큰·hover·focus-visible). 회귀 가드 `verify-advisor-stop.mjs` 29/29 PASS([B]★중지/전송 상호배타·전송 disabled 잔재 0 [C]★abort=오류 아님·부분 보존·자리표시자 pop·abort 먼저 판정) + `npm run verify` 59/59·단언 1474/0(회귀無), tsc/build green(전 10라우트), Playwright 실동작(dev 3100, 신호 연동 슬로우 스트림 모킹: 생성 중 "■ 중지" 단독+부분답변 렌더→중지 시 부분답변 보존·오류 prefix 없음·blink 미잔류·"전송" 복귀·복사/피드백 출현·콘솔 0). 조문·엔진·답변·전송·검색·로깅·페르소나 무접촉. 2026-06-21 완료. → 상담(Pillar 2) 가드 10종.
- [x] (개발팀) **상담 컨텍스트 주입 경계 출처 식별성 차단 — `formatContext`가 back-data 원본 문서명(내부규정·딜 문서명)을 LLM 프롬프트에 그대로 주입하던 잔여 누출 벡터 마감(헤더/칩 `buildSources` 일반화와 동일 단일 출처를 프롬프트 경계에도 적용=원칙 3 심층방어)** — `formatContext`(retrieve.ts)가 `[참고자료 N] (${chunk.topic})`로 bd- 청크 원본 문서명을 그대로 프롬프트에 실어, 페르소나 비노출 지시에만 의존하던 갭(헤더 경계는 15:33 `sources.ts`가 막았으나 프롬프트 경계는 "가드된 경계"로 무변경이던 의도적 미적용 지점) 마감. **동일 단일 출처**(`isBackdata`/`BACKDATA_LABEL`)로 bd- 라벨만 "내부 참고자료" 치환, **청크 본문(실제 지식)은 그대로 주입=RAG 품질 무손상**, core 개념 topic 보존. 순환참조 무(sources.ts→retrieve.ts 는 `import type` 런타임 소거). 회귀 가드 `verify-advisor-context-injection.mjs` 21/21 PASS([C]★혼합 목록 누출 0=bd 원본 문서명·식별 토막 미등장 [D]본문 보존 [F]단일 출처 정합) + `npm run verify` 58/58·단언 1445/0(회귀無), tsc/build green(전 10라우트). 조문·엔진·검색 채점·산출물·페르소나 무접촉. 2026-06-21 완료. → 상담 출처 식별성 헤더/칩(buildSources)+컨텍스트 주입(formatContext) 양방향 마감. 상담(Pillar 2) 가드 9종. 잔여 후보=`backdata.ts` 인덱스 로더 가드.
- [x] (개발팀) **상담 멀티턴 대화 이력 요청 경계 정제 — 내부 액션 마커(`<<doc:…>>`)가 표시·복사에선 제거되나 다음 턴 요청 본문엔 그대로 실려 모델 컨텍스트로 되돌아가던 송신측 누출 갭 마감** — assistant 메시지 원시 content(스트림 누적)에 마커가 남아 `ask`가 이력(`base`)을 정제 없이 재전송 → 사용자 비노출 계약 마커가 모델 컨텍스트로 환류(표시 경계 `parseAction`의 대칭 누락) + 부가 필드(`sources`) 통째 전송 갭 동반. 순수 함수 `sanitizeHistory`(action-marker.ts, assistant content→`parseAction.body`·마커뿐 빈 턴 제외·user 무변형·role·content 만) 신설 + AdvisorChat 요청 본문 `messages: sanitizeHistory(base)` 배선(raw `messages: base` 직송 제거). 회귀 가드 `verify-advisor-history-sanitize.mjs` 38/38 PASS([F]★누출 0 [G]배선) + `npm run verify` 57/57·단언 1424/0(회귀無), tsc/build green(전 10라우트), Playwright 실동작(fetch 스파이 2턴: 턴1 끝줄 마커 스트림→턴2 요청 본문 assistant 이력 마커 제거·sources 미전송·화면 누출 0·서류작성 버튼 보존·콘솔 0). 조문·엔진·답변·전송·검색·페르소나 무접촉. 2026-06-21 완료. → 상담 마커 비노출 표시 경계+송신 경계 양방향 마감. 상담 가드 8종.
- [x] (개발팀) **상담 답변 "복사" 버튼 — 구조화 답변을 실무 문서/메일로 옮기는 표준 동선 마감** — 답변 카드(`AdvisorChat`)에 피드백·출처·서류작성 버튼은 있어도 복사 수단이 없어 표·체크리스트를 드래그 선택해야 하던 갭(동종 채팅 UI 기본 기능 부재) 마감. `copyAnswer(i, body)`(`navigator.clipboard.writeText`·미지원 try/catch 무시·1.5s "✓ 복사됨" 토글) + 피드백 행 우측 `.copy-btn`(접근명 aria-label/title·`:focus-visible`). ★복사 대상=`parseAction` 으로 **내부 액션 마커(`<<doc:…>>`) 제거된 body**(원시 content 미복사=마커 비노출 계약 유지), 답변 완성(`body && !busy`) 시에만 노출. 회귀 가드 `verify-advisor-copy.mjs` 23/23 PASS([B]★복사=body·content 직접복사 잔존 0) + `npm run verify` 54/54·단언 1329/0(회귀無), tsc/build green(전 10라우트), Playwright 실동작(끝줄 마커 스트림→본문·클립보드 둘 다 마커 없음·"✓ 복사됨" 전환 실증·콘솔 0). 조문·엔진·답변·전송·검색·로깅·페르소나 무접촉. 2026-06-21 완료. → 상담(Pillar 2) 가드 7종.
- [x] (개발팀) **상담 오류 메시지 친화적 한국어 치환(표시 경계) — 영문 SDK 메시지·원시 JSON 본문이 100% 한국어 제품 채팅에 그대로 노출되던 UX·정보누출 갭** — ①route.ts mid-stream catch 가 스트림 실패 시 `[오류] "+e.message`로 **영문 SDK 메시지**(overloaded·rate limit·연결오류)를 본문에 흘리고, ②AdvisorChat fetch catch 가 `!res.ok` 시 `res.text()`(=서버 `{error}` 의 **원시 JSON 문자열** `{"error":"잘못된 요청"}`)를 그대로 렌더·네트워크 실패 시 영문 `Failed to fetch` 노출하던 결함(공개·무인증 엔드포인트라 내부 메시지 노출=정보누출, CLAUDE.md 원칙 3) 마감. 순수 함수 `advisorErrorMessage`(`src/lib/advisor/error-message.ts`, 상태코드·키워드 분류로 친화 한국어, **서버 친화 한국어 `{error}` 는 passthrough**=이중 일반화 방지, 어떤 입력도 원문 영문/JSON 미반환=누출 0) 신설 → route.ts·AdvisorChat 양쪽 단일 출처 사용(raw `e.message` 직출 제거). 정상 응답·답변·전송·검색·로깅 무회귀(표시 경계만). 회귀 가드 `verify-advisor-error-message.mjs` 44/44 PASS([D]★누출 0=영문 토큰·중괄호(JSON) 출력 미등장 [F]배선) + `npm run verify` 53/53·단언 1306/0(회귀無), tsc/build green(`/advisor`·`ƒ /api/advisor` 포함), ★Playwright **실동작**(fetch 모킹 2시나리오: `{"error":"잘못된 요청"}`→"오류: 잘못된 요청"·`leaksRawJson=false` / `TypeError("Failed to fetch")`→"오류: 네트워크 연결을 확인한 뒤…"·`leaksEnglish=false`, 콘솔 0). 조문·엔진·산출물 무접촉. 2026-06-21 완료. → 상담(Pillar 2) 가드 6종(로그 PII·리트리버·출처 식별성·액션 마커·요청 검증·오류 메시지). 잔여 후보=컨텍스트 주입 경계·`backdata.ts` 인덱싱 가드.

## 프로세스
- [x] (개발팀) **today-plan M3 — 하루치(13:46 이후) 미커밋 작업 정리** — 14:02~16:18 a11y/process thread(앱 11파일+가드 14스크립트, ~5시간치)가 working tree 에만 존재하던 유실 리스크 마감. 그린 게이트 재검증(`npm run verify` 45/45·단언 1048/0, tsc EXIT 0, build 10라우트) 후 논리 2커밋(`7decdd9` test·`06ea302` feat) 정리, trust-saas src/scripts working tree clean. trust-corp 보고문서·trust-qna 바이너리 범위 외. 2026-06-21 17:02 완료. → 이후 매 iteration 그린 직후 즉시 커밋 권장(재누적 방지).

## 신규 자동화 기회 (사업팀 발굴, 검토 대기)
- (대기)
