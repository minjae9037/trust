# 24시간 연속 개발 워크로그 — 2026-06-25

> 형식: `- HH:MM [팀] 작업 / 검증결과 / 다음스텝`. 연속 워커가 iteration마다 append.

- 08:06 [개발] **실제소유자(ubo)=위탁자 동일인데 위탁자가 모두 법인 입력 지점 교차검증 advisory 마감(검증 가드 신설) — 표시 전용·게이트 아님.**
  - 배경/상태: 직전 세션이 `DocStep.tsx` 에 해당 advisory(uboSameAsCorpTrustor) 를 구현했으나 **미커밋·전용 회귀 가드 부재** 상태로 dangling 되어 있었다(다음 워커 iteration 과 충돌·유실 위험). 입력 지점 교차검증 advisory 패밀리(평가기준일/이사회 회의일/우선수익자 동일주체/신탁보수>한도 등)는 **각 advisory가 전용 `verify-*-advisory.mjs` 가드와 함께 출하**하는 것이 확립된 컨벤션인데 이 건만 가드가 없었다 → 이번 iteration 에서 가드를 신설해 완결 deliverable 로 마감 후 커밋.
  - 작업(2파일): ①`DocStep.tsx`(in-flight 검수·확정) — 실제소유자확인서(docId="ubo")의 "위탁자와 동일 여부"(sameAsTrustor) 라디오를 "동일"(yes)로 표기했는데 위탁자(STEP 02 PartyCard)가 **모두 법인**이면 부드럽게 되짚는다. 근거 = 특정금융정보법 §5의2·시행령: 법인 고객의 실제소유자는 그 법인 지분 25% 이상 자연인(법인 자신은 자신의 실제소유자가 될 수 없음). ★false-positive 방지 = `form.trustors.every(t=>t.type==="법인")` — 위탁자 중 한 명이라도 개인이면 "동일"이 그 개인을 가리킬 수 있어 미표출. 기존 입력(`form.trustors[].type`·ubo `sameAsTrustor`) 파생이라 **새 상태/모델/엔진/조문 무접촉**, 막지 않음(차단 아님·사용자 선택 보존). role=status·aria-live=polite + 선두 ⚠ aria-hidden, 색 var(--c-brown)(차단 적색 아님), field-hint 재사용(새 CSS 0). ②`scripts/verify-ubo-sameastrustor-corp-advisory.mjs` 신규 가드.
  - 검증결과: 신규 가드 **21/21 PASS**([A]DocStep 배선=조건 5종·문구·role=status·⚠ aria-hidden·field-hint·brown·차단 적색 미사용 [B]단일출처/모델 계약=PartyType·model.ubo.sameAsTrustor·schema 라디오 옵션 [C]무회귀=radiogroup·setField 배선·advisory 패밀리 공존 [D]무접촉=validate/builders 문구 미혼입·globals 새 클래스 0). `npm run verify`(verify-all) **165/165·단언 4376 PASS / 0 FAIL**(직전 164/4355 → +1 가드·+21 단언) 회귀無. `npx tsc --noEmit` **EXIT 0** / `npx next build` **EXIT 0**(전 10라우트). ※브라우저 스모크(ubo 단계 진입+위탁자 법인+동일 표기 트리거)는 이번 iteration 미실행 — 정적 가드가 조건식·렌더 마크업을 verbatim 단언하고 tsc 가 스코프·타입을 보장하나 런타임 트리거 실증은 잔여(다음 iteration 후보).
  - 다음스텝: (개발 후보) ubo advisory 브라우저 스모크 실증 / 입력 지점 교차검증 advisory 추가 갈래 발굴(예: 채무자 유형 vs 우선수익자 구조) / 모바일 stepper. (★사업) M1 자체판정 공식 추인 + 추가 정본 교차(출시 게이트 핵심·대표님 1차 출시 선언 전제). (마케팅) 상표·도메인 정식 조회(M4) 미착수.
- 08:12 [개발] **이사회 의사록(Doc 05) 작성인데 위탁자가 모두 개인 입력 지점 교차검증 advisory 신설(+전용 회귀 가드) — 표시 전용·게이트 아님.**
  - 배경/근거: 이사회 의사록(boardMin)은 스키마 정의상 "위탁자(법인) 이사회의 담보신탁 결의" 서류다(이사회=법인의 의사결정 기관). 그런데 위탁자 유형(STEP 02 PartyCard 의 법인/개인)과 이사회 의사록 입력(Doc 05 자유 텍스트)이 서로 다른 화면에서 입력돼, **위탁자가 모두 개인(자연인)인데도 이사회 의사록이 조용히 채워질 수 있었다**(법인 위탁자 폼을 개인으로 바꾸고 Doc 05 미수정, 또는 개인 위탁자인데 서류를 잘못 고른 경우). 개인은 이사회 자체가 없어 이 서류의 작성 전제가 성립하지 않는다. 직전 iteration 의 ubo=위탁자 동일·전원 법인 advisory(c75053c)의 **대칭(역) 형태** — 거기는 every(법인)일 때, 이번은 every(개인)일 때 표출.
  - 작업(2파일): ①`DocStep.tsx` — `boardMinIndividualTrustor` 파생 상수 + 회의 일자(meetingDate) 필드 하단 advisory 렌더. 조건 = `docId==="boardMin" && f.key==="meetingDate" && val 채움(trim>0) && trustors.length>0 && trustors.every(t=>t.type==="개인")`. ★false-positive 방지 = `every(개인)` — 위탁자 중 한 명이라도 법인이면 그 법인의 이사회 결의로 정당하므로 미표출. 날짜 실재 여부와 무관(구조 부정합은 날짜 유효성과 별개)이라 dateInfo 게이팅 없이 "채움" 신호만 봄. 기존 입력(`form.trustors[].type`·`boardMin.meetingDate`) 파생이라 **새 상태/모델/엔진/조문 무접촉**, 막지 않음(차단 아님·사용자 선택 보존). role=status·aria-live=polite + 선두 ⚠ aria-hidden, 색 var(--c-brown)(차단 적색 아님), field-hint 재사용(새 CSS 0). 같은 meetingDate 필드의 기존 boardMeetingAfterContract(회의일>체결일) advisory 와 독립 공존. ②`scripts/verify-boardmin-individual-trustor-advisory.mjs` 신규 가드.
  - 검증결과: 신규 가드 **22/22 PASS**([A]DocStep 배선=조건 5종·문구·role=status·⚠ aria-hidden·field-hint·brown·차단 적색 미사용 [B]단일출처/모델 계약=PartyType·model.boardMin.meetingDate·schema text·date 필드·boardMin 서류 정의 [C]무회귀=boardMeetingAfterContract·uboSameAsCorpTrustor 패밀리 공존 [D]무접촉=validate/builders 문구 미혼입·globals 새 클래스 0). `npm run verify`(verify-all) **166/166·단언 4398 PASS / 0 FAIL**(직전 165/4376 → +1 가드·+22 단언) 회귀無. `npx tsc --noEmit` **EXIT 0** / `npx next build` **EXIT 0**(전 10라우트). ※브라우저 스모크(boardMin 단계 진입+위탁자 전원 개인+회의일자 입력 트리거)는 이번 iteration 미실행 — 정적 가드가 조건식·렌더 마크업을 verbatim 단언하고 tsc 가 스코프·타입을 보장하나 런타임 트리거 실증은 잔여(다음 iteration 후보).
  - 다음스텝: (개발 후보) 입력 지점 advisory 브라우저 스모크 실증(ubo·boardMin 누적) / 추가 갈래 발굴(예: 채무자 유형 vs 우선수익자 구조) / 모바일 stepper. (★사업) M1 자체판정 공식 추인 + 추가 정본 교차(출시 게이트 핵심·대표님 1차 출시 선언 전제). (마케팅) 상표·도메인 정식 조회(M4) 미착수.
- 08:20 [개발] **실제소유자확인서(ubo) "위탁자와 다름"인데 실제 소유자 성명 미입력 입력 지점 교차검증 advisory 신설(+전용 회귀 가드) — 표시 전용·게이트 아님.**
  - 배경: 입력 지점 교차검증 advisory 패밀리에 ubo 갈래의 **보완(대칭) 케이스**를 추가. 직전 08:06 `uboSameAsCorpTrustor`(c75053c)는 "위탁자와 동일"(sameAsTrustor=yes)인데 위탁자가 모두 법인일 때(법인은 자기 실소유자가 될 수 없음) 표출했다. 그 반대 갈래인 **"다름"(no)** 경로는 비어 있었다 — 사용자가 "실제소유자가 위탁자와 다름"을 고르면 실소유자는 위탁자 본인이 아닌 **별도 자연인**이므로 그 성명(uboName)을 반드시 식별해 기재해야 하는데(특금법 §5의2·시행령: 법인 고객의 실제소유자 = 그 법인 지분 25% 이상 보유 자연인), "다름" 라디오와 성명 텍스트가 같은 서류 안 서로 다른 입력이라 **"다름"을 고른 뒤 성명을 비워 둔 채(또는 "동일"→"다름"으로 바꾸고 성명을 안 채운 채) 조용히 진행**돼 산출물 실제소유자확인서 성명칸이 빈칸으로 박힐 수 있었다.
  - 작업(2파일): ①`DocStep.tsx` — text 분기에 `uboDistinctNameMissing` 파생 상수 추가: `docId==="ubo" && f.key==="uboName" && form.docContents.ubo?.sameAsTrustor==="no" && (val 비어 있음/공백)`. ★false-positive 방지 = "다름"을 **명시적으로** 고른 경우에만(미선택·"동일"이면 미표출), 성명이 한 글자라도 있으면 미표출(작성 완료 간주). 기존 입력(`ubo.sameAsTrustor`·`uboName`) 파생이라 **새 상태/모델/엔진/조문 무접촉**, 막지 않음(차단 아님·작성 중 임시 빈칸 등 사용자 선택 보존). role=status·aria-live=polite + 선두 ⚠ aria-hidden, 색 var(--c-brown)(차단 적색 아님), field-hint 재사용(새 CSS 0). ②`scripts/verify-ubo-distinct-name-missing-advisory.mjs` 신규 가드.
  - 검증결과: 신규 가드 **22/22 PASS**([A]DocStep 배선=조건 4종·문구·role=status·⚠ aria-hidden·field-hint·brown·차단 적색 미사용 [B]단일출처/모델 계약=model.ubo.uboName·sameAsTrustor·schema text/라디오 옵션 [C]무회귀=text setField 배선·보완 갈래 uboSameAsCorpTrustor 및 boardMin/valReport advisory 패밀리 공존 [D]무접촉=validate/builders 문구 미혼입·globals 새 클래스 0). `npm run verify`(verify-all) **167/167·단언 4420 PASS / 0 FAIL**(직전 166/4398 → +1 가드·+22 단언) 회귀無. `npx tsc --noEmit` **EXIT 0** / `npx next build` **EXIT 0**(전 10라우트). ※브라우저 스모크(ubo 단계 진입+"다름" 선택+성명 빈칸 트리거)는 이번 iteration 미실행 — 정적 가드가 조건식·렌더 마크업을 verbatim 단언하고 tsc 가 스코프·타입을 보장하나 런타임 트리거 실증은 잔여(다음 iteration 후보, ubo advisory 2갈래 묶음 스모크 권장).
  - 다음스텝: (개발 후보) ubo advisory 2갈래(동일·전원법인 / 다름·성명빈칸) 브라우저 스모크 묶음 실증 / 입력 지점 교차검증 advisory 추가 갈래 발굴 / 모바일 stepper. (★사업) M1 자체판정 공식 추인 + 추가 정본 교차(출시 게이트 핵심·대표님 1차 출시 선언 전제). (마케팅) 상표·도메인 정식 조회(M4) 미착수.
- 08:28 [개발] **우선수익자에 대출금액 입력했는데 피담보채권 채무자·문구(별첨2) 미입력 입력 지점 교차검증 advisory 신설(+전용 회귀 가드) — 표시 전용·게이트 아님.**
  - 배경/근거: 직전까지의 입력 지점 교차검증 advisory 패밀리(날짜 선후·당사자 동일주체·ubo/이사회 구조·한도/보수)는 거의 DocStep(서류 단계)·StepLoanCalc·StepBasic 에 있었는데, **STEP 02 당사자 입력(PartyCard)의 우선수익자 전용 필드 2종에 완결성 공백**이 남아 있었다. 우선수익자에 대출금액(loanAmount)을 입력하면 그 우선수익자는 별첨2 가.우선수익자 표에 기재되는 **활성 당사자**다(신탁본문 제1조 정의: "피담보채권"="별첨2에 적힌 것"·"채무자"="별첨2에 적힌 자" — 신탁이 담보하는 채권을 특정하는 법적 핵심 항목). 그런데 게이트(validateDoc)는 우선수익자의 loanAmount>0 만 검사하고 **피담보채권 채무자(claimDebtor)·피담보채권 문구(securedClaim)는 검사하지 않아**, 대출금액만 채우고 두 칸을 비워 두면 산출물 별첨2 가.우선수익자 표의 채무자·피담보채권 칸이 **빈칸으로 박힌다**(builders.js: HTML 미리보기는 muted "(STEP 02 에서 입력)" 플레이스홀더로 나그하나 실제 DOCX 셀은 빈칸). DocStep `uboDistinctNameMissing`(다름인데 성명 빈칸) 완결성 advisory 와 동형의 **보완 갈래**(거기는 ubo 성명 빈칸, 여기는 우선수익자 별첨2 채무자·피담보채권 빈칸).
  - 작업(2파일): ①`PartyCard.tsx` — `loanActive=isPositiveAmount(party.loanAmount)` + `claimDebtorMissing`/`securedClaimMissing` 파생 상수 추가, showLoanFields(우선수익자 전용) 블록 안 각 입력 직하에 advisory 렌더. ★false-positive 방지 = **loanActive(대출금액 양수)일 때만** — 대출금액 미입력·무효(loanInvalid 별도 적색 안내)인 작성 전/미사용 행은 미표출, 해당 칸이 한 글자라도 있으면 미표출(작성 완료 간주). 기존 입력(loanAmount·claimDebtor·securedClaim) 파생이라 **새 상태/모델/엔진/조문 무접촉**, 막지 않음(작성 중 임시 빈칸 등 사용자 선택 보존). role=status·aria-live=polite + 선두 ⚠ aria-hidden, 색 var(--c-brown)(차단 적색 아님), field-hint 재사용(새 CSS 0). loanInvalid 적색 인라인과 상호배타(loanActive=false면 loanInvalid 영역). ②`scripts/verify-priority-securedclaim-missing-advisory.mjs` 신규 가드.
  - 검증결과: 신규 가드 **24/24 PASS**([A]PartyCard 배선=loanActive·두 조건·문구 2종·role=status·⚠ aria-hidden·field-hint·brown·차단 적색 미사용 [B]단일출처/모델 계약=Party.loanAmount/claimDebtor/securedClaim·isPositiveAmount import·showLoanFields 스코프 [C]무회귀=loanInvalid 인라인·loan-hangul readback·claimDebtor/securedClaim 입력 배선·birthReadback/bizInvalid 패밀리 공존 [D]무접촉=validate/builders 문구 미혼입·globals 새 클래스 0). `npm run verify`(verify-all) **168/168·단언 4444 PASS / 0 FAIL**(직전 167/4420 → +1 가드·+24 단언) 회귀無. `npx tsc --noEmit` **EXIT 0** / `npx next build` **EXIT 0**(전 10라우트). ※브라우저 스모크(우선수익자 단계+대출금액 입력+채무자/문구 빈칸 트리거)는 이번 iteration 미실행 — 정적 가드가 조건식·렌더 마크업을 verbatim 단언하고 tsc 가 스코프·타입을 보장하나 런타임 트리거 실증은 잔여(다음 iteration 후보, 입력 지점 advisory 누적 스모크 묶음 권장).
  - 다음스텝: (개발 후보) 입력 지점 advisory 브라우저 스모크 묶음 실증(ubo·boardMin·우선수익자 별첨2 누적) / 추가 갈래 발굴 / 모바일 stepper. (★사업) M1 자체판정 공식 추인 + 추가 정본 교차(출시 게이트 핵심·대표님 1차 출시 선언 전제). (마케팅) 상표·도메인 정식 조회(M4) 미착수.
- 09:41 [개발] **★4회 연기된 "입력 지점 advisory 브라우저 스모크 실증" 부채 마감 + 그 흐름에서 발견한 신규 완결성 공백(실제소유자확인서 지분율 빈칸) advisory 신설(+전용 가드) — 검증 우선 + 구현 동시.** (커밋 `c01a16a`)
  - 배경/판단: 오늘 08:06~08:28 4개 iteration이 전부 입력 지점 교차검증 advisory를 추가하면서 **공통으로 "브라우저 스모크 실증"을 다음 iteration으로 연기**(매 건 "정적 가드가 마크업을 단언하나 런타임 트리거 실증은 잔여")했다. 16종 입력 지점 advisory가 전부 정적 가드(verify-*.mjs)만 있고 **playwright 런타임 실증 0건**(repo에 브라우저 스모크 인프라 없음, e2e-engine-test.mjs는 엔진 전용). advisory #5를 또 쌓는 중복 피로 대신, 누적된 실증 부채를 먼저 닫고 그 과정에서 발견한 진짜 공백만 구현하는 쪽을 택함.
  - 검증(playwright MCP, `npx next dev --webpack -p 3100`, /app→한투→담보신탁→신규): 3개 기존 패밀리를 **트리거·소거 양방향 런타임 실증** — ①우선수익자 별첨2(PartyCard): 대출금액 10억 입력→채무자·문구 빈칸 advisory 2종 `role=status` 렌더(한글 "일십억원정" 동시), 채무자 입력→해당만 소거(필드별 가드), 대출금액 비움→loanActive=false로 둘 다 소거. ②ubo 동일·전원법인(DocStep): "동일" 선택+위탁자 법인→"법인은 자기 실소유자 불가" advisory. ③ubo 다름·성명빈칸: "다름"→성명 advisory, 성명 입력→소거. **콘솔 에러 0건**(경고는 전부 Next dev의 global-error.css preload 노이즈=앱 코드 무관). 스크린샷 2매(trust-saas/.playwright-mcp/).
  - 구현(2파일, 스모크 중 발견한 미커버 공백): 위 ubo "다름" 경로에서 성명(uboName) 빈칸은 advisory가 있는데 **지분율(uboShare) 빈칸은 비어 있어**, "다름"+성명만 채우고 지분율을 비우면 산출물 고유정보 표 "지분율 (%)" 칸이 빈칸으로 박혔다(builders.js docRows kvRow). 지분율은 특금법 §5의2상 실제소유자(법인 지분 25%↑ 자연인) 식별 법적 정량값. ①`DocStep.tsx` — `uboShareDistinctMissing`(docId==="ubo" && f.key==="uboShare" && ubo.sameAsTrustor==="no" && val 빈칸) 파생 상수 + uboShare 필드에 advisory 렌더. uboDistinctNameMissing(성명)의 **동형 보완 갈래**(거기 성명·여기 지분율). ★상호배타 = 값 채우면 기존 pctInfo readback(interpretSharePct: 빈 입력 null → 충족/미만)이 대신 떠 둘이 겹치지 않음. ★오탐 방지 = "다름" 명시 선택 + 빈칸일 때만. 새 상태/모델/엔진/조문 무접촉·비차단. ②`scripts/verify-ubo-share-distinct-missing-advisory.mjs` 신규 가드.
  - 검증결과: 신규 가드 **26/26 PASS**([A]DocStep 배선 [B]model/schema/calc 계약+interpretSharePct 빈입력 null 상호배타 [C]무회귀=pctInfo readback·보완 갈래 uboDistinctNameMissing/uboSameAsCorpTrustor 공존 [D]무접촉). `npm run verify` **169/169·단언 4470 PASS / 0 FAIL**(직전 168/4444 → +1 가드·+26 단언) 회귀無. `npx tsc --noEmit` **EXIT 0** / `npx next build` **EXIT 0**(전 10라우트). **신규 advisory도 같은 iteration에 브라우저 런타임 실증**(Fast Refresh): 다름+지분율빈칸→advisory 트리거✓, 지분율 "30" 입력→소거+"지분율 30% · 실제소유자 기준(25% 이상) 충족" readback 인계✓, "동일" 전환→share-distinct advisory 0건(오탐 없음)+uboSameAsCorpTrustor 대체✓.
  - 다음스텝: (개발 후보) 입력 지점 advisory 스모크를 **재사용 가능한 커밋 자산(playwright 스펙)으로 승격** 검토 — 단 playwright dev 의존성 추가는 dependency surface 결정이라 ★대표님/기획 합의 후(현 16종은 정적 가드+이번 수동 스모크로 커버). 추가 갈래 발굴 / 모바일 stepper. (★사업) M1 자체판정 공식 추인 + 추가 정본 교차(출시 게이트 핵심). (마케팅) 상표·도메인 정식 조회(M4) 미착수.
- 08:51 [개발] **내 계약 0건 빈 화면 "새 계약 작성하기" 1차 CTA 신설(+전용 회귀 가드) — ★사용자 가시 UX(첫 사용 흐름 연결), 표시·내비게이션 전용.**
  - 배경/판단: 오늘 08:06~09:41 5개 iteration이 전부 입력 지점 교차검증 advisory(16종 누적)였고 09:41 워크로그가 **"advisory #5를 또 쌓는 중복 피로"** 를 명시했다 → 17번째 advisory 대신, 첫 사용자가 실제로 처음 보는 화면의 진짜 UX 갭을 골라 마감. **내 계약(`ContractsView`)의 계약 0건 빈 화면**은 종전엔 안내 문구 한 줄("아직 저장된 계약이 없습니다…")뿐인 **막다른 화면**이었다 — 계약이 하나도 없을 때(첫 진입·캐시 삭제 = 신규 사용자가 처음 보는 순간)가 정작 "작성을 시작"해야 할 시점인데, 그 자리에서 바로 새 계약 작성으로 갈 길이 없어 상단 breadcrumb 로 되돌아가야 했다(첫 사용 흐름 단절). 빈 화면에 추가 텍스트가 아닌 advisory 갈래를 또 더하는 대신, **1차 CTA(call-to-action)** 를 둬 흐름을 연결.
  - 작업(2파일+가드): ①`ContractsView.tsx` — 옵셔널 `onStart?: () => void` 내비게이션 prop 추가, rows.length===0 빈 화면 분기에 안내 문구 + **btn-primary "+ 새 계약 작성하기"** 버튼(onStart 전달 시에만 렌더=후방호환, 선두 "+" 글리프 aria-hidden, 새 CSS 0=기존 btn-primary/btn-sm/field-hint+인라인 style). 기존 안내 문구는 "…저장하면 여기에 모입니다"로 다듬음(보존). ②`TrustApp.tsx` — `<ContractsView onStart={() => setView(company ? "home" : "company")} />` — goBack 의 contracts 분기(`setView(company ? "home" : "company")`)와 **동일 의미의 내비게이션**(신탁사 정해졌으면 home=서류 선택, 아니면 company=신탁사 선택). 순수 뷰 전환 콜백 — 새 상태/모델/엔진/조문/검증/산출물 무접촉. ③`scripts/verify-contracts-empty-start-cta.mjs` 신규 가드.
  - 검증결과: 신규 가드 **20/20 PASS**([A]ContractsView 배선=onStart 옵셔널 prop·rows===0 분기 내 버튼·onClick=onStart·btn-primary·문구·+ 글리프 aria-hidden·버튼 마크업 파일 전체 1개·안내 문구 유지 [B]TrustApp 배선=onStart 전달+company?home:company 내비게이션·goBack 분기 일관 [C]무회귀=onOpen prop·toolbar rows>0 게이트·필터 빈 결과 안내·visible.map 카드 목록 보존 [D]무접촉=validate/docx import 무변경·globals 새 클래스 0). `npm run verify`(verify-all) **170/170·단언 4490 PASS / 0 FAIL**(직전 169/4470 → +1 가드·+20 단언) 회귀無. `npx tsc --noEmit` **EXIT 0** / `npx next build` **EXIT 0**(전 10라우트). **브라우저 런타임 실증**(playwright MCP, `npx next dev --webpack -p 3100`, localStorage.clear()→계약 0건): /app→내 계약 진입 시 빈 화면에 "+ 새 계약 작성하기" 버튼 렌더✓, ①company===null(신탁사 미선택)에서 CTA 클릭→"신탁사 선택" 화면 이동✓ ②한국투자부동산신탁 선택 후 내 계약→CTA 클릭→"작성할 서류를 선택해 주세요"(서류 선택/home) 이동·breadcrumb 한국투자부동산신탁 표기✓. **콘솔 에러 0건**(경고 1=Next dev global-error.css preload 노이즈=앱 코드 무관).
  - 다음스텝: (개발 후보) 빈 화면 외 다른 첫-사용 흐름 점검(상담 진입 등) / 입력 지점 advisory playwright 스펙 커밋 자산 승격(★대표님/기획 dependency 합의 후) / 모바일 stepper. (★사업) M1 자체판정 공식 추인 + 추가 정본 교차(출시 게이트 핵심·대표님 1차 출시 선언 전제). (마케팅) 상표·도메인 정식 조회(M4) 미착수.
- 09:09 [개발] **브레드크럼 '내 계약 N' 현재 위치 표시(active+aria-current) + 저장 건수 배지(저장소 구독) 신설(+전용 회귀 가드) — ★돌아온 사용자 wayfinding(저장된 작업 인지·현재 위치), 표시·내비게이션 전용.** (커밋 `e4d5a69`)
  - 배경/판단: 직전 08:51(`99611b6`)이 내 계약 0건 빈 화면 CTA로 **첫 사용 흐름**을 연결한 데 이어, 그 흐름의 **대칭 = 돌아온 사용자(저장된 계약 보유) 흐름**의 갭을 골라 마감(또 advisory 갈래를 쌓지 않음). 두 갭: ①상단 브레드크럼은 신탁사·서류·단계 crumb 에는 현재 뷰일 때 `active`+`aria-current="page"`를 붙이는데 **'내 계약' crumb 만 없어**, contracts 뷰에 들어가면 어느 항목도 현재 위치로 표시되지 않았다(company crumb 은 `view!=="contracts"` 로 숨겨짐 → 브레드크럼에 현재 위치 표시 전무·WCAG 2.4.8 Location 일관성 갭). ②돌아온 사용자는 저장된 계약이 있어도 그 신호가 chrome 어디에도 없어, 매번 '신탁사 선택'으로 진입하며 자기 작업이 기다리는지 알 수 없었다(제품의 핵심 = 작업 재개인데 재개 진입점에 신호 0).
  - 작업(3파일+가드): ①`TrustApp.tsx` — '내 계약' crumb 에 `view==="contracts"` 일 때 `active`+`aria-current="page"` 부여(다른 crumb 과 동일 패턴) + 저장 계약이 있으면(`savedCount>0`) 건수 배지(시각 `(N)`=aria-hidden 장식, 의미=`sr-only` ", 저장 N건" — 기존 글리프/의미 분리 컨벤션). 건수는 `useSyncExternalStore(subscribeContracts, contractCount, ()=>0)` 로 저장소 변경에 구독(항상 최신·SSR 스냅샷 0=서버 localStorage 부재 → 하이드레이션 후 클라이언트 실수치). ②`contractRepo.ts` — 저장 건수 구독 인프라(`contractCount`/`subscribeContracts`): `writeAll` 의 `setItem` **성공 후에만** 통지(`emitContractsChanged` — 쓰기 실패=무손상이라 미통지), 다른 탭은 `window "storage"` 이벤트(우리 KEY 또는 전체 clear=null 한정)로 반영, 정리 함수가 리스너·이벤트 둘 다 해제. ★모든 변형(저장·삭제·복원·이름변경·복제·가져오기)이 `writeAll` **단일 경로**를 지나 배지가 저장소와 절대 어긋나지 않는다(staleness 0). ③`verify-chat-dialog-a11y.mjs` — TrustApp 의 react import 정확목록(`{ useEffect, useRef, useState }`) 단언이 `useSyncExternalStore` 추가에 깨져, 의도(=useRef import 됨)대로 **useRef 포함 여부**로 완화(무관 import 추가 내성). ④`scripts/verify-breadcrumb-contracts-count.mjs` 신규 가드. ★표시·내비게이션 전용 — 조문·엔진·검증(validate)·산출물(docx) 무접촉(순수 카운트 `readAll().length`).
  - 검증결과: 신규 가드 **33/33 PASS**([A]contractRepo 구독 인프라=contractCount/subscribeContracts·writeAll 성공 후 emit(setItem·throw 뒤)·storage KEY 한정·정리 해제 [B]TrustApp 배선=useSyncExternalStore·crumb active+aria-current(contracts 한정)·배지 aria-hidden "(N)"+sr-only [C]무회귀=타 crumb active/aria-current·상담 링크·onStart CTA 배선 보존 [D]무접촉=writeAll StorageWriteError 보존·TrustApp validate/docx import 부재·새 CSS 0). `npm run verify`(verify-all) **171/171·단언 4523 PASS / 0 FAIL**(직전 170/4490 → +1 가드·+33 단언, 추가로 완화한 1단언 회귀無). `npx tsc --noEmit` **EXIT 0** / `npx next build` **EXIT 0**(전 10라우트). **브라우저 런타임 실증**(playwright MCP, `npx next dev --webpack -p 3100`, localStorage.clear()): ①0건—내 계약 crumb 배지 없음, contracts 뷰 진입 시 `active`+`aria-current="page"`✓·빈 CTA 보존✓ ②위저드에서 저장→**네비게이션 없이** 브레드크럼 라이브 "내 계약 (1)"·sr-only ", 저장 1건"✓(useSyncExternalStore 반응성) ③contracts 뷰에서 삭제→배지 라이브 소거·crumb active 유지✓(staleness 0). **콘솔 에러 0건**(경고=Next dev global-error.css preload 노이즈=앱 코드 무관).
  - 다음스텝: (개발 후보) 다른 첫/재방문 흐름 점검(상담 진입 등) / 입력 지점 advisory playwright 스펙 커밋 자산 승격(★대표님/기획 dependency 합의 후) / 모바일 stepper. (★사업) M1 자체판정 공식 추인 + 추가 정본 교차(출시 게이트 핵심·대표님 1차 출시 선언 전제). (마케팅) 상표·도메인 정식 조회(M4) 미착수.
- 09:18 [개발] **첫 화면(신탁사 선택)에 "이어서 작업하기" 재개 진입점(저장 N건) 신설(+전용 회귀 가드) — ★돌아온 사용자 재방문 흐름 마감, 표시·내비게이션 전용.** (커밋 `854caad`)
  - 배경/판단: 직전 09:09(`e4d5a69`)이 브레드크럼 "내 계약 (N)" 배지로 **돌아온 사용자가 저장 작업 보유를 인지**하게 했고, 그 직전 08:51(`99611b6`)이 0건 빈 화면 "새 계약 작성하기" CTA로 **첫 사용(start) 흐름**을 연결했다. 남은 대칭 갭 = **재개(resume) 흐름의 진입점**: 돌아온 사용자가 앱에서 **가장 먼저 보는 화면은 CompanyPage**(`view="company"`, "신탁사를 선택해 주세요")인데, 이 첫 화면에서 저장 작업으로 가는 신호가 **상단 브레드크럼의 작은 "(N)" 배지뿐**이라, 작업을 이어가려는 사용자도 본문에서는 신탁사부터 다시 고르거나 작은 배지를 찾아야 했다(제품 핵심 = 작업 재개인데 첫 화면 본문에 재개 CTA 0). 또 advisory 갈래(현 16종)를 쌓지 않고, start(빈 화면 CTA)와 짝이 되는 resume 진입점으로 첫/재방문 흐름 묶음을 닫음.
  - 작업(2파일): ①`TrustApp.tsx` — `CompanyPage` 에 `savedCount: number`·`onResume?: () => void` prop 추가, page-header 아래에 재개 배너 렌더. 조건 = `onResume && savedCount > 0`(저장 0건·콜백 미전달이면 미표출 — **신규 사용자 첫 화면 무변경·후방호환**). 배너 = `role="region"`+`aria-label="저장된 작업 이어서 하기"` 컨테이너 안 안내 문구("저장된 계약 **N건**이 있습니다.", field-hint) + `btn-primary btn-sm` "이어서 작업하기" 버튼(후미 "→" 글리프 aria-hidden=장식 접근명 오염 0). TrustApp 렌더에서 `savedCount={savedCount}`(브레드크럼 배지와 **동일 단일 출처** useSyncExternalStore)·`onResume={() => setView("contracts")}`(브레드크럼 "내 계약" crumb 클릭과 **동일 내비게이션 의미**). 순수 뷰 전환 콜백 — **새 상태/모델/엔진/조문/검증/산출물 무접촉**, 새 CSS 0(기존 field-hint/btn-primary/btn-sm+인라인 style). ②`scripts/verify-company-resume-banner.mjs` 신규 가드.
  - 검증결과: 신규 가드 **25/25 PASS**([A]CompanyPage 배선=savedCount/onResume prop·`onResume && savedCount>0` 가드·role=region+aria-label·문구·btn-primary·"→" aria-hidden·버튼 마크업 파일 전체 1개 [B]TrustApp 배선=savedCount={savedCount}·onResume=setView("contracts")·브레드크럼 crumb 동일 내비 단일출처 [C]무회귀=company-grid·onPick·제목·빈 화면 CTA(onStart)·브레드크럼 "(N)" 배지 보존 [D]무접촉=TrustApp validate/docx import 부재·globals 새 클래스 0). `npm run verify`(verify-all) **172/172·단언 4548 PASS / 0 FAIL**(직전 171/4523 → +1 가드·+25 단언) 회귀無. `npx tsc --noEmit` **EXIT 0** / `npx next build` **EXIT 0**(전 10라우트). **브라우저 런타임 실증**(playwright MCP, `npx next dev --webpack -p 3100`): ①localStorage.clear()→0건—첫 화면에 재개 배너·버튼 **미표출**✓(신규 사용자 무변경) ②계약 2건 시드 후 재진입→배너 렌더·안내 "저장된 계약 2건이 있습니다."·버튼 "이어서 작업하기 →"(→ aria-hidden)✓ ③"이어서 작업하기" 클릭→**내 계약 뷰 이동**(2건 목록 표시)·브레드크럼 "내 계약" `aria-current="page"`·배너 소거✓. **콘솔 에러 0건**(경고 1=Next dev global-error.css preload 노이즈=앱 코드 무관).
  - ⚠관측(인계): `trust-saas/src/lib/engine/clauses/disposalBody.ts` 에 **미커밋 변경(37+/58−)** 이 워킹트리에 dangling — 이번 iteration 산출 아님(오늘 시작 git status 스냅샷에도 없던 변경, 이전 워커 잔재 추정). 처분신탁(R2)은 today-plan상 **연결 보류(import 0)**·조문 소스라 verbatim 대조+사업팀 검수 없이 커밋 불가 → **건드리지 않고 그대로 인계**. ★기획/CEO 원인 점검 필요(누가 언제 편집했는지·유효 여부).
  - 다음스텝: (개발 후보) 상담(/advisor) 진입 흐름 점검(첫/재방문 묶음의 남은 한 축) / 입력 지점 advisory playwright 스펙 커밋 자산 승격(★대표님/기획 dependency 합의 후) / 모바일 stepper. (★거버넌스) disposalBody.ts dangling 변경 원인·처리 결정. (★사업) M1 자체판정 공식 추인 + 추가 정본 교차(출시 게이트 핵심·대표님 1차 출시 선언 전제). (마케팅) 상표·도메인 정식 조회(M4) 미착수.
- 09:32 [개발] **홈 랜딩(/) "이어서 작업하기" 재개 진입점(저장 N건·내 계약 딥링크) 신설(+전용 회귀 가드) — ★재방문 흐름을 제품 실제 진입점(홈)으로 끌어올림, 표시·내비게이션 전용.** (커밋 `16e9e33`)
  - 배경/판단: 직전 3개 iteration(99611b6 0건 빈 화면 CTA=첫 사용·e4d5a69 브레드크럼 "내 계약 (N)" 배지=현재 위치·854caad CompanyPage 재개 배너=재개 진입점)이 첫/재방문 wayfinding 을 마감했으나 **모두 `/app` 내부**에 머물렀다. 정작 돌아온 사용자가 제품에서 **가장 먼저 보는 화면은 홈(`/`)** 인데, 이 진입점엔 저장 작업 신호가 전무해 "서류 자동화" PILLAR(→ /app 신탁사 선택)부터 다시 들어가 작업을 찾아야 했다(재방문 흐름이 정작 홈에서 끊김). 또 advisory 갈래(현 16종)를 쌓지 않고, 마감한 /app 내부 재개 흐름을 **제품 실제 진입점인 홈으로 끌어올려** 한 번에 내 계약으로 보냄. 홈은 PILLAR 카드가 /app(신탁사 선택)으로만 가던 것을, 별도 재개 진입점은 **?view=contracts 딥링크로 내 계약 직행**(더 짧은 동선).
  - 작업(3파일+가드): ①`HomeResumeEntry.tsx`(신규 client island) — `savedCount=useSyncExternalStore(subscribeContracts, contractCount, ()=>0)`(브레드크럼 배지·CompanyPage 배너와 **동일 단일 출처**). `savedCount===0 → null`(신규 사용자 첫 방문 홈 무변경·SSR 스냅샷 0=서버 localStorage 부재 → 하이드레이션 후 저장 있을 때만 표출). >0 이면 `/app?view=contracts` 로 가는 Link("저장된 계약 **N건** — 이어서 작업하기 →"). 장식 글리프(↩·—·→) 전부 aria-hidden(접근명은 가시 텍스트 "저장된 계약 N건 이어서 작업하기"가 전달 — N건↔이어서 사이 실제 공백 보존). 새 CSS 0(기존 토큰 var(--c-paper)/--c-line/--r-lg/--shadow-sm + 인라인 style). ②`page.tsx` — 히어로 문단과 PILLAR 그리드 사이에 `<HomeResumeEntry />` 렌더(page.tsx 는 **서버 컴포넌트 유지** — island 만 클라이언트, RSC 경계 보존). ③`TrustApp.tsx` — 진입 useEffect 가 `?view=contracts` → `setView("contracts")`+주소창 `/app` 정리(replaceState). 기존 `?doc` 딥링크(상담 코파일럿→서류)는 **early return 으로 보존**(두 분기 공존·간섭 0). ★표시·내비게이션 전용 — 조문·엔진·검증(validate)·산출물(docx) 무접촉(순수 카운트 readAll().length + Link). ④`scripts/verify-home-resume-entry.mjs` 신규 가드.
  - 검증결과: 신규 가드 **24/24 PASS**([A]island 배선=use client·useSyncExternalStore 단일출처·savedCount===0 null·/app?view=contracts Link·문구·글리프 aria-hidden [B]page.tsx 배선=import·<HomeResumeEntry/> 렌더·서버 컴포넌트 유지·PILLAR 위 위치 [C]TrustApp 딥링크=?view=contracts→setView contracts·replaceState·기존 ?doc early return 보존 [D]무접촉=island validate/docx import 부재·globals 새 클래스 0·PILLAR 보존). `npm run verify`(verify-all) **173/173·단언 4572 PASS / 0 FAIL**(직전 172/4548 → +1 가드·+24 단언) 회귀無. `npx tsc --noEmit` **EXIT 0** / `npx next build` **EXIT 0**(전 10라우트, **`/` 정적 prerender 유지** — island 가 페이지를 dynamic 으로 강제하지 않음). **브라우저 런타임 실증**(playwright MCP, `npx next dev --webpack -p 3100`): ①localStorage.clear()→0건—홈에 재개 진입점 **미표출**✓(신규 사용자 무변경) ②계약 2건 시드(KEY=`trust_contracts`) 후 홈 재진입→"저장된 계약 2건 — 이어서 작업하기 →" 렌더·**계산된 접근명 "저장된 계약 2건 이어서 작업하기"**(글리프 제외·공백 보존)✓ ③클릭→`/app` 로 주소창 정리(replaceState)·"내 계약" crumb `aria-current="page"`·"저장된 계약" 뷰+저장 목록 표시✓. **콘솔 에러 0건**(경고 2=Next dev global-error.css preload 노이즈=앱 코드 무관). ※테스트 시드 localStorage 정리 완료.
  - 다음스텝: (개발 후보) 상담(/advisor) 진입 흐름 점검(첫/재방문 묶음의 남은 한 축, 본 건으로 서류측 재개 동선은 홈까지 완비) / 입력 지점 advisory playwright 스펙 커밋 자산 승격(★대표님/기획 dependency 합의 후) / 모바일 stepper. (★거버넌스) disposalBody.ts dangling 변경 원인·처리 결정(이번 iteration 무접촉 인계 유지). (★사업) M1 자체판정 공식 추인 + 추가 정본 교차(출시 게이트 핵심·대표님 1차 출시 선언 전제). (마케팅) 상표·도메인 정식 조회(M4) 미착수.
- 09:52 [개발] **대리금융기관(제20조) 지정 ON인데 회사명 빈칸 입력 지점 교차검증 advisory 신설(+전용 회귀 가드) — ★법적 효력이 큰 별첨4 제20조 verbatim 조문(조문 자동반영)에 빈칸 회사명 박힘 방지, 표시 전용·게이트 아님.**
  - 배경/판단: 직전 6개 iteration(08:51~09:32)이 첫/재방문 wayfinding·재개 동선을 홈까지 마감했고 그 앞 08:06~09:41은 입력 지점 advisory 16종을 쌓았다(09:41 worklog가 "advisory #5를 또 쌓는 중복 피로" 명시). wayfinding·관리(ContractsView 검색/정렬/복제/백업)·크로스필러(브레드크럼 💬상담→·챗 FAB)·모바일(stepper 가로 레일 이미 구현)·a11y(advisor 28종 가드)·미저장 유실방지(beforeunload+dirty guard+export/import)가 전부 포화임을 코드로 확인 → 포화 패턴 반복 대신 제품 #1 원칙(정확성·법적 효력 문서)에 직결되는 **명확히 미커버인 완결성 갭**을 골라 마감. 발견: STEP 05 계약 조건의 **대리금융기관(제20조)** 은 "조문 자동반영"(EngineBadge) 항목으로 회사명(agentBank)이 별첨4 제20조 "대리금융기관의 선임" 조문의 `{{AGENT_BANK}}` 자리에 흘러드는데, builders.js:596·annex.ts:106 이 빈 값을 `[              ]`(빈 괄호)로 치환한다(섹션 hint 도 "빈 값이면 빈칸 출력" 명시). 게이트(validateDoc)는 지정 여부만 보고 회사명 채움은 검사하지 않아, 지정만 켜고 이름을 비운 채 진행하면 산출물 제20조에 **빈칸 회사명**이 박혔다. 제20조는 대리금융기관에게 "우선수익자로서의 일체의 권한"을 위임하는 조항이라 **권한을 위임받는 주체가 공란 = 결함 조문**(법적 효력이 큰 갭). 기존 대리금융기관 advisory(verify-conditions-agentbank-solo, "단독+지정"=구조 불일치)는 이 빈칸 완결성을 못 잡았다 — ubo "다름"인데 성명 빈칸·우선수익자 대출 있는데 채무자 빈칸 advisory 와 **동형의 "활성 항목인데 식별 필드 빈칸 → 산출물 빈칸"** 완결성 갈래로, solo advisory 와 직교(서로 다른 root cause)·공존.
  - 작업(1파일+가드): ①`StepConditions.tsx` — `agentBankNameMissing = !!c.agentBankEnabled && (c.agentBank||"").trim().length===0` 파생 상수 + 대리금융기관 회사명 입력 직하(agentBankEnabled 렌더 블록 안)에 advisory 렌더("지정 켰는데 회사명 빈칸 → 별첨4 제20조 빈칸 출력 → 입력 또는 해제 권유"). ★오탐 방지 = 지정을 **명시적으로 켜고**(체크박스 on) 회사명이 trim 빈칸일 때만, 한 글자라도 채우면 미표출(작성 완료 간주), 지정 해제 시 블록 전체 미렌더. 기존 입력(agentBankEnabled·agentBank) 파생이라 **새 상태/모델/엔진/조문 무접촉**, 막지 않음(작성 중 임시 빈칸 등 사용자 선택 보존). role=status·aria-live=polite + 선두 ⚠ aria-hidden, 색 var(--c-brown)(차단 적색 아님), field-hint 재사용(새 CSS 0). ②`scripts/verify-conditions-agentbank-name-missing-advisory.mjs` 신규 가드.
  - 검증결과: 신규 가드 **21/21 PASS**([A]StepConditions 배선=파생 상수·조건·문구 3종(빈칸 사실·제20조 빈칸 출력·입력/해제 권유)·role=status·⚠ aria-hidden·field-hint·brown [B]무회귀=solo advisory(!isMulti) 직교 공존·정족수 disabled·기존 agentBank 체크박스/입력/별첨4 안내·isMulti 파생 보존 [C]무접촉=validate/builders 문구 미혼입·globals 새 클래스 0 [D]산출물 빈칸 근거=builders.js·annex.ts 의 `[      ]` 치환·제20조 "일체의 권한" 위임 verbatim 단언). `npm run verify`(verify-all) **174/174·단언 4593 PASS / 0 FAIL**(직전 173/4572 → +1 가드·+21 단언) 회귀無. `npx tsc --noEmit` **EXIT 0** / `npx next build` **EXIT 0**(전 10라우트). **브라우저 런타임 실증**(playwright MCP, `npx next dev --webpack -p 3100`, /app→한투→담보신탁→신규→STEP 05): ①대리금융기관 지정 ON+회사명 빈칸→새 advisory `role=status` 렌더(solo advisory 와 직교 공존 2종 동시)✓ ②회사명 "한국투자증권 주식회사" 입력→새 advisory 소거, solo 는 단독이라 유지(직교 확인)✓ ③지정 해제→입력칸·advisory 블록 전체 미렌더✓. **콘솔 에러 0건**(경고 2=Next dev global-error.css preload 노이즈=앱 코드 무관).
  - 다음스텝: (개발 후보) 입력 지점 advisory 추가 갈래 발굴(예: 제21조 건축주=수탁자 명의 vs 위탁자 구조) / 상담(/advisor) 진입 흐름 점검 / 입력 지점 advisory playwright 스펙 커밋 자산 승격(★대표님/기획 dependency 합의 후). (★거버넌스) disposalBody.ts dangling 변경 무접촉 인계 유지. (★사업) M1 자체판정 공식 추인 + 추가 정본 교차(출시 게이트 핵심·대표님 1차 출시 선언 전제). (마케팅) 상표·도메인 정식 조회(M4) 미착수.
- 10:34 [개발] **상담 정체성 매칭(canonical 복합어)→strong grounding — 핵심 정의 질문 "관련도 낮음" 오분류 해소(+전용 회귀 가드) — ★공개/정적 배포(KNOWLEDGE-only) 출하 구성의 사용자 가시 품질 결함, grounding 강도 판정 전용·점수/조문/엔진/산출물 무접촉.** (커밋 `c33f26f`)
  - 배경/판단: 7일 advisory(16종)·wayfinding(홈까지) 포화가 코드로 확인돼(09:52 worklog "advisory 중복 피로" 명시) advisory/wayfinding 반복 대신, 제품 #1 원칙(정확성)에 직결되는 **데이터 근거(gap-report) 있는 실제 결함**을 골라 마감. 조사 경로: gap-report 2026-06-22 미적중 최다 질문 = "담보신탁이 무엇인가요?"(8회) — 제품 1차 범위 담보신탁의 **가장 기본 질문**. 조사 결과 ①josa-stem·멀티턴 회수 수정은 이미 적용됨(retrieve 직접 호출 시 trust-collateral score 5 회수✓) ②`advisor-improve.mjs`는 로그의 **frozen topScore 를 재생(再生)**할 뿐 현재 retrieve 로 재채점하지 않아 gap-report 단순 재생성(today-plan P4)은 stale 데이터 반복(실착수 가치 0) ③진짜 결함 = **회수는 되나 점수 5 < STRONG_GROUNDING_SCORE(6) → '약한 grounding' 오분류**: trust-collateral 정확 청크를 받고도 사용자에겐 "⚠ 관련도 낮음" 칩·LLM 엔 WEAK_GROUNDING_NOTE("주제가 어긋났을 수 있음")가 붙음. ★핵심 도메인 정의 질문에 자기 코어 개념을 "관련도 낮음"으로 표시 = 신뢰를 가장 중요한 질문에서 깎음.
  - 작업(3 src + 2 가드 수정 + 1 신규 가드): ①`retrieve.ts` — `Retrieved.identity`(질의가 topic 의 4자 이상 고유 복합어를 조사 제거 후 **정확 일치**로 포함하는지). ★score 무변경(가산만 채점 계약·retrieve 가드 [A]~[J] 전부 보존). 4자 게이트로 일반어(신탁2·구조2·단계2·세제2·pf2) 제외(오탐 차단). ②`system.ts` — `groundingStrength(topScore, identity=false)`: identity 면 점수 미만이어도 strong, 후방호환(1-arg=종전 점수 단독). ③`route.ts` — `groundingStrength(retrieved[0]?.score ?? 0, retrieved[0]?.identity ?? false)`. ④⑤ grounding-confidence/signal 가드에 identity 케이스·2-arg route 정규식 갱신. ⑥`verify-advisor-identity-grounding.mjs` 신규(점수<임계인데 strong·josa경유·오탐차단·점수무변경회귀·후방호환·route배선·멀티턴 10블록).
  - 검증결과: 신규 가드 **21/21 PASS**. `npm run verify` **175/175·단언 4620 PASS / 0 FAIL**(직전 174/4593 → +1 가드·+27 단언, retrieve 채점 가드 회귀無=점수 무변경 입증). `npx tsc --noEmit` **EXIT 0** / `npx next build` **EXIT 0**(전 10라우트). **라이브 dev(3100) 헤더 스모크**: ⚠최초 curl 테스트가 bash 에서 한글 UTF-8 을 mojibake 로 송신→회수 0 오관측, **node fetch(정상 UTF-8) 재현**으로 정정 — `X-Advisor-Grounding=strong` end-to-end 산출·sources 디코드 확인. ★투명성: backdata 인덱스(39221청크, **gitignore=내부전용**) 로드 구성에선 "담보신탁이 무엇인가요?" 회수 1위가 backdata qna 청크 score 7.5라 **이미 strong**(identity 무관) → 이 fix 는 그 구성을 안 바꿈. 하지만 **공개/정적 배포는 backdata 부재 KNOWLEDGE-only**(backdata.ts 주석 명시 지원 모드)라 trust-collateral score 5 가 1위 → **바로 그 출하 구성에서 오분류가 발생**하고 fix 가 정확히 교정(가드 [B][E]로 KNOWLEDGE-only strong 입증). 즉 fix 는 출하 구성 직결 + 다른 구성 무회귀.
  - 다음스텝(★발굴한 후속, backlog 기재): ①`advisor-improve.mjs` 가 frozen topScore 재생 → **현재 retrieve 로 재채점**하도록 개선(코드 수정으로 해소된 미적중이 gap-report 에 반영되게)=자가고도화 루프 정직성. ②backdata qna 청크(×1.25 부스트)가 **큐레이션 core 청크를 일상적으로 상회** → core 정의 질문에서 큐레이션 개념 칩 대신 문서 fragment 가 1위. 회수 품질 정책(사업/기획 검토 안건). ③상담(/advisor) 진입 흐름 점검(첫/재방문 묶음 잔여 축). (★사업) M1 추인. (마케팅) M4 상표·도메인.
- 10:48 [개발] **상담(/advisor) 세션 localStorage 영속 + 빈 상태 "이어서 대화하기" 재개 진입점 신설(신규 sessionRepo + 전용 회귀 가드) — ★재방문 흐름의 "남은 한 축"(상담 측 휘발) 마감, 표시·재개 전용·상담 코어 무접촉.** (커밋 `d72fe65`)
  - 배경/판단: 직전 7~8개 iteration(08:06~10:34)이 입력 지점 advisory 16종 + 서류 측 wayfinding(0건 빈 화면 CTA·브레드크럼 "내 계약 N" 배지·첫화면/홈 재개 진입점)을 마감했고, 그 전 iteration 들의 worklog 가 공통으로 다음스텝에 **"상담(/advisor) 진입 흐름 점검(첫/재방문 묶음의 남은 한 축)"** 을 4회 이상 적어 왔다(매번 연기). 코드 확인 결과 진짜 갭 = **상담 측은 영속 자체가 없음** — 서류 측은 `contractRepo`(localStorage)로 입력을 영속하고 홈·첫화면·브레드크럼에 재개 동선이 완비됐는데, 상담은 대화 상태가 `AdvisorChat` 의 `useState<Msg[]>([])` 메모리에만 있어 **새로고침·이탈 시 진행 중 상담이 통째로 휘발**했다(제품 핵심=작업 재개인데 PILLAR 2 는 재개 0). advisory #17 을 또 쌓는 대신, 명명만 되고 미착수였던 이 대칭 갭을 닫음.
  - 작업(1 신규 + 1 수정 + 1 가드): ①`src/lib/advisor/sessionRepo.ts`(신규) — KEY=`trust_advisor_session`. 순수 `sanitizeForStorage`(완료 라운드만: error 자리표시자·빈/공백 본문(진행 중 빈 답변)·답 없는 마지막 질문 제외, role/content/sources/grounding 만 추림·입력 무변형) + `isValidMsg`(손상 격리) + `loadSession`(SSR 안전·유효본만) + `saveSession`(**best-effort**: 용량초과/시크릿모드 swallow, 남길 라운드 없으면 무동작=기존 저장본 보존) + `clearSession`(removeItem — 비움의 **단일 경로**). contractRepo 패턴(SSR 가드·try/catch·순수 헬퍼 단언)을 따르되 상담 코어와 분리. ②`AdvisorChat.tsx` — `useEffect`로 마운트 적재(`savedSession` 상태, SSR 스냅샷 null → 하이드레이션 일치)·msgs/busy 변경 시 자동 저장(`!busy && msgs.length>0` — 생성 중 partial·빈 상태 미저장)·`resumeSession`(빈 상태에서만 노출되는 진입점 복원)·`newConversation` 가 `clearSession`+진입점 숨김. 빈 상태에 `savedSession` 있을 때만 region 진입점("↩ 지난 상담 N개 질문이 저장돼 있습니다" + btn-primary "이어서 대화하기 →"). **새 상태/모델/엔진/조문/검증/산출물 무접촉**, 새 CSS 0(기존 토큰 var(--c-paper)/--c-line/--r-lg + field-hint/btn-primary/btn-sm + 인라인 style), 장식 글리프(↩·→) aria-hidden. ③`scripts/verify-advisor-session-persistence.mjs` 신규 가드.
  - 검증결과: 신규 가드 **30/30 PASS**([A]sessionRepo 계약=KEY·API 5종·SSR 안전·saveSession 빈이면 보존·best-effort·clear 단일경로 [B]sanitizeForStorage **런타임 실행**=완료 라운드만·error/빈/답없는질문 제외·필드 추림·입력 무변형·isValidMsg 손상격리 [C]AdvisorChat 배선=import·savedSession·마운트 적재·자동저장 의존성[msgs,busy]·resume·newConversation clear [D]빈 상태 진입점=savedSession 조건·role=region·문구·글리프 aria-hidden·제안칩 위 [E]무접촉=retrieve/system/docx/log import 부재·globals 새 클래스 0). `npm run verify`(verify-all) **176/176·단언 4650 PASS / 0 FAIL**(직전 175/4620 → +1 가드·+30 단언) 회귀無. `npx tsc --noEmit` **EXIT 0** / `npx next build` **EXIT 0**(전 10라우트, **/advisor 정적 prerender 유지**). **브라우저 런타임 실증**(playwright MCP, `npx next dev --webpack -p 3100`): ①localStorage.clear()→첫방문 상담 빈 상태에 재개 진입점 **미표출**✓(신규 사용자 무변경) ②세션 시드 후 재진입→region "지난 상담 **2개 질문**"·"이어서 대화하기 →"(→ aria-hidden) 렌더✓ ③"이어서 대화하기" 클릭→2라운드 대화 복원(마크다운 **볼드**·출처 칩 trust-collateral·턴 랜드마크 "내 질문/상담 답변 1·2"·피드백/복사 버튼)·진입점 소거·"＋ 새 대화" 바 노출✓ ④"＋ 새 대화"→msgs 비움+localStorage 키 null+진입점 소거+제안칩 4개 복귀✓ ⑤**자동저장 변별 실증**: 답 없는 마지막 질문 포함 3아이템 시드→복원은 2질문 충실 렌더하나 자동저장 effect 가 미완료 라운드 strip→localStorage 2아이템(끝=assistant)✓(복원 관대·영속 엄격 설계 입증). **콘솔 에러 0건**(경고 1=Next dev global-error.css preload 노이즈=앱 코드 무관). 테스트 localStorage 정리 완료.
  - 다음스텝: (개발 후보) 다중 세션(이력 목록) 보관 검토 — 현재는 단일 활성 세션(마지막 대화)만 영속(★스코프 확대라 기획 합의 후). 상담↔서류 교차 동선(상담 답변 doc-action 후 상담 복귀) 점검 / 입력 지점 advisory playwright 스펙 커밋 자산 승격(★대표님/기획 dependency 합의 후). (★거버넌스) `disposalBody.ts` dangling 변경 원인·처리(이번 iteration 무접촉 인계 유지). (★사업) M1 자체판정 공식 추인 + 추가 정본 교차(출시 게이트 핵심·대표님 1차 출시 선언 전제). (마케팅) 상표·도메인 정식 조회(M4) 미착수.
- 10:54 [개발] **상담 자가고도화 루프 [분석] 단계 "현재 엔진 재채점(re-score)" — gap-report 박제 score 거짓 부채 제거(+전용 회귀 가드) — ★자가고도화 정직성(today-plan P4 "gap-report 재생성"의 root-cause 마감), 분석 전용·src/조문/엔진 무접촉.** (커밋 `09e8b1a`)
  - 배경/판단: today-plan P4 가 "gap-report **재생성**(현 스냅샷 stale) → 실제 미적중률 재측정"을 적었고, 10:34 worklog 가 그 **근본 원인**을 이미 진단해 backlog #1 후속으로 남겼다 — `advisor-improve.mjs` 가 로그의 **기록 시점 박제(frozen) topScore/hit** 으로 미적중을 분류해, 그 사이 retrieve 개선(josa-stem·정체성 grounding)으로 **이미 해소된 질문이 gap-report 에 계속 공백으로 재보고**(자가고도화 루프의 거짓 부채 → 단순 재생성은 stale 데이터 반복·실착수 가치 0). advisory(16종)·wayfinding(홈까지)·상담 영속이 포화임이 코드로 확인된 상태(직전 worklog 들이 "advisory 중복 피로" 반복 명시)라, advisory #17 대신 **데이터 근거 있는 진짜 결함의 root cause** 를 골라 마감.
  - 작업(1 수정 + 1 신규 가드): ①`scripts/advisor-improve.mjs` — `retrieve`(retrieve.ts)·`groundingStrength`/`STRONG_GROUNDING_SCORE`(system.ts) import(verify-* 가드와 동일 TS 로더 경유), `rescore(q)` 헬퍼 신설 = 각 로그 질문을 **현재 retrieve() 로 다시 채점**하고 라우트(route.ts)와 **동일 판정** `groundingStrength(top?.score ?? 0, top?.identity ?? false)` 적용. 공백 = 현재도 weak grounding 인 질문만(frozen score 단독 분류 폐기). 투명성: "기록 시점 미적중이었으나 현재 코드로 strong 전환된 해소 건수(resolvedSinceLog)"를 요약+전용 섹션으로 보고. ★재채점 코퍼스 = KNOWLEDGE 단독(retrieve 의 extra 미주입 = bd- 백데이터는 라우트에서만 주입) = **공개/정적 출하 구성과 동일** → backdata 부재 출하 구성의 실제 회수 품질을 정직 반영. ★멀티턴 맥락(buildRetrievalQuery)은 로그에 없어 단발 질의로 재채점(첫 턴 기준·맥락 의존 후속질문은 보수적으로 공백에 더 잘 잡힘). **분석 전용 — src/조문/knowledge.ts/KNOWLEDGE 코퍼스 무접촉**(엔진은 호출만·읽기 전용), 기록 대상은 gitignore 된 `advisor-logs/gap-report.md` 한 경로뿐(보강은 사업팀 검수 게이트 유지). ②`scripts/verify-advisor-improve-rescore.mjs` 신규 가드.
  - 검증결과: 신규 가드 **18/18 PASS**([A]스크립트 배선=retrieve/groundingStrength/STRONG_GROUNDING_SCORE import·rescore 헬퍼·라우트 동형 판정·strength==="weak" isGap·frozenMiss&&!isGap 해소 델타·재채점 기준 명시·TS 로더 실행 [B]엔진 계약 behavioral=정체성 정의 질문 strong(공백 아님)·코퍼스 밖/빈 질의 weak(공백)·top.identity=true [C]해소 델타 정직성=점수<임계인데 현재 strong → resolved [D]무접촉=gap-report.md 만 기록·knowledge.ts/src 무기록·KNOWLEDGE 무변형). `npm run verify`(verify-all) **177/177·단언 4668 PASS / 0 FAIL**(직전 176/4650 → +1 가드·+18 단언) 회귀無. `npx tsc --noEmit` **EXIT 0** / `npx next build` **EXIT 0**(전 라우트, src 무변경이라 앱 무영향). **실측 재생성**(TS 로더 실행): 총 31질문 중 현재 엔진 공백 **7건(22.6%)**, **해소 8건** — gap-report 2026-06-22 최다 미적중 "담보신탁이 무엇인가요?"(×7)·"우선수익권이 무엇인가요?"가 10:34 identity fix 로 strong 전환돼 **공백→해소 섹션으로 정확히 이동**(거짓 부채 제거 실증). 잔여 진짜 공백 = 코퍼스 밖("건설 현장 공정관리…" score 5)·맥락 의존 후속("그럼 절차는?" score 4)·테스트 노이즈(mojibake 로그·합성 토큰). ※로그의 mojibake 항목(과거 bash-curl 테스트 잔재)은 바이트 손상이라 score 0 으로 정직하게 공백 집계 — 로그 무변형 인계.
  - 다음스텝: (개발 후보) 상담↔서류 교차 동선(상담 답변 doc-action 후 상담 복귀) 점검 / 잔여 진짜 공백("건설 현장"·맥락 의존 후속질문)에 대한 회수 개선 검토 / 입력 지점 advisory playwright 스펙 커밋 자산 승격(★대표님/기획 dependency 합의 후). (★거버넌스) `disposalBody.ts` dangling 처리 확인(4f33e93 에서 커밋된 듯 — 워킹트리 클린 확인). (★사업) M1 자체판정 공식 추인 + 추가 정본 교차(출시 게이트 핵심·대표님 1차 출시 선언 전제). (마케팅) 상표·도메인 정식 조회(M4) 미착수.
- 11:02 [개발] **서류 위저드 진행 중(미저장) 초안 localStorage 자동 영속 + 첫 화면(신탁사 선택) "이어서 작성하기" 복원 진입점 신설(신규 draftRepo + contractStore.restoreDraft 액션 + 전용 회귀 가드) — ★재방문 흐름 묶음의 "마지막 실질 갭"(서류 측 미저장 휘발) 마감, 표시·재개 전용·검증/조문/엔진/산출물 무접촉.** (커밋 `e8912f3`)
  - 배경/판단: 직전 iteration(10:48 `d72fe65`)이 상담(/advisor) 세션을 sessionRepo 로 영속했고, 그 전에는 명시 저장 계약을 contractRepo 로 영속·홈/첫화면/브레드크럼 재개 동선을 완비했다. 그런데 **위저드에서 작성 중이던(아직 "저장" 누르지 않은) 서류 입력은 contractStore(zustand) 메모리에만** 있어, 새로고침·탭 닫기·실수 이탈 시 통째로 휘발했다(beforeunload 는 "막아만 줄 뿐" 복구 불가 — SPA 내 이동엔 발화도 안 함). 서류 입력(당사자·금액·물건·조건)은 **사용자가 가장 많은 노력을 들이는 핵심 자산**이라, 이 휘발이 재방문 흐름 묶음의 마지막 실질 갭이었다. 코드로 직접 확인: `contractStore` 는 zustand `persist` 미사용(localStorage·persist 미들웨어 0) → in-progress form 은 비영속 확정. advisory(16종)·wayfinding(홈까지)·상담 영속이 포화임이 코드로 재확인된 상태라, advisory #17 대신 sessionRepo 와 **동형의 대칭 갭**(서류 측 초안 영속)을 골라 마감.
  - 작업(2 신규 + 1 수정 + 1 가드): ①`src/lib/store/draftRepo.ts`(신규) — KEY=`trust_draft`. `isValidDraft`(docTypeId·두 폼 존재 검사로 손상·이질 데이터 격리) + `loadDraft`(SSR 안전·유효본만) + `saveDraft`(**best-effort**: 용량초과/시크릿모드 swallow) + `clearDraft`(removeItem — 비움의 단일 경로). sessionRepo·contractRepo 패턴(SSR 가드·try/catch·순수)을 따르되 **검증(validate)·산출물(docx) 무접촉**(모델 타입만 import). ②`contractStore.ts` — `restoreDraft` 신규 액션: loadContract 와 달리 **미저장(savedHash=null·currentContractId=null)** 으로 복원해 여전히 dirty(초안=아직 저장 안 한 작업이라 저장본으로 둔갑 금지·beforeunload 계속 경고). 두 폼 blank 한 단계 병합(구버전·부분 초안 키 누락 격리)·form 재계산·작성하던 단계(tab/step) 그대로 복귀. ③`TrustApp.tsx` — 마운트 1회 `loadDraft`→`restorableDraft` 상태(SSR 스냅샷 null·하이드레이션 일치). **자동 저장 effect**: `draftDirty`(서류 선택됨+미저장)면 store 스냅샷 `saveDraft`, not dirty(방금 저장됨)면 `clearDraft`(contractRepo 가 보관), **서류 미선택(docTypeId null)이면 무동작**(★마운트 직후 빈 상태가 복원 대상 초안을 덮어쓰지 않게 — 핵심 안전장치). `resumeDraft`(restoreDraft+위저드 진입·진입점 숨김)·goHome 이탈 확정 시 `clearDraft`(버린 작업 재출현 방지). CompanyPage 에 "작성 중이던 〈서류명〉 서류가 있습니다 + 이어서 작성하기 →" 진입점(저장된 계약 재개 배너보다 위·별개 신호). 새 상태/CSS 0(기존 field-hint/btn-primary/btn-sm+인라인). ④`scripts/verify-trust-draft-persistence.mjs` 신규 가드.
  - 검증결과: 신규 가드 **39/39 PASS**([A]draftRepo 계약=KEY·API 4종·SSR 안전·best-effort·비움 단일경로 [B]isValidDraft 런타임=정상통과·docTypeId빈/폼부재/null 격리 [C]restoreDraft=미저장(savedHash/currentContractId null)·재계산·두 폼 병합·단계 복귀 [D]TrustApp 배선=import·마운트 적재·자동저장(dirty save·저장됨 clear·미선택 무동작)·resume·goHome clear·prop 전달 [E]진입점=초안 있을 때만·role=region·문구·글리프 aria-hidden·btn-primary·재개배너보다 위 [F]무접촉=draftRepo validate/docx import 부재·globals 새 클래스 0). `npm run verify`(verify-all) **178/178·단언 4707 PASS / 0 FAIL**(직전 177/4668 → +1 가드·+39 단언) 회귀無. `npx tsc --noEmit` **EXIT 0** / `npx next build` **EXIT 0**(전 10라우트). **브라우저 런타임 실증**(playwright MCP, `npx next dev --webpack -p 3100`): ①localStorage.clear()→신규 사용자 첫 화면 초안 진입점 **미표출**✓(후방호환) ②신탁사→담보신탁→신규→위탁자명 "여주개발 주식회사" 입력→`trust_draft` 자동 저장(docTypeId=collateral·step=1·trustor 보존)✓ ③`/app` 새로고침(★미저장이라 **beforeunload 경고 정상 발화**=guard 작동 실증, accept)→첫 화면 region "작성 중이던 **담보신탁** 서류가 있습니다"·"이어서 작성하기 →"✓ ④클릭→위저드 STEP 01 복원(위탁자="여주개발 주식회사"·SaveBar "● 저장되지 않은 변경"=미저장 표기 유지✓=저장본 둔갑 안 함) ⑤제목 입력+저장→`trust_draft` **CLEARED**+`trust_contracts` 1건 적립✓(저장되면 초안 비움). **콘솔 에러 0건**(경고 2=Next dev global-error.css preload 노이즈=앱 코드 무관). 테스트 localStorage 정리 완료.
  - 다음스텝: (개발 후보) 다중 초안(여러 서류 동시 진행) 보관 검토 — 현재는 단일 활성 초안(마지막 작성 서류)만 영속(★스코프 확대라 기획 합의 후). 상담↔서류 교차 동선(doc-action 후 복귀) 점검 / 잔여 진짜 공백 회수 개선 / 입력 지점 advisory playwright 스펙 커밋 자산 승격(★대표님/기획 dependency 합의 후). (★사업) M1 자체판정 공식 추인 + 추가 정본 교차(출시 게이트 핵심·대표님 1차 출시 선언 전제). (마케팅) 상표·도메인 정식 조회(M4) 미착수.
- 11:19 [개발] **서류↔상담 왕복 복귀 — 브레드크럼 "💬 상담 →" 크럼을 `/advisor?resume=1` 딥링크로 + 상담 화면이 직전 대화를 빈 상태 추가 클릭 없이 즉시 복원(마운트 effect ?resume=1 분기 + 전용 회귀 가드) — ★상담↔서류 왕복(작업 재개)의 마지막 끊김 마감, 표시·내비게이션·재개 전용·조문/엔진/검증(validate)/산출물(docx) 무접촉.**
  - 배경/판단: 직전 다수 iteration(08:06~11:02)이 입력 지점 advisory 16종 + 서류 측 wayfinding(0건 빈 화면 CTA·브레드크럼 "내 계약 N" 배지·홈/첫화면 재개 진입점·미저장 초안 영속)·상담 세션 영속(10:48 `d72fe65`)을 마감했고, 그 worklog 들이 공통으로 다음스텝에 **"상담↔서류 교차 동선(doc-action 후 복귀) 점검"** 을 반복 명명(여러 차례 연기)했다. 코드 확인(Explore) 결과 진짜 갭 = **돌아오는 방향의 끊김**: 상담은 답변 doc-action(`<<doc:…>>`)으로 "📄 …서류 작성하기"→`/app?doc=…`로 보내고(선도 동선 완비), 상담 세션은 sessionRepo 로 영속되며 빈 상태에 "이어서 대화하기" 진입점도 있으나, 서류(/app)→상담(/advisor) **되돌아오는 동선(브레드크럼 "💬 상담 →")이 단순 `href="/advisor"`** 라 빈 상태를 거쳐 한 번 더 클릭해야 직전 대화가 복원됐다(제품 핵심=작업 재개인데 상담↔서류 왕복의 마지막 끊김). advisory #17 을 또 쌓는 대신, HomeResumeEntry(`?view=contracts`) 자동복원 딥링크 패턴의 **상담 측 대칭**으로 이 왕복을 한 클릭에 닫음.
  - 작업(2 src + 1 신규 가드 + 3 가드 갱신): ①`TrustApp.tsx` — 브레드크럼 "💬 상담 →" 크럼을 `href="/advisor?resume=1"` 딥링크로(가시 문구 "상담"·장식 글리프 💬/→ aria-hidden 보존=접근명 무변경). ②`AdvisorChat.tsx` — 마운트 useEffect 에 `?resume=1` 분기: 저장 세션 있으면 빈 상태 추가 클릭 없이 **즉시 복원**(`setMsgs(saved)`+피드백/복사 초기화+scrollDown=resumeSession 과 동형 효과), 파라미터는 **세션 유무와 무관하게** `replaceState("/advisor")` 로 정리(새로고침·공유 시 잔류 방지). 파라미터 없는 직접 방문은 종전대로 `setSavedSession`(빈 상태 "이어서 대화하기" 진입점 — 후방호환). 저장본 없으면 `?resume=1` 이어도 무동작(빈 상태). **새 상태/모델/엔진/조문/검증/산출물 무접촉·새 CSS 0**(기존 크럼/링크 재사용). ③`scripts/verify-advisor-cross-resume.mjs` 신규 가드. ④기존 가드 4종 갱신: 세션 영속 가드(마운트 effect 단언 직접 방문 경로로 완화)·breadcrumb-a11y·breadcrumb-contracts-count·decorative-text-glyph-a11y 의 상담 크럼 `href="/advisor"` 정확일치 단언을 `?resume=1` 반영(각 가드 실제 의도=`<Link>` crumb·class=crumb·글리프 보존은 유지).
  - 검증결과: 신규 가드 **15/15 PASS**([A]TrustApp 크럼 딥링크=`/advisor?resume=1`·가시 문구/글리프 보존·상담 링크 단일 [B]AdvisorChat 즉시 복원=resume 파라미터 읽기·replaceState 정리·세션 없으면 무동작·setMsgs 복원+초기화·else 직접 방문 setSavedSession [C]무회귀=resumeSession 빈상태 복원·빈 상태 진입점·자동 저장 effect·newConversation clear 보존 [D]무접촉=AdvisorChat/TrustApp 에 retrieve/system/docx import 부재·globals 새 클래스 0). `npm run verify`(verify-all) **179/179·단언 4722 PASS / 0 FAIL**(직전 178/4707 → +1 가드·+15 단언, 갱신 4가드 회귀無). `npx tsc --noEmit` **EXIT 0** / `npx next build` **EXIT 0**(전 10라우트, /·/advisor·/app 정적 prerender 유지). **브라우저 런타임 실증**(playwright MCP, `npx next dev --webpack -p 3100`): ①세션 시드(trust_advisor_session 2아이템) 후 /app 진입→브레드크럼 크럼 `/url: /advisor?resume=1`·접근명 "상담"(글리프 제외)✓, `/advisor?resume=1` 진입→**URL `/advisor`로 정리**(파라미터 제거)+빈 상태 추가 클릭 없이 대화 즉시 복원(article "내 질문 1/상담 답변 1"·**우선수익권** 볼드·trust-collateral 출처·피드백/복사 버튼·"＋ 새 대화" 바)✓ ②직접 방문 `/advisor`(파라미터 없음)→빈 상태 "무엇을 도와드릴까요?"+region "지난 상담 이어서 하기"·"이어서 대화하기"(1개 질문) 진입점 보존(자동 복원 미발동)✓ ③무세션+`/advisor?resume=1`→URL 정리+빈 상태(heading·0 article·크래시 0)✓(후방호환). **콘솔 0 errors** 전 구간(경고=Next dev global-error.css preload 노이즈·앱 무관). 테스트 localStorage 정리 완료.
  - 다음스텝: (개발 후보) 상담 답변 doc-action→서류 진입 시 "상담으로 돌아가기" 명시 진입점 보강 검토(현재는 브레드크럼 크럼으로 왕복 닫힘) / 다중 세션·다중 초안 보관(★스코프 확대=기획 합의 후) / 잔여 진짜 공백("건설 현장"·맥락 의존 후속) 회수 개선 / 입력 지점 advisory playwright 스펙 커밋 자산 승격(★대표님/기획 dependency 합의 후). (★사업) M1 자체판정 공식 추인 + 추가 정본 교차(출시 게이트 핵심·대표님 1차 출시 선언 전제). (마케팅) 상표·도메인 정식 조회(M4) 미착수.
- 11:40 [개발] **★[버그수정·무결성] 상담 doc-action(`/app?doc=`) 딥링크 도착 시 저장된 진행 중 초안이 사용자 입력 0인데도 조용히 삭제되던 유실 결함 차단 — 자동 비움 조건을 "명시 저장됨(savedHash≠null)"으로 좁힘 + 회귀 가드 갱신. 표시/재개·내비게이션 경계 전용·조문/엔진/검증(validate)/산출물(docx) 무접촉.** (커밋 예정)
  - 배경/근거: 직전 다수 iteration이 서류 초안(11:02 `e8912f3`)·상담 세션(10:48)·상담↔서류 왕복(11:19 `2eaa675`)을 영속·재개로 마감하며 제품 #1 가치(작업 재개·무손실)를 집중 구축했다. 그 흐름에서 advisory #17/wayfinding 반복(이미 포화) 대신, **재개 묶음 자체의 정합성을 코드로 정밀 점검**해 실제 데이터 유실 경로를 찾았다. ★루트 코즈: `TrustApp.tsx` 의 초안 자동 영속 effect 가 `if (draftDirty) saveDraft() else clearDraft()` 구조였는데, `isFormDirty(blankForm, savedHash=null)` 는 **손대지 않은 빈 양식이면 false**(불필요 경고 방지 설계, contractStore.ts:314). 그런데 상담 답변의 doc-action(`<<doc:…>>`)으로 도착하는 `/app?doc=collateral` 딥링크 핸들러(TrustApp.tsx:80)가 마운트 1회에 `setDocType(doc)` 로 **docTypeId 만 설정**(setDocType 은 form 무변경·contractStore.ts:115)하면, 새 마운트라 store form 은 blank·savedHash 는 null → effect 가 `not dirty` 로 판정해 **else→`clearDraft()`** 로 빠졌다. 결과: 사용자가 상담 중 "서류 작성하기"를 누른 순간, **이전에 저장돼 있던 진행 중 초안이 한 글자도 입력하기 전에 localStorage 에서 조용히 삭제**되고, 딥링크는 "단계 선택(category)" 뷰로 직행해 첫 화면 "이어서 작성하기" 복원 진입점도 못 보게 했다(상담→서류 동선에서 유실, 제품이 가장 많이 투자한 무손실 가치를 정면으로 깨뜨림). 같은 결함은 새 서류 선택(pickDoc) 직후 빈 양식 찰나에도 동일(초안을 즉시 삭제).
  - 작업(1 src + 1 가드): ①`TrustApp.tsx` — 자동 영속 effect 의 `else { clearDraft() }` 를 `else if (store.savedHash !== null) { clearDraft() }` 로 좁힘. 즉 **명시 저장됨(저장 직후 markSaved 로 savedHash 채워져 not dirty 가 된 상태)일 때만** 비운다(저장본은 contractRepo 가 보관 → 별도 초안 불필요, 종전 의도 보존). `savedHash===null` 인 not-dirty(=손대지 않은 빈 양식)에는 비우지 않아, 딥링크·새 서류 선택이 docTypeId 만 채운 찰나에 저장된 초안이 살아남는다. effect deps 에 `store.savedHash` 추가(저장 기준선 변화 반영). 새 상태/모델/엔진/조문/검증/산출물 무접촉·새 CSS 0. ★단일 출처 보존: dirty 판정·복원·이탈 비움·저장 후 비움 경로 모두 그대로. ②`verify-trust-draft-persistence.mjs` — 종전 `else→clearDraft` 단언을 `else if (savedHash !== null)→clearDraft` 로 갱신 + **무조건 else→clearDraft 부재**(빈 양식 유실 차단) + **deps 에 store.savedHash 포함** 신규 단언 2종 추가(39→41 단언).
  - 검증결과: 갱신 가드 **41/41 PASS**(직전 39, +2 단언). `npm run verify`(verify-all) **179/179·단언 4724 PASS / 0 FAIL**(직전 179/4722 → 가드 수 동일·+2 단언, 회귀無). `npx tsc --noEmit` **EXIT 0** / `npx next build` **EXIT 0**(전 10라우트·정적 prerender 유지). **브라우저 런타임 실증**(playwright MCP, `npx next dev --webpack -p 3100`): ①`/app`→`localStorage.clear()`+초안 시드(`trust_draft`=담보신탁 초안) ②`/app?doc=collateral`(상담 doc-action 도착 시뮬레이션) 진입→**`trust_draft` 그대로 보존**(`draftKeptIntact: true`)·URL `/app` 정리·콘솔 0 errors ✓(종전 코드라면 이 시점에 초안 삭제) ③`/app`(첫 화면) 재진입→복원 진입점 region "작성 중이던 **담보신탁** 서류가 있습니다 · 이어서 작성하기 →" 정상 렌더(11:02 기능 무회귀) ✓. 테스트 localStorage 정리 완료. ※"저장 후 초안 비움"(savedHash≠null) 경로는 가드 구조 단언 + tsc 로 보장(loadContract·markSaved 가 savedHash 설정 → 종전과 동일 비움).
  - 다음스텝: (개발 후보) 단일 초안 KEY 한계상, 딥링크/새 서류로 *다른* 초안 작업을 실제 입력하기 시작하면 단일 `trust_draft` 가 덮어써지는 것은 잔존(다중 초안=★스코프 확대·기획 합의 후) → 그 전까지 "진행 중 초안이 있는데 다른 서류로 진입" 시 비파괴 안내/충돌 표면화 보강 검토(기획 정책 판단 필요). 상담→서류 진입 시 "상담으로 돌아가기" 명시 진입점 / 입력 지점 advisory playwright 스펙 커밋 자산 승격(★대표님/기획 합의 후). (★사업) M1 자체판정 공식 추인 + 추가 정본 교차(출시 게이트 핵심·대표님 1차 출시 선언 전제). (마케팅) 상표·도메인 정식 조회(M4) 미착수.
- 12:05 [개발] **서류 위저드(DocStep) 실시간 미리보기 접기/펼치기 토글 신설(+전용 회귀 가드) — ★미리보기 UX(좁은 화면 force-stack 우회·넓은 화면 입력 집중), 표시 전용·조문/엔진/검증/산출물 무접촉.** (커밋 `15820d8`)
  - 배경/판단: 직전 다수 iteration(08:06~11:40)이 입력 지점 advisory 16종·서류/상담 wayfinding·재개·미저장 무손실을 포화 마감했고(worklog 들이 "advisory 중복 피로"·재개 묶음 완비를 반복 명시), 오늘 inbox 최우선 P0(우선수익자 복수 2인 본문 표/날인 전원 표기)는 이미 `verify-multiparty.mjs`(실제 DOCX 바이트+HTML, 계약서·신청서 2종, 2위탁자·2우선수익자)가 verify-all 에 편입돼 **개발 산출 측 완결+가드 락**(코드 Explore 재확인: 별첨2·날인 모두 [0] 고정 인덱스 없이 전수 iterate)이라 잔여는 사업팀 추인(사람 액션). advisory #17/wayfinding 반복 대신 today-plan P4 의도(위저드/미리보기 UX) 방향에서 **실측 가능한 미리보기 UX 갭**을 골라 마감. ★발견(Explore + globals.css 미디어쿼리 실측): DocStep 2분할(좌 입력/우 sticky 미리보기 iframe)은 좁은 화면(≤1080px)에서 미디어쿼리가 미리보기를 단일 열 **맨 위(order:-1)·60vh 고정**으로 올려, 사용자가 입력란에 닿으려면 미리보기를 지나쳐 아래로 스크롤하고 반영을 보려면 다시 위로 스크롤해야 했다(2분할 WYSIWYG 이점 상실). 또 넓은 화면에서도 입력 집중 시 미리보기 열을 접을 길이 없었다. ※대안 검토·기각: 필드↔미리보기 스크롤 동기화(가장 고영향)는 iframe `sandbox=""`(same-origin 차단=의도적 보안) 때문에 고위험이라 제외, 빈 상태 템플릿 스켈레톤은 verbatim 산출 엔진(builders.js) 접촉이라 제외. ★날인 "대표자 " 접두는 개인 당사자에도 노출되나 `verify-appform-rep-individual.mjs` 가 **조사분석서 verbatim 양식 구조 보존**으로 의도적으로 락한 결정(값만 비움)이라 무접촉(임의 "수정" 금지).
  - 작업(2 src + 1 신규 가드 + 1 가드 갱신): ①`DocStep.tsx` — `previewOpen` 상태(useState(true)=기본 펼침·후방호환). 외곽 div 클래스 조건부(`previewOpen ? "doc-split" : "doc-split doc-split--preview-collapsed"`). 미리보기 머리말에 토글 버튼(`preview-toggle`·onClick `setPreviewOpen((v)=>!v)`·`aria-expanded`·`aria-controls="doc-preview-body"`·가시 라벨 접기/펼치기·선두 글리프 ▾/▸ aria-hidden·previewHtml 유무 무관 항상 노출). 접기 대상 본문을 `<div id="doc-preview-body" hidden={!previewOpen}>` 래퍼로 묶음(iframe **언마운트 안 함**=hidden 만 토글→펼칠 때 재로딩 없이 직전 미리보기 즉시 복원, `previewNote`(팝업 차단 안내)는 래퍼 **밖**=접어도 유지). ②`globals.css` — `.doc-split--preview-collapsed{grid-template-columns:1fr}` + 접힘 시 미리보기 `order:-1·position:static·max-height:none`(슬림 머리말 바를 항상 위로=다시 펼치기 쉽게) + `.doc-preview-body{flex:1…}`(펼침 시 iframe 이 남은 높이 채움) + `.preview-toggle`(크게 보기와 동일 알약형·margin-left:auto). **새 상태/모델/엔진/조문/검증/산출물 무접촉**(previewOpen 은 순수 레이아웃 상태·미리보기 생성 useMemo 의존성 `[debouncedForm, docId]`·validateDoc 의존성 무변경). ③`scripts/verify-docstep-preview-collapse.mjs` 신규 가드. ④`verify-docstep-livestatus.mjs` 갱신: SR 영속 라이브 영역 위치 단언이 루트 `<div className="doc-split">` 리터럴에 앵커돼 있었는데 클래스가 조건부로 바뀌어, 의도(=SR 영역이 doc-split 루트 직후·입력 컬럼 앞) 보존하며 앵커를 `className={previewOpen ?` 시작부로 갱신.
  - 검증결과: 신규 가드 **26/26 PASS**([A]상태/외곽 클래스 [B]토글 배선=onClick·aria-expanded/controls·글리프 aria-hidden·라벨·파일 전체 1개 [C]본문 래퍼=id·hidden·previewNote 밖·iframe 안 [D]무회귀=크게 보기·iframe sandbox=""·초안 배지·갱신 표시 [E]CSS=단일 열·order:-1·doc-preview-body flex·preview-toggle [F]무접촉=previewDocHTML/validateDoc 의존성 무변경). `npm run verify`(verify-all) **180/180·단언 4750 PASS / 0 FAIL**(직전 179/4724 → +1 가드·+26 단언, livestatus 가드 31/1→32/0 앵커 갱신 회귀無). `npx tsc --noEmit` **EXIT 0** / `npx next build` **EXIT 0**(전 10라우트·정적 prerender 유지). **브라우저 런타임 실증**(playwright MCP, `npx next dev --webpack -p 3100`, /app→한투→담보신탁→신규→Doc 01): ①데스크톱(1280) 펼침=2열 그리드(353+371px)·iframe 렌더 ②접기 클릭→단일 열(747px)·입력 컬럼 전체 폭·`#doc-preview-body` display:none·토글 "▸ 펼치기" aria-expanded=false·크게보기/초안배지 머리말 유지 ③펼치기 클릭→2열 복원·iframe 가시·**재로딩 없이 직전 본문 즉시 복원**(hidden 토글) ④**좁은 화면(760) 실측**: 펼침=미리보기 order:-1 540px 가 입력 위를 막아 폼 top 1460 → 접기→미리보기 114px(슬림 머리말 바)로 축소·**폼 +932px 상승**(top 528)·슬림 바는 위에 유지(원클릭 재펼침). **콘솔 0 errors** 전 구간(경고 3=Next dev global-error.css preload 노이즈·앱 무관). 스크린샷 3매(.playwright-mcp/preview-collapse-{1-expanded,2-collapsed,3-narrow-collapsed}.png).
  - 다음스텝: (개발 후보) JointForm(공동사업협약) 미리보기에도 동일 토글 이식(현재 DocStep 한정) / 필드↔미리보기 스크롤 동기화는 iframe sandbox 정책 변경 동반이라 ★보안 합의 후 / 미리보기 접힘 상태 세션 영속 검토(현 in-memory·기본 펼침). (★사업) M1 자체판정 공식 추인 + 추가 정본 교차(출시 게이트 핵심·대표님 1차 출시 선언 전제). (★거버넌스) inbox P0(우선수익자 복수)는 개발 산출+verify-multiparty 가드로 완결 — 잔여=사업팀 verbatim 추인(사람). (마케팅) 상표·도메인 정식 조회(M4) 미착수.
- 12:17 [개발] **공동사업표준협약서(JointForm) 실시간 미리보기 접기/펼치기 토글 신설(+전용 회귀 가드) — DocStep 패리티(직전 12:05 다음스텝 최상위 후보 마감), 표시·레이아웃 전용·조문/엔진/검증(validateJoint)/산출물(docx) 무접촉.** (커밋 `fbd2f30`)
  - 배경/판단: 직전 12:05(`15820d8`)이 담보신탁 DocStep 미리보기에 접기/펼치기 토글을 신설하며 다음스텝 **최상위 개발 후보로 "JointForm(공동사업협약) 미리보기에도 동일 토글 이식(현재 DocStep 한정)"** 을 명시했다. 코드 확인(Read) 결과 JointForm 도 DocStep 과 **동일 .doc-split 2분할**(좌 입력/우 sticky 실시간 미리보기 iframe·동일 클래스 doc-split/doc-split-input/doc-split-preview)인데 토글만 없어 비대칭이 남아 있었다 — 좁은 화면(≤1080px)에서 미디어쿼리가 미리보기를 단일 열 맨 위(order:-1)·60vh 고정으로 올려 입력란에 닿으려면 미리보기를 지나쳐 스크롤해야 했고(WYSIWYG 이점 상실), 넓은 화면에서도 입력 집중 시 미리보기를 접을 길이 없었다. advisory #17/wayfinding 반복(이미 포화) 대신 직전 iteration 이 직접 지목한 패리티 갭을 마감. ★12:05 에 추가된 CSS(globals.css 의 `.doc-split--preview-collapsed`·`.doc-preview-body`·`.preview-toggle`)는 클래스 기반이라 JointForm 의 동일 .doc-split 에 **그대로 적용**(신규 CSS 0·코드 재사용).
  - 작업(1 src + 1 신규 가드 + 1 가드 갱신): ①`JointForm.tsx` — `previewOpen` 상태(useState(true)=기본 펼침·후방호환). 외곽 div 클래스 조건부(`previewOpen ? "doc-split" : "doc-split doc-split--preview-collapsed"`). 미리보기 머리말에 토글 버튼(`preview-toggle`·onClick `setPreviewOpen((v)=>!v)`·`aria-expanded`·`aria-controls="joint-preview-body"`·가시 라벨 접기/펼치기·선두 글리프 ▾/▸ aria-hidden·previewHtml 유무 무관 항상 노출·type=button). 접기 대상 본문(iframe/빈 상태)을 `<div id="joint-preview-body" className="doc-preview-body" hidden={!previewOpen}>` 래퍼로 묶음(iframe **언마운트 안 함**=hidden 만 토글→펼칠 때 재로딩 없이 직전 미리보기 즉시 복원, `previewNote`(팝업 차단 안내)는 래퍼 **밖**=접어도 유지). ★DocStep 과 별개 id(`joint-preview-body`)=두 컴포넌트 동시 마운트 안 되나 aria-controls 정합·중복 id 방지. **새 상태/모델/엔진/조문/검증/산출물 무접촉**(previewOpen 은 순수 레이아웃·미리보기 useMemo 의존성 `[debouncedForm]`·validateJoint 의존성 무변경). ②`scripts/verify-jointform-preview-collapse.mjs` 신규 가드. ③`verify-joint-preview-window.mjs` 갱신: `.doc-split` 리터럴 앵커 단언이 조건부 클래스로 깨져, 의도(=2분할 doc-split 도입) 보존하며 펼침 분기 `className={previewOpen ? "doc-split"` 로 앵커 갱신(DocStep 12:05 의 livestatus 앵커 갱신과 동형).
  - 검증결과: 신규 가드 **25/25 PASS**([A]상태/외곽 조건부 클래스 [B]토글 배선=onClick·aria-expanded/controls(joint-preview-body)·글리프 aria-hidden·라벨·type=button·파일 전체 1개 [C]본문 래퍼=id·hidden·previewNote 밖·iframe 안 [D]무회귀=크게 보기·iframe sandbox=""·초안 배지·갱신 표시·실시간 배지 [E]CSS 공유=단일 열·order:-1·doc-preview-body flex·preview-toggle [F]무접촉=previewJointHTML/[debouncedForm]·validateJoint 의존성 무변경). `npm run verify`(verify-all) **181/181·단언 4775 PASS / 0 FAIL**(직전 180/4750 → +1 가드·+25 단언, joint-preview-window 가드 39/1→40/0 앵커 갱신 회귀無). `npx tsc --noEmit` **EXIT 0** / `npx next build` **EXIT 0**(전 10라우트·정적 prerender 유지). **브라우저 런타임 실증**(playwright MCP, `npx next dev --webpack -p 3100`, /app→한투→공동사업표준협약서→신규): ①데스크톱(1280) 펼침=2열 그리드(531+557px)·toggle "▾ 접기" aria-expanded=true·iframe 렌더·입력 컬럼 531px ②접기 클릭→단일 열(1111px)·**입력 컬럼 531→1111px 전체 폭**·`#joint-preview-body` display:none·toggle "▸ 펼치기" aria-expanded=false·크게보기/실시간배지 머리말 유지·iframe DOM 유지(언마운트 안 함) ③펼치기 클릭→2열 복원(531+557)·iframe 가시·**재로딩 없이 즉시 복원**(hidden 토글). **콘솔 에러 0건** 전 구간(경고 2=Next dev global-error.css preload 노이즈·앱 무관).
  - 다음스텝: (개발 후보) 미리보기 접힘 상태 세션/로컬 영속 검토(현 in-memory·기본 펼침·DocStep·JointForm 공통) / 필드↔미리보기 스크롤 동기화는 iframe sandbox 정책 변경 동반이라 ★보안 합의 후 / 입력 지점 advisory playwright 스펙 커밋 자산 승격(★대표님/기획 dependency 합의 후). (★사업) M1 자체판정 공식 추인 + 추가 정본 교차(출시 게이트 핵심·대표님 1차 출시 선언 전제). (★거버넌스) docs/legal 3종·_review-package.md 등 워킹트리 dangling 변경(이번 iteration 산출 아님·법무 소관)은 무접촉 인계. (마케팅) 상표·도메인 정식 조회(M4) 미착수.
- 12:38 [개발] **실시간 미리보기 접기/펼치기 선호 localStorage 영속 — 직전 12:17 다음스텝 최상위 후보(접힘 상태 in-memory 휘발) 마감 + DocStep·JointForm 공용 단일 출처(신규 previewPref + usePreviewOpen 훅 + 전용 회귀 가드) — ★미리보기 UX(문서 간·새로고침 후 선호 유지), 표시 전용·조문/엔진/검증/산출물 무접촉.** (커밋 예정)
  - 배경/판단: 직전 2개 iteration(12:05 `15820d8` DocStep·12:17 `fbd2f30` JointForm)이 미리보기 접기/펼치기 토글을 신설하며 **공통으로 다음스텝 최상위 후보로 "미리보기 접힘 상태 세션/로컬 영속 검토(현 in-memory·기본 펼침·DocStep·JointForm 공통)"** 를 명시했다. 코드 확인 결과 진짜 갭 = 두 컴포넌트 모두 `const [previewOpen, setPreviewOpen] = useState(true)` 라 **선택이 컴포넌트 in-memory 상태**에만 있어, 문서를 바꾸거나(DocStep 단계 전환=언마운트→재마운트) 새로고침하면 매번 기본 펼침으로 되돌아갔다 — 좁은 화면에서 미리보기를 접어 입력에 집중하던 사용자가 단계를 옮길 때마다 다시 접어야 했다. 오늘 inbox P0(우선수익자 복수)는 이미 `verify-multiparty` 가드로 개발 산출 완결(잔여=사업팀 추인=사람 액션)이고 advisory(16종)·wayfinding 포화가 worklog 에 반복 확인된 상태라, advisory #17 대신 직전 iteration 들이 직접 지목한 영속 갭을 마감.
  - 작업(2 신규 + 2 src 수정 + 1 신규 가드 + 2 가드 갱신): ①`src/lib/store/previewPref.ts`(신규) — KEY=`trust_preview_open`. `loadPreviewOpen()`(SSR 안전·미저장/손상 시 기본 펼침 true·"0"만 접힘) + `savePreviewOpen(open)`(best-effort: 용량초과/시크릿모드 swallow). contractRepo/sessionRepo/draftRepo 패턴(SSR 가드·try/catch)을 따르되 **import 0(순수 localStorage 유틸·의존성 표면 0)**. ②`src/lib/store/usePreviewOpen.ts`(신규 공용 훅) — `[previewOpen, toggle]` 튜플 반환. ★SSR/하이드레이션 안전: 초기값 항상 `useState(true)`(서버 마크업 일치) + 마운트 effect 에서 `setPreviewOpen(loadPreviewOpen())` 로 저장 선호 반영(HomeResumeEntry·draft 복원과 동형). ★영속은 **사용자 토글에서만**(`setPreviewOpen` 업데이터 안 `savePreviewOpen(next)`) — 마운트 적재 경로엔 save 없어 저장값을 자기 자신으로 덮어쓰지 않음(effect 순서/클로버 문제 원천 차단). ③`DocStep.tsx`·④`JointForm.tsx` — 로컬 `useState(true)`/인라인 `onClick={() => setPreviewOpen((v)=>!v)}` 를 `usePreviewOpen()`/`onClick={togglePreview}` 로 위임(두 컴포넌트 **동일 KEY 공유**=선호 공통). 마크업·aria·CSS 무변경(순수 상태 출처 교체). ⑤`scripts/verify-preview-open-persistence.mjs` 신규 가드. ⑥⑦ 기존 preview-collapse 가드 2종([A] previewOpen 출처·[B] onClick 단언)을 새 배선으로 갱신(의도=기본 펼침·토글 보존).
  - 검증결과: 신규 가드 **32/32 PASS**([A]previewPref 계약=KEY·API 2종·SSR 안전·기본 펼침·best-effort [B]**previewPref 런타임 실행**(window/localStorage 스텁)=미저장→true·저장 0/1 왕복·손상→true·SSR→true·쓰기실패 swallow [C]usePreviewOpen=use client·useState(true)·마운트 load effect·토글 updater 안 save 1곳뿐(클로버 차단)·튜플 반환 [D]DocStep/JointForm 배선=import·usePreviewOpen()·onClick=togglePreview·옛 setPreviewOpen 잔재 0 [E]무접촉=engine/validate/docx 무import). `npm run verify`(verify-all) **182/182·단언 4809 PASS / 0 FAIL**(직전 181/4775 → +1 가드·+34 단언: 신규 32 + 갱신 2가드 각 +1). `npx tsc --noEmit` **EXIT 0** / `npx next build` **EXIT 0**(전 10라우트·/·/advisor·/app 정적 prerender 유지). **브라우저 런타임 실증**(playwright MCP, `npx next dev --webpack -p 3100`, /app→한투→담보신탁→신규→담보신탁계약서 DocStep): ①첫 진입(미저장)→토글 "▾ 접기"·aria-expanded=true·`doc-split`·`trust_preview_open`=null(기본 펼침·후방호환)✓ ②접기 클릭→"▸ 펼치기"·aria-expanded=false·`doc-split doc-split--preview-collapsed`·저장값 **"0"**✓ ③**STEP 01(비-DocStep)로 이동→DocStep 언마운트**(.preview-toggle 부재)→**담보신탁계약서 재진입(remount)**→토글이 저장값 "0" 읽어 **접힘 상태로 복원**("▸ 펼치기"·collapsed)✓(=문서 간 유지·load effect 실증) ④펼치기 클릭→"▾ 접기"·저장값 **"1"** 왕복✓. **콘솔 에러 0건** 전 구간(하이드레이션 미스매치 0·경고는 Next dev preload 노이즈). 테스트 localStorage 정리 완료.
  - 다음스텝: (개발 후보) 미리보기 크게보기(새 창)·기타 표시 선호 영속 확장 검토 / 필드↔미리보기 스크롤 동기화는 iframe sandbox 정책 변경 동반이라 ★보안 합의 후 / 입력 지점 advisory playwright 스펙 커밋 자산 승격(★대표님/기획 dependency 합의 후). (★사업) M1 자체판정 공식 추인 + 추가 정본 교차(출시 게이트 핵심·대표님 1차 출시 선언 전제). (★거버넌스) docs/legal 3종·_review-package.md 등 워킹트리 dangling 변경(이번 iteration 산출 아님·법무 소관)은 무접촉 인계. (마케팅) 상표·도메인 정식 조회(M4) 미착수.
